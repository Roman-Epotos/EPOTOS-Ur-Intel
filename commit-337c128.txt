commit 337c12828437377e581b7064c4d77e66442c714e
Author: Roman-Epotos <strat.epotos@gmail.com>
Date:   Thu Jul 2 12:28:54 2026 +0300

    fix: redirect ╤Б╤В╤А╨░╨╜╨╕╤Ж╨░ ╨┤╨╗╤П ╤Б╨╛╤Е╤А╨░╨╜╨╡╨╜╨╕╤П contract_id ╨▓ localStorage ╨┐╨╡╤А╨╡╨┤ ╨╛╤В╨║╤А╤Л╤В╨╕╨╡╨╝ 24

diff --git a/app/hooks/useBitrixAuth.ts b/app/hooks/useBitrixAuth.ts
index 487b3d7..5d2a3a1 100644
--- a/app/hooks/useBitrixAuth.ts
+++ b/app/hooks/useBitrixAuth.ts
@@ -65,7 +65,9 @@ export function useBitrixAuth() {
             }))
             const contractId = params.get('contract_id')
               ?? sessionStorage.getItem('pending_contract_id')
+              ?? localStorage.getItem('redirect_contract_id')
             sessionStorage.removeItem('pending_contract_id')
+            localStorage.removeItem('redirect_contract_id')
             if (contractId) {
               window.location.replace(`/contracts/${contractId}`)
             } else if (!window.location.pathname.startsWith('/contracts/')) {
diff --git a/app/lib/notify.ts b/app/lib/notify.ts
index 57b52b3..dba01a8 100644
--- a/app/lib/notify.ts
+++ b/app/lib/notify.ts
@@ -100,7 +100,7 @@ export async function createBitrixChat(opts: {
     if (data.error) { console.error('Bitrix chat create error:', data.error); return null }
     const chatId = data.result
     const link = opts.contract_id
-      ? `https://epotos-ur-intel.vercel.app/contracts/${opts.contract_id}`
+      ? `https://epotos-ur-intel.vercel.app/redirect?contract_id=${opts.contract_id}`
       : null
     const docRef = link
       ? `[URL=${link}]${opts.document_number} тАФ ${opts.document_title}[/URL]`
diff --git a/app/redirect/page.tsx b/app/redirect/page.tsx
new file mode 100644
index 0000000..dcf981e
--- /dev/null
+++ b/app/redirect/page.tsx
@@ -0,0 +1,28 @@
+'use client'
+
+import { useEffect } from 'react'
+import { useSearchParams } from 'next/navigation'
+import { Suspense } from 'react'
+
+function RedirectContent() {
+  const searchParams = useSearchParams()
+
+  useEffect(() => {
+    const contractId = searchParams.get('contract_id')
+    if (contractId) {
+      localStorage.setItem('redirect_contract_id', contractId)
+    }
+    // ╨Я╨╡╤А╨╡╨╜╨░╨┐╤А╨░╨▓╨╗╤П╨╡╨╝ ╨▓ ╨С24 ╨┤╨╗╤П ╨╛╤В╨║╤А╤Л╤В╨╕╤П ╨┐╤А╨╕╨╗╨╛╨╢╨╡╨╜╨╕╤П
+    window.location.replace('https://gkepotos.bitrix24.ru/marketplace/app/252/')
+  }, [])
+
+  return <div className="flex items-center justify-center h-screen text-gray-500">╨Я╨╡╤А╨╡╨╜╨░╨┐╤А╨░╨▓╨╗╨╡╨╜╨╕╨╡...</div>
+}
+
+export default function RedirectPage() {
+  return (
+    <Suspense fallback={<div className="flex items-center justify-center h-screen">╨Ч╨░╨│╤А╤Г╨╖╨║╨░...</div>}>
+      <RedirectContent />
+    </Suspense>
+  )
+}
\ No newline at end of file
diff --git a/repomix-api-yurintel.txt b/repomix-api-yurintel.txt
index 1c2bfc6..cd26eb3 100644
--- a/repomix-api-yurintel.txt
+++ b/repomix-api-yurintel.txt
@@ -4981,400 +4981,125 @@ export function renderDocumentTypeOptions() {
 }
 
 ================
-File: app/api/approvals/[sessionId]/approve/route.ts
+File: app/api/approvals/[sessionId]/revoke/route.ts
 ================
 import { createClient } from '@supabase/supabase-js'
 import { NextRequest, NextResponse } from 'next/server'
-import { sendBitrixNotify, sendBitrixMessage, sendBitrixChatMessage } from '@/app/lib/notify'
 
 const supabase = createClient(
   process.env.NEXT_PUBLIC_SUPABASE_URL!,
   process.env.SUPABASE_SECRET_KEY!
 )
 
+const ADMIN_IDS = [30, 1148]
+const CORS = { 'Access-Control-Allow-Origin': '*' }
+
+export async function OPTIONS() {
+  return new NextResponse(null, {
+    headers: { ...CORS, 'Access-Control-Allow-Methods': 'POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' }
+  })
+}
+
 export async function POST(
   request: NextRequest,
   { params }: { params: Promise<{ sessionId: string }> }
 ) {
   try {
     const { sessionId } = await params
-    const body = await request.json()
-    const { participant_id, comment, user_name, contract_id } = body
-    const isAcknowledge = body.is_acknowledge === true
-    const isRejected = body.is_rejected === true
+    const { participant_id, reason, user_bitrix_id, user_name, contract_id } = await request.json()
 
-    // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░
-    const { error: updateError } = await supabase
+    if (!participant_id || !reason?.trim()) {
+      return NextResponse.json({ error: 'participant_id ╨╕ ╨┐╤А╨╕╤З╨╕╨╜╨░ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400, headers: CORS })
+    }
+
+    const isAdmin = ADMIN_IDS.includes(parseInt(user_bitrix_id))
+
+    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░
+    const { data: participant, error: pErr } = await supabase
       .from('approval_participants')
-      .update({
-        status: isAcknowledge ? 'acknowledged' : isRejected ? 'rejected' : 'approved',
-        comment: comment || null,
-        decided_at: new Date().toISOString(),
-      })
+      .select('id, status, role, user_name, bitrix_user_id, stage')
       .eq('id', participant_id)
+      .single()
 
-    if (updateError) {
-      return NextResponse.json({ error: updateError.message }, { status: 400 })
+    if (pErr || !participant) {
+      return NextResponse.json({ error: '╨г╤З╨░╤Б╤В╨╜╨╕╨║ ╨╜╨╡ ╨╜╨░╨╣╨┤╨╡╨╜' }, { status: 404, headers: CORS })
     }
 
-    // ╨Я╤А╨╕ ╨╛╤В╨║╨╗╨╛╨╜╨╡╨╜╨╕╨╕ тАФ ╨╝╨╡╨╜╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╨║╨╛╨╜╤В╤А╨░╨║╤В╨░ ╨╕ ╤Б╨╡╤Б╤Б╨╕╨╕
-    if (isRejected) {
-      const { data: sessionData } = await supabase
-        .from('approval_sessions')
-        .select('contract_id')
-        .eq('id', sessionId)
-        .single()
-
-      const actualContractId = sessionData?.contract_id ?? contract_id
-
-      await supabase.from('contracts').update({ status: '╨╛╤В╨║╨╗╨╛╨╜╤С╨╜' }).eq('id', actualContractId)
-      await supabase.from('approval_sessions').update({ status: 'cancelled' }).eq('id', sessionId)
+    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╤А╨░╨▓╨░
+    const isOwnVote = parseInt(user_bitrix_id) === participant.bitrix_user_id
+    if (!isAdmin && !isOwnVote) {
+      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╨╛╤В╨╝╨╡╨╜╤Л ╤Н╤В╨╛╨│╨╛ ╨│╨╛╨╗╨╛╤Б╨░' }, { status: 403, headers: CORS })
+    }
 
-      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░
+    // ╨У╨Ф ╨╝╨╛╨╢╨╡╤В ╨╛╤В╨╝╨╡╨╜╨╕╤В╤М ╤В╨╛╨╗╤М╨║╨╛ ╨╡╤Б╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╡╤Й╤С ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜
+    if (!isAdmin && isOwnVote) {
       const { data: contractData } = await supabase
         .from('contracts')
-        .select('title, number, author_bitrix_id')
-        .eq('id', actualContractId)
+        .select('status')
+        .eq('id', contract_id)
         .single()
 
-      if (contractData) {
-        const { data: participantsData } = await supabase
-          .from('approval_participants')
-          .select('bitrix_user_id')
-          .eq('session_id', sessionId)
-          .not('bitrix_user_id', 'is', null)
-
-        const participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
-        const rejectedRecipients = [...new Set([
-          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
-          ...participantIds,
-        ])]
-
-        await sendBitrixNotify({
-          recipients: rejectedRecipients,
-          type: 'document_rejected',
-          document_id: actualContractId,
-          document_title: contractData.title ?? '',
-          document_number: contractData.number ?? '',
-          extra: comment ?? undefined,
-        })
-        
-
-        // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨│╤А╤Г╨┐╨┐╨╛╨▓╨╛╨╣ ╤З╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б24
-        const { data: sessionInfo } = await supabase
-          .from('approval_sessions')
-          .select('bitrix_chat_id')
-          .eq('id', sessionId)
-          .single()
-        if (sessionInfo?.bitrix_chat_id) {
-          const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
-          const link = `https://${bitrixPortal}/contracts/${actualContractId}`
-          await sendBitrixChatMessage(
-            sessionInfo.bitrix_chat_id,
-            `тЭМ ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╛╤В╨║╨╗╨╛╨╜╤С╨╜: ${contractData.number} тАФ ${contractData.title} [${link}]${comment ? `\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${comment}` : ''}`
-          )
-        }
+      if (contractData?.status === '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜') {
+        return NextResponse.json({ error: '╨Э╨╡╨╗╤М╨╖╤П ╨╛╤В╨╝╨╡╨╜╨╕╤В╤М ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ тАФ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Г╨╢╨╡ ╨┐╨╡╤А╨╡╤И╤С╨╗ ╨▓ ╤Б╤В╨░╤В╤Г╤Б ┬л╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜┬╗. ╨Ю╨▒╤А╨░╤В╨╕╤В╨╡╤Б╤М ╨║ ╨░╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А╤Г.' }, { status: 403, headers: CORS })
       }
     }
 
-    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
-    await supabase
-      .from('contract_logs')
-      .insert({
-        contract_id,
-        action: isAcknowledge ? '╨Ю╨╖╨╜╨░╨║╨╛╨╝╨╗╨╡╨╜' : isRejected ? '╨Ю╤В╨║╨╗╨╛╨╜╨╡╨╜╨╛' : '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╛',
-        details: isAcknowledge
-          ? '╨г╤З╨░╤Б╤В╨╜╨╕╨║ ╨╛╨╖╨╜╨░╨║╨╛╨╝╨╕╨╗╤Б╤П(╨░╤Б╤М) ╤Б ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨╝'
-          : comment ? `╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ${comment}` : '╨С╨╡╨╖ ╨║╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╤П',
-        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
-      })
-
-    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╤Б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В
-    await supabase
-      .from('approval_messages')
-      .insert({
-        session_id: sessionId,
-        message: isAcknowledge
-          ? `ЁЯСБ ${user_name} ╨╛╨╖╨╜╨░╨║╨╛╨╝╨╗╨╡╨╜ ╤Б ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨╝`
-          : isRejected
-            ? `тЭМ ${user_name} ╨╛╤В╨║╨╗╨╛╨╜╨╕╨╗(╨░) ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В${comment ? `. ╨Я╤А╨╕╤З╨╕╨╜╨░: ┬л${comment}┬╗` : ''}`
-            : `тЬЕ ${user_name} ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗(╨░) ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В${comment ? `. ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ┬л${comment}┬╗` : ''}`,
-        author_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
-        is_ai: false,
-      })
-
-    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨▓╤Б╨╡ ╨╗╨╕ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕
-    const { data: participants } = await supabase
+    // ╨Ю╤В╨╝╨╡╨╜╤П╨╡╨╝ ╨│╨╛╨╗╨╛╤Б тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╨▓ pending
+    const { error: updateErr } = await supabase
       .from('approval_participants')
-      .select('status, role')
-      .eq('session_id', sessionId)
-
-    const required = participants?.filter(p => p.role === 'required') ?? []
-    const allDone = required.every(p =>
-      p.status === 'approved' || p.status === 'disabled' || p.status === 'completed_by_initiator'
-    )
-
-    if (allDone && required.length > 0) {
-      // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ contract_id ╨╕╨╖ ╤Б╨╡╤Б╤Б╨╕╨╕ ╨╜╨░╨┐╤А╤П╨╝╤Г╤О ╨┤╨╗╤П ╨╜╨░╨┤╤С╨╢╨╜╨╛╤Б╤В╨╕
-      const { data: sessionData } = await supabase
-        .from('approval_sessions')
-        .select('contract_id')
-        .eq('id', sessionId)
-        .single()
-
-      const actualContractId = sessionData?.contract_id ?? contract_id
-
-      const { error: contractUpdateError } = await supabase
-        .from('contracts')
-        .update({ status: '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜' })
-        .eq('id', actualContractId)
+      .update({ status: 'pending', comment: null, decided_at: null })
+      .eq('id', participant_id)
 
-      if (contractUpdateError) {
-        console.error('Contract status update error:', contractUpdateError)
-      }
+    if (updateErr) {
+      return NextResponse.json({ error: updateErr.message }, { status: 400, headers: CORS })
+    }
 
-      await supabase
-        .from('approval_sessions')
-        .update({ status: 'completed' })
-        .eq('id', sessionId)
+    // ╨Х╤Б╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╨▒╤Л╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜ тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╨▓ ╨╜╨░_╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕
+    const { data: contractData } = await supabase
+      .from('contracts')
+      .select('status, title, number')
+      .eq('id', contract_id)
+      .single()
 
+    if (contractData?.status === '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜') {
       await supabase
-        .from('contract_logs')
-        .insert({
-          contract_id: actualContractId,
-          action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨╖╨░╨▓╨╡╤А╤И╨╡╨╜╨╛',
-          details: '╨Т╤Б╨╡ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В. ╨Ф╨╛╨│╨╛╨▓╨╛╤А ╨│╨╛╤В╨╛╨▓ ╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╕╤О.',
-          user_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
-        })
-
-      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ + ╨▓╤Б╨╡╤Е ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╨╛ ╨┐╨╛╨╗╨╜╨╛╨╝ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕
-      const { data: contractData } = await supabase
         .from('contracts')
-        .select('title, number, author_bitrix_id')
-        .eq('id', actualContractId)
-        .single()
+        .update({ status: '╨╜╨░_╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕' })
+        .eq('id', contract_id)
+    }
 
-      if (contractData) {
-        const { data: participantsData } = await supabase
-          .from('approval_participants')
-          .select('bitrix_user_id')
-          .eq('session_id', sessionId)
-          .not('bitrix_user_id', 'is', null)
+    // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
+    const revokeText = isAdmin
+      ? `ЁЯФД ╨Р╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░ ${participant.user_name}.\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`
+      : `ЁЯФД ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗(╨░) ╤Б╨▓╨╛╤С ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡.\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`
 
-        const participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
-        const gcManagerIds = [1, 246, 504]
+    await supabase.from('approval_messages').insert({
+      session_id: sessionId,
+      author_name: user_name,
+      bitrix_user_id: parseInt(user_bitrix_id),
+      message: revokeText,
+      is_ai: false,
+    })
 
-        const recipients = [...new Set([
-          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
-          ...gcManagerIds,
-          ...participantIds,
-        ])]
+    // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ last_message_at
+    await supabase
+      .from('contracts')
+      .update({ last_message_at: new Date().toISOString() })
+      .eq('id', contract_id)
 
-        await sendBitrixNotify({
-          recipients,
-          type: 'document_approved',
-          document_id: actualContractId,
-          document_title: contractData.title ?? '',
-          document_number: contractData.number ?? '',
-        })
-        
+    // ╨Ы╨╛╨│
+    await supabase.from('contract_logs').insert({
+      contract_id,
+      action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨╛╤В╨╝╨╡╨╜╨╡╨╜╨╛',
+      details: `${isAdmin ? `╨Р╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░ ${participant.user_name}` : `${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗(╨░) ╤Б╨▓╨╛╤С ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡`}. ╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`,
+      user_name,
+    })
 
-        // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨│╤А╤Г╨┐╨┐╨╛╨▓╨╛╨╣ ╤З╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б24
-        const { data: approvedSessionInfo } = await supabase
-          .from('approval_sessions')
-          .select('bitrix_chat_id')
-          .eq('id', sessionId)
-          .single()
-        if (approvedSessionInfo?.bitrix_chat_id) {
-          const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
-          const link = `https://${bitrixPortal}/contracts/${actualContractId}`
-          await sendBitrixChatMessage(
-            approvedSessionInfo.bitrix_chat_id,
-            `тЬЕ ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜: ${contractData.number} тАФ ${contractData.title} [${link}]`
-          )
-        }
-      }
-    }
+    return NextResponse.json({ success: true, returned_to_pending: true }, { headers: CORS })
 
-    return NextResponse.json({ success: true, all_approved: allDone })
   } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500 })
-  }
-}
-
-export async function DELETE(
-  request: NextRequest,
-  { params }: { params: Promise<{ sessionId: string }> }
-) {
-  try {
-    const { sessionId } = await params
-    const body = await request.json()
-    const { reason, user_name, contract_id } = body
-
-    if (!reason?.trim()) {
-      return NextResponse.json({ error: '╨г╨║╨░╨╢╨╕╤В╨╡ ╨┐╤А╨╕╤З╨╕╨╜╤Г ╨┐╤А╨╡╤А╤Л╨▓╨░╨╜╨╕╤П' }, { status: 400 })
-    }
-
-    const { error: sessionError } = await supabase
-      .from('approval_sessions')
-      .update({ status: 'cancelled', cancel_reason: reason })
-      .eq('id', sessionId)
-
-    if (sessionError) {
-      return NextResponse.json({ error: sessionError.message }, { status: 400 })
-    }
-
-    await supabase
-      .from('contracts')
-      .update({ status: '╨░╤А╤Е╨╕╨▓' })
-      .eq('id', contract_id)
-
-    await supabase
-      .from('approval_messages')
-      .insert({
-        session_id: sessionId,
-        message: `ЁЯЪл ╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╡╤А╨▓╨░╨╜╨╛ ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А╨╛╨╝. ╨Я╤А╨╕╤З╨╕╨╜╨░: ┬л${reason}┬╗`,
-        author_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
-        is_ai: false,
-      })
-
-    await supabase
-      .from('contract_logs')
-      .insert({
-        contract_id,
-        action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╡╤А╨▓╨░╨╜╨╛',
-        details: `╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`,
-        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
-      })
-
-    return NextResponse.json({ success: true })
-  } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500 })
-  }
-}
-
-================
-File: app/api/approvals/[sessionId]/revoke/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
-
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
-
-const ADMIN_IDS = [30, 1148]
-const CORS = { 'Access-Control-Allow-Origin': '*' }
-
-export async function OPTIONS() {
-  return new NextResponse(null, {
-    headers: { ...CORS, 'Access-Control-Allow-Methods': 'POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' }
-  })
-}
-
-export async function POST(
-  request: NextRequest,
-  { params }: { params: Promise<{ sessionId: string }> }
-) {
-  try {
-    const { sessionId } = await params
-    const { participant_id, reason, user_bitrix_id, user_name, contract_id } = await request.json()
-
-    if (!participant_id || !reason?.trim()) {
-      return NextResponse.json({ error: 'participant_id ╨╕ ╨┐╤А╨╕╤З╨╕╨╜╨░ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400, headers: CORS })
-    }
-
-    const isAdmin = ADMIN_IDS.includes(parseInt(user_bitrix_id))
-
-    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░
-    const { data: participant, error: pErr } = await supabase
-      .from('approval_participants')
-      .select('id, status, role, user_name, bitrix_user_id, stage')
-      .eq('id', participant_id)
-      .single()
-
-    if (pErr || !participant) {
-      return NextResponse.json({ error: '╨г╤З╨░╤Б╤В╨╜╨╕╨║ ╨╜╨╡ ╨╜╨░╨╣╨┤╨╡╨╜' }, { status: 404, headers: CORS })
-    }
-
-    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╤А╨░╨▓╨░
-    const isOwnVote = parseInt(user_bitrix_id) === participant.bitrix_user_id
-    if (!isAdmin && !isOwnVote) {
-      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╨╛╤В╨╝╨╡╨╜╤Л ╤Н╤В╨╛╨│╨╛ ╨│╨╛╨╗╨╛╤Б╨░' }, { status: 403, headers: CORS })
-    }
-
-    // ╨У╨Ф ╨╝╨╛╨╢╨╡╤В ╨╛╤В╨╝╨╡╨╜╨╕╤В╤М ╤В╨╛╨╗╤М╨║╨╛ ╨╡╤Б╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╡╤Й╤С ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜
-    if (!isAdmin && isOwnVote) {
-      const { data: contractData } = await supabase
-        .from('contracts')
-        .select('status')
-        .eq('id', contract_id)
-        .single()
-
-      if (contractData?.status === '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜') {
-        return NextResponse.json({ error: '╨Э╨╡╨╗╤М╨╖╤П ╨╛╤В╨╝╨╡╨╜╨╕╤В╤М ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ тАФ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Г╨╢╨╡ ╨┐╨╡╤А╨╡╤И╤С╨╗ ╨▓ ╤Б╤В╨░╤В╤Г╤Б ┬л╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜┬╗. ╨Ю╨▒╤А╨░╤В╨╕╤В╨╡╤Б╤М ╨║ ╨░╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А╤Г.' }, { status: 403, headers: CORS })
-      }
-    }
-
-    // ╨Ю╤В╨╝╨╡╨╜╤П╨╡╨╝ ╨│╨╛╨╗╨╛╤Б тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╨▓ pending
-    const { error: updateErr } = await supabase
-      .from('approval_participants')
-      .update({ status: 'pending', comment: null, decided_at: null })
-      .eq('id', participant_id)
-
-    if (updateErr) {
-      return NextResponse.json({ error: updateErr.message }, { status: 400, headers: CORS })
-    }
-
-    // ╨Х╤Б╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╨▒╤Л╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜ тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╨▓ ╨╜╨░_╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕
-    const { data: contractData } = await supabase
-      .from('contracts')
-      .select('status, title, number')
-      .eq('id', contract_id)
-      .single()
-
-    if (contractData?.status === '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜') {
-      await supabase
-        .from('contracts')
-        .update({ status: '╨╜╨░_╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕' })
-        .eq('id', contract_id)
-    }
-
-    // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
-    const revokeText = isAdmin
-      ? `ЁЯФД ╨Р╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░ ${participant.user_name}.\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`
-      : `ЁЯФД ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗(╨░) ╤Б╨▓╨╛╤С ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡.\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`
-
-    await supabase.from('approval_messages').insert({
-      session_id: sessionId,
-      author_name: user_name,
-      bitrix_user_id: parseInt(user_bitrix_id),
-      message: revokeText,
-      is_ai: false,
-    })
-
-    // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ last_message_at
-    await supabase
-      .from('contracts')
-      .update({ last_message_at: new Date().toISOString() })
-      .eq('id', contract_id)
-
-    // ╨Ы╨╛╨│
-    await supabase.from('contract_logs').insert({
-      contract_id,
-      action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨╛╤В╨╝╨╡╨╜╨╡╨╜╨╛',
-      details: `${isAdmin ? `╨Р╨┤╨╝╨╕╨╜╨╕╤Б╤В╤А╨░╤В╨╛╤А ${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░ ${participant.user_name}` : `${user_name} ╨╛╤В╨╝╨╡╨╜╨╕╨╗(╨░) ╤Б╨▓╨╛╤С ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡`}. ╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`,
-      user_name,
-    })
-
-    return NextResponse.json({ success: true, returned_to_pending: true }, { headers: CORS })
-
-  } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500, headers: CORS })
+    const message = err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500, headers: CORS })
   }
 }
 
@@ -6386,218 +6111,277 @@ export async function GET(request: NextRequest) {
 }
 
 ================
-File: app/api/bitrix-tasks/route.ts
+File: app/api/approvals/[sessionId]/approve/route.ts
 ================
-import { NextRequest, NextResponse } from 'next/server'
 import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
+import { sendBitrixNotify, sendBitrixMessage, sendBitrixChatMessage } from '@/app/lib/notify'
 
 const supabase = createClient(
   process.env.NEXT_PUBLIC_SUPABASE_URL!,
   process.env.SUPABASE_SECRET_KEY!
 )
 
-const WEBHOOK = process.env.BITRIX_WEBHOOK_URL!
-
-// ╨Я╨╛╤Б╤В╨░╨╜╨╛╨▓╤Й╨╕╨║ = ╨У╨Ф ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕ ╨┐╨╛ ╨┐╤А╨╡╤Д╨╕╨║╤Б╤Г
-const COMPANY_DIRECTORS: Record<string, number> = {
-  '╨в╨е':  1,    // ╨з╨░╤Й╨╕╨╜╨░ ╨Х╨╗╨╡╨╜╨░ тАФ ╨У╨╡╨╜. ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨Ю╨Ю╨Ю ╨в╨╡╤Е╨╜╨╛
-  '╨Э╨Я╨Я': 592,  // ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨Э╨Я╨Я
-  '╨б╨Я╨в': 6,    // ╨Т╨░╨╗╤О╨║ ╨Р╨╜╨┤╤А╨╡╨╣ тАФ ╨У╨Ф ╨б╨Я╨в/╨Ю╨б
-  '╨Ю╨б':  6,
-  '╨н-╨Ъ': 954,  // ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨н-╨Ъ
-}
-
-export async function POST(request: NextRequest) {
+export async function POST(
+  request: NextRequest,
+  { params }: { params: Promise<{ sessionId: string }> }
+) {
   try {
+    const { sessionId } = await params
     const body = await request.json()
-    const {
-      checklist_item_id,   // id ╨┐╤Г╨╜╨║╤В╨░ ╤З╨╡╨║-╨╗╨╕╤Б╤В╨░ (╨╕╨╗╨╕ null ╨┐╤А╨╕ ╤Б╨╛╨╖╨┤╨░╨╜╨╕╨╕ ╨▓╤Б╨╡╤Е)
-      contract_id,
-      contract_number,
-      contract_title,
-      company_prefix,
-      item_title,
-      item_description,
-      due_date,
-      responsible_bitrix_id,  // ╨╕╤Б╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М (╨┐╨╛ ╤Г╨╝╨╛╨╗╤З╨░╨╜╨╕╤О = ╨░╨▓╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░)
-      author_bitrix_id,        // ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ тЖТ ╨╜╨░╨▒╨╗╤О╨┤╨░╤В╨╡╨╗╤М ╨▓ ╨╖╨░╨┤╨░╤З╨╡
-      // ╨Ф╨╗╤П ╤Б╨╛╨╖╨┤╨░╨╜╨╕╤П ╨▓╤Б╨╡╤Е ╨╖╨░╨┤╨░╤З ╤Б╤А╨░╨╖╤Г
-      items,  // ╨╝╨░╤Б╤Б╨╕╨▓ {id, title, description, due_date} ╨╡╤Б╨╗╨╕ bulk
-      mode,   // 'single_with_checklist' ╨┤╨╗╤П ╨╛╨┤╨╜╨╛╨╣ ╨╖╨░╨┤╨░╤З╨╕ ╤Б ╤З╨╡╨║-╨╗╨╕╤Б╤В╨╛╨╝
-      task_title, // ╨╜╨░╨╖╨▓╨░╨╜╨╕╨╡ ╨╖╨░╨┤╨░╤З╨╕ ╨┤╨╗╤П single_with_checklist
-    } = body
+    const { participant_id, comment, user_name, contract_id } = body
+    const isAcknowledge = body.is_acknowledge === true
+    const isRejected = body.is_rejected === true
 
-    if (!contract_id || !company_prefix) {
-      return NextResponse.json({ error: '╨Э╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╨┐╨╛╨╗╤П' }, { status: 400 })
+    // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░
+    const { error: updateError } = await supabase
+      .from('approval_participants')
+      .update({
+        status: isAcknowledge ? 'acknowledged' : isRejected ? 'rejected' : 'approved',
+        comment: comment || null,
+        decided_at: new Date().toISOString(),
+      })
+      .eq('id', participant_id)
+
+    if (updateError) {
+      return NextResponse.json({ error: updateError.message }, { status: 400 })
     }
 
-    const directorId = COMPANY_DIRECTORS[company_prefix] ?? 1
+    // ╨Я╤А╨╕ ╨╛╤В╨║╨╗╨╛╨╜╨╡╨╜╨╕╨╕ тАФ ╨╝╨╡╨╜╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╨║╨╛╨╜╤В╤А╨░╨║╤В╨░ ╨╕ ╤Б╨╡╤Б╤Б╨╕╨╕
+    if (isRejected) {
+      const { data: sessionData } = await supabase
+        .from('approval_sessions')
+        .select('contract_id')
+        .eq('id', sessionId)
+        .single()
 
-    // ╨д╤Г╨╜╨║╤Ж╨╕╤П ╤Б╨╛╨╖╨┤╨░╨╜╨╕╤П ╨╛╨┤╨╜╨╛╨╣ ╨╖╨░╨┤╨░╤З╨╕ ╨▓ ╨С╨╕╤В╤А╨╕╨║╤Б24
-    const createBitrixTask = async (
-      title: string,
-      description: string | null,
-      deadline: string | null,
-      responsibleId: number,
-      itemId: string
-    ): Promise<{ bitrix_task_id: string | null; error?: string }> => {
-      
-      // ╨д╨╛╤А╨╝╨╕╤А╤Г╨╡╨╝ ╨╛╨┐╨╕╤Б╨░╨╜╨╕╨╡ ╨╖╨░╨┤╨░╤З╨╕
-      const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
-      const contractLink = `https://${bitrixPortal}/contracts/${contract_id}`
-      const contractRef = `ЁЯУД ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: [URL=${contractLink}]${contract_number} тАФ ${contract_title}[/URL]`
-      const noDeadlineNote = !deadline 
-        ? '\n\nтЪая╕П ╨б╤А╨╛╨║ ╨╜╨╡ ╤Г╤Б╤В╨░╨╜╨╛╨▓╨╗╨╡╨╜. ╨Э╨╡╨╛╨▒╤Е╨╛╨┤╨╕╨╝╨╛ ╨╖╨░╨┤╨░╤В╤М ╤Б╤А╨╛╨║ ╨╕╤Б╨┐╨╛╨╗╨╜╨╡╨╜╨╕╤П!' 
-        : ''
-      const fullDescription = `${description || title}\n\n${contractRef}${noDeadlineNote}`
+      const actualContractId = sessionData?.contract_id ?? contract_id
 
-      const fields: Record<string, unknown> = {
-        TITLE: title,
-        DESCRIPTION: fullDescription,
-        DESCRIPTION_IN_BBCODE: 'Y',
-        RESPONSIBLE_ID: String(responsibleId),
-        CREATED_BY: String(directorId),  // ╨┐╨╛╤Б╤В╨░╨╜╨╛╨▓╤Й╨╕╨║ = ╨У╨Ф ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕
-        PRIORITY: '1',
-        ...(author_bitrix_id && author_bitrix_id !== responsibleId
-          ? { SE_PARAMETER: [{ CODE: 'WATCHERS', VALUE: String(author_bitrix_id) }] }
-          : {}),
-      }
+      await supabase.from('contracts').update({ status: '╨╛╤В╨║╨╗╨╛╨╜╤С╨╜' }).eq('id', actualContractId)
+      await supabase.from('approval_sessions').update({ status: 'cancelled' }).eq('id', sessionId)
 
-      if (deadline) {
-        // ╨Ъ╨╛╨╜╨▓╨╡╤А╤В╨╕╤А╤Г╨╡╨╝ ╨┤╨░╤В╤Г ╨▓ ╤Д╨╛╤А╨╝╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б (ISO 8601)
-        fields.DEADLINE = new Date(deadline).toISOString()
-      }
+      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░
+      const { data: contractData } = await supabase
+        .from('contracts')
+        .select('title, number, author_bitrix_id')
+        .eq('id', actualContractId)
+        .single()
 
-      const url = `${WEBHOOK}tasks.task.add.json`
-      const resp = await fetch(url, {
-        method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({ fields }),
-      })
+      if (contractData) {
+        const { data: participantsData } = await supabase
+          .from('approval_participants')
+          .select('bitrix_user_id')
+          .eq('session_id', sessionId)
+          .not('bitrix_user_id', 'is', null)
 
-      const data = await resp.json()
+        const participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
+        const rejectedRecipients = [...new Set([
+          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
+          ...participantIds,
+        ])]
 
-      if (data.error) {
-        return { bitrix_task_id: null, error: data.error_description ?? data.error }
-      }
+        await sendBitrixNotify({
+          recipients: rejectedRecipients,
+          type: 'document_rejected',
+          document_id: actualContractId,
+          document_title: contractData.title ?? '',
+          document_number: contractData.number ?? '',
+          extra: comment ?? undefined,
+        })
+        
 
-      const taskId = String(data.result?.task?.id)
+        // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨│╤А╤Г╨┐╨┐╨╛╨▓╨╛╨╣ ╤З╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б24
+        const { data: sessionInfo } = await supabase
+          .from('approval_sessions')
+          .select('bitrix_chat_id')
+          .eq('id', sessionId)
+          .single()
+        if (sessionInfo?.bitrix_chat_id) {
+          const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
+          const link = `https://${bitrixPortal}/contracts/${actualContractId}`
+          await sendBitrixChatMessage(
+            sessionInfo.bitrix_chat_id,
+            `тЭМ ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╛╤В╨║╨╗╨╛╨╜╤С╨╜: ${contractData.number} тАФ ${contractData.title} [${link}]${comment ? `\n╨Я╤А╨╕╤З╨╕╨╜╨░: ${comment}` : ''}`
+          )
+        }
+      }
+    }
 
-      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ bitrix_task_id ╨▓ Supabase
-      await supabase
-        .from('contract_checklist')
-        .update({ bitrix_task_id: taskId })
-        .eq('id', itemId)
+    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
+    await supabase
+      .from('contract_logs')
+      .insert({
+        contract_id,
+        action: isAcknowledge ? '╨Ю╨╖╨╜╨░╨║╨╛╨╝╨╗╨╡╨╜' : isRejected ? '╨Ю╤В╨║╨╗╨╛╨╜╨╡╨╜╨╛' : '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╛',
+        details: isAcknowledge
+          ? '╨г╤З╨░╤Б╤В╨╜╨╕╨║ ╨╛╨╖╨╜╨░╨║╨╛╨╝╨╕╨╗╤Б╤П(╨░╤Б╤М) ╤Б ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨╝'
+          : comment ? `╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ${comment}` : '╨С╨╡╨╖ ╨║╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╤П',
+        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
+      })
 
-      return { bitrix_task_id: taskId }
-    }
+    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╤Б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В
+    await supabase
+      .from('approval_messages')
+      .insert({
+        session_id: sessionId,
+        message: isAcknowledge
+          ? `ЁЯСБ ${user_name} ╨╛╨╖╨╜╨░╨║╨╛╨╝╨╗╨╡╨╜ ╤Б ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨╝`
+          : isRejected
+            ? `тЭМ ${user_name} ╨╛╤В╨║╨╗╨╛╨╜╨╕╨╗(╨░) ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В${comment ? `. ╨Я╤А╨╕╤З╨╕╨╜╨░: ┬л${comment}┬╗` : ''}`
+            : `тЬЕ ${user_name} ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗(╨░) ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В${comment ? `. ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ┬л${comment}┬╗` : ''}`,
+        author_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
+        is_ai: false,
+      })
 
-    // === SINGLE WITH CHECKLIST: ╨╛╨┤╨╜╨░ ╨╖╨░╨┤╨░╤З╨░ ╤Б ╤З╨╡╨║-╨╗╨╕╤Б╤В╨╛╨╝ ===
-    if (mode === 'single_with_checklist' && items && Array.isArray(items)) {
-      const taskTitle = task_title || `╨Ъ╨╛╨╜╤В╤А╨╛╨╗╤М ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ тДЦ ${contract_number}`
+    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨▓╤Б╨╡ ╨╗╨╕ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕
+    const { data: participants } = await supabase
+      .from('approval_participants')
+      .select('status, role')
+      .eq('session_id', sessionId)
 
-      // ╨Ф╨╡╨┤╨╗╨░╨╣╨╜ = ╨╝╨░╨║╤Б╨╕╨╝╨░╨╗╤М╨╜╨░╤П ╨┤╨░╤В╨░ ╨╕╨╖ ╨┐╤Г╨╜╨║╤В╨╛╨▓
-      const dates = items
-        .map((i: { due_date?: string }) => i.due_date)
-        .filter(Boolean)
-        .map((d: string | undefined) => new Date(d!).getTime())
-      const maxDeadline = dates.length > 0 ? new Date(Math.max(...dates)).toISOString() : null
+    const required = participants?.filter(p => p.role === 'required') ?? []
+    const allDone = required.every(p =>
+      p.status === 'approved' || p.status === 'disabled' || p.status === 'completed_by_initiator'
+    )
 
-      const contractRef = `ЁЯУД ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${contract_number} тАФ ${contract_title}`
-      const noDeadlineNote = !maxDeadline
-        ? '\n\nтЪая╕П ╨б╤А╨╛╨║ ╨╜╨╡ ╤Г╤Б╤В╨░╨╜╨╛╨▓╨╗╨╡╨╜. ╨Э╨╡╨╛╨▒╤Е╨╛╨┤╨╕╨╝╨╛ ╨╖╨░╨┤╨░╤В╤М ╤Б╤А╨╛╨║ ╨╕╤Б╨┐╨╛╨╗╨╜╨╡╨╜╨╕╤П!'
-        : ''
+    if (allDone && required.length > 0) {
+      // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ contract_id ╨╕╨╖ ╤Б╨╡╤Б╤Б╨╕╨╕ ╨╜╨░╨┐╤А╤П╨╝╤Г╤О ╨┤╨╗╤П ╨╜╨░╨┤╤С╨╢╨╜╨╛╤Б╤В╨╕
+      const { data: sessionData } = await supabase
+        .from('approval_sessions')
+        .select('contract_id')
+        .eq('id', sessionId)
+        .single()
 
-      const fields: Record<string, unknown> = {
-        TITLE: taskTitle,
-        DESCRIPTION: `${contractRef}${noDeadlineNote}`,
-        DESCRIPTION_IN_BBCODE: 'Y',
-        RESPONSIBLE_ID: String(responsible_bitrix_id),
-        CREATED_BY: String(directorId),
-        PRIORITY: '1',
-      }
-      if (maxDeadline) fields.DEADLINE = maxDeadline
+      const actualContractId = sessionData?.contract_id ?? contract_id
 
-      // ╨б╨╛╨╖╨┤╨░╤С╨╝ ╨╖╨░╨┤╨░╤З╤Г
-      const taskResp = await fetch(`${WEBHOOK}tasks.task.add.json`, {
-        method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({ fields }),
-      })
-      const taskData = await taskResp.json()
+      const { error: contractUpdateError } = await supabase
+        .from('contracts')
+        .update({ status: '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜' })
+        .eq('id', actualContractId)
 
-      if (taskData.error) {
-        return NextResponse.json(
-          { error: taskData.error_description ?? taskData.error },
-          { status: 400 }
-        )
+      if (contractUpdateError) {
+        console.error('Contract status update error:', contractUpdateError)
       }
 
-      const taskId = String(taskData.result?.task?.id)
+      await supabase
+        .from('approval_sessions')
+        .update({ status: 'completed' })
+        .eq('id', sessionId)
 
-      // ╨Ф╨╛╨▒╨░╨▓╨╗╤П╨╡╨╝ ╨┐╤Г╨╜╨║╤В╤Л ╨║╨░╨║ ╤З╨╡╨║-╨╗╨╕╤Б╤В ╨╖╨░╨┤╨░╤З╨╕
-      for (const item of items) {
-        const checkResp = await fetch(`${WEBHOOK}task.checklistitem.add.json`, {
-          method: 'POST',
-          headers: { 'Content-Type': 'application/json' },
-          body: JSON.stringify({
-            TASKID: taskId,
-            fields: { TITLE: item.title },
-          }),
+      await supabase
+        .from('contract_logs')
+        .insert({
+          contract_id: actualContractId,
+          action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨╖╨░╨▓╨╡╤А╤И╨╡╨╜╨╛',
+          details: '╨Т╤Б╨╡ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В. ╨Ф╨╛╨│╨╛╨▓╨╛╤А ╨│╨╛╤В╨╛╨▓ ╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╕╤О.',
+          user_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
         })
-        const checkData = await checkResp.json()
-        console.log(`checklistitem.add taskId=${taskId} title="${item.title}":`, JSON.stringify(checkData))
-        await new Promise(r => setTimeout(r, 200))
-      }
 
-      return NextResponse.json({ success: true, bitrix_task_id: taskId })
-    }
+      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ + ╨▓╤Б╨╡╤Е ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╨╛ ╨┐╨╛╨╗╨╜╨╛╨╝ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╕
+      const { data: contractData } = await supabase
+        .from('contracts')
+        .select('title, number, author_bitrix_id')
+        .eq('id', actualContractId)
+        .single()
 
-    // === BULK: ╤Б╨╛╨╖╨┤╨░╤В╤М ╨▓╤Б╨╡ ╨╖╨░╨┤╨░╤З╨╕ ===
-    if (items && Array.isArray(items)) {
-      const results = []
-      for (const item of items) {
-        // ╨Я╤А╨╛╨┐╤Г╤Б╨║╨░╨╡╨╝ ╤Г╨╢╨╡ ╤Б╨╛╨╖╨┤╨░╨╜╨╜╤Л╨╡
-        if (item.bitrix_task_id) {
-          results.push({ id: item.id, skipped: true, bitrix_task_id: item.bitrix_task_id })
-          continue
+      if (contractData) {
+        const { data: participantsData } = await supabase
+          .from('approval_participants')
+          .select('bitrix_user_id')
+          .eq('session_id', sessionId)
+          .not('bitrix_user_id', 'is', null)
+
+        const participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
+        const gcManagerIds = [1, 246, 504]
+
+        const recipients = [...new Set([
+          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
+          ...gcManagerIds,
+          ...participantIds,
+        ])]
+
+        await sendBitrixNotify({
+          recipients,
+          type: 'document_approved',
+          document_id: actualContractId,
+          document_title: contractData.title ?? '',
+          document_number: contractData.number ?? '',
+        })
+        
+
+        // ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨│╤А╤Г╨┐╨┐╨╛╨▓╨╛╨╣ ╤З╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б24
+        const { data: approvedSessionInfo } = await supabase
+          .from('approval_sessions')
+          .select('bitrix_chat_id')
+          .eq('id', sessionId)
+          .single()
+        if (approvedSessionInfo?.bitrix_chat_id) {
+          const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
+          const link = `https://${bitrixPortal}/contracts/${actualContractId}`
+          await sendBitrixChatMessage(
+            approvedSessionInfo.bitrix_chat_id,
+            `тЬЕ ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜: ${contractData.number} тАФ ${contractData.title} [${link}]`
+          )
         }
-        const result = await createBitrixTask(
-          item.title,
-          item.description,
-          item.due_date,
-          responsible_bitrix_id,
-          item.id
-        )
-        results.push({ id: item.id, ...result })
-        // ╨Я╨░╤Г╨╖╨░ ╨╝╨╡╨╢╨┤╤Г ╨╖╨░╨┐╤А╨╛╤Б╨░╨╝╨╕ ╤З╤В╨╛╨▒╤Л ╨╜╨╡ ╨┐╤А╨╡╨▓╤Л╤Б╨╕╤В╤М ╨╗╨╕╨╝╨╕╤В ╨С╨╕╤В╤А╨╕╨║╤Б
-        await new Promise(r => setTimeout(r, 300))
       }
-      return NextResponse.json({ success: true, results })
     }
 
-    // === SINGLE: ╤Б╨╛╨╖╨┤╨░╤В╤М ╨╛╨┤╨╜╤Г ╨╖╨░╨┤╨░╤З╤Г ===
-    if (!checklist_item_id || !item_title) {
-      return NextResponse.json({ error: '╨Э╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л item ╨┤╨░╨╜╨╜╤Л╨╡' }, { status: 400 })
+    return NextResponse.json({ success: true, all_approved: allDone })
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500 })
+  }
+}
+
+export async function DELETE(
+  request: NextRequest,
+  { params }: { params: Promise<{ sessionId: string }> }
+) {
+  try {
+    const { sessionId } = await params
+    const body = await request.json()
+    const { reason, user_name, contract_id } = body
+
+    if (!reason?.trim()) {
+      return NextResponse.json({ error: '╨г╨║╨░╨╢╨╕╤В╨╡ ╨┐╤А╨╕╤З╨╕╨╜╤Г ╨┐╤А╨╡╤А╤Л╨▓╨░╨╜╨╕╤П' }, { status: 400 })
     }
 
-    const result = await createBitrixTask(
-      item_title,
-      item_description,
-      due_date,
-      responsible_bitrix_id,
-      checklist_item_id
-    )
+    const { error: sessionError } = await supabase
+      .from('approval_sessions')
+      .update({ status: 'cancelled', cancel_reason: reason })
+      .eq('id', sessionId)
 
-    if (result.error) {
-      return NextResponse.json({ error: result.error }, { status: 400 })
+    if (sessionError) {
+      return NextResponse.json({ error: sessionError.message }, { status: 400 })
     }
 
-    return NextResponse.json({ success: true, bitrix_task_id: result.bitrix_task_id })
+    await supabase
+      .from('contracts')
+      .update({ status: '╨░╤А╤Е╨╕╨▓' })
+      .eq('id', contract_id)
+
+    await supabase
+      .from('approval_messages')
+      .insert({
+        session_id: sessionId,
+        message: `ЁЯЪл ╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╡╤А╨▓╨░╨╜╨╛ ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А╨╛╨╝. ╨Я╤А╨╕╤З╨╕╨╜╨░: ┬л${reason}┬╗`,
+        author_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
+        is_ai: false,
+      })
+
+    await supabase
+      .from('contract_logs')
+      .insert({
+        contract_id,
+        action: '╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╡╤А╨▓╨░╨╜╨╛',
+        details: `╨Я╤А╨╕╤З╨╕╨╜╨░: ${reason}`,
+        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
+      })
 
+    return NextResponse.json({ success: true })
   } catch (err) {
-    console.error('bitrix-tasks error:', err)
-    return NextResponse.json({ error: '╨Т╨╜╤Г╤В╤А╨╡╨╜╨╜╤П╤П ╨╛╤И╨╕╨▒╨║╨░ ╤Б╨╡╤А╨▓╨╡╤А╨░' }, { status: 500 })
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500 })
   }
 }
 
@@ -6919,165 +6703,440 @@ export async function DELETE(
       }
     }
 
-    const { error } = await supabase
-      .from('counterparty_documents')
-      .delete()
-      .eq('id', docId)
+    const { error } = await supabase
+      .from('counterparty_documents')
+      .delete()
+      .eq('id', docId)
+
+    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
+    return NextResponse.json({ success: true })
+
+  } catch (err) {
+    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
+  }
+}
+
+================
+File: app/api/signed-documents/route.ts
+================
+import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
+import { sendBitrixNotify, sendBitrixMessage } from '@/app/lib/notify'
+
+export const maxDuration = 60
+
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
+
+const ADMIN_IDS = [30, 1148]
+const GC_MANAGER_IDS = [1, 246, 504]
+// ╨Т╨╗╨░╨┤╨╕╨╝╨╕╤А╨╛╨▓ ╨Т╤П╤З╨╡╤Б╨╗╨░╨▓ (╨н-╨Ъ), ╨Т╨╕╨╜╨╛╨│╤А╨░╨┤╨╛╨▓╨░ ╨Р╨╜╨╜╨░ (╨б╨Я╨в, ╨Ю╨б, ╨Э╨Я╨Я)
+const SPECIAL_SIGNERS: Record<number, string[]> = {
+  782: ['╨н-╨Ъ'],
+  152: ['╨б╨Я╨в', '╨Ю╨б', '╨Э╨Я╨Я'],
+}
+
+function canUploadSigned(
+  userId: number,
+  authorId: number | null,
+  initiatorId: number | null,
+  companyPrefix: string
+): boolean {
+  if (ADMIN_IDS.includes(userId)) return true
+  if (GC_MANAGER_IDS.includes(userId)) return true
+  if (authorId === userId) return true
+  if (initiatorId === userId) return true
+  for (const [id, prefixes] of Object.entries(SPECIAL_SIGNERS)) {
+    if (parseInt(id) === userId && prefixes.includes(companyPrefix)) return true
+  }
+  return false
+}
+
+// ╨Я╨╛╨╗╤Г╤З╨╕╤В╤М ╤Б╨┐╨╕╤Б╨╛╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╤Е ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨▓
+export async function GET(request: NextRequest) {
+  const contract_id = request.nextUrl.searchParams.get('contract_id')
+  if (!contract_id) {
+    return NextResponse.json({ error: 'contract_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+  }
+
+  const { data, error } = await supabase
+    .from('signed_documents')
+    .select('*')
+    .eq('contract_id', contract_id)
+    .order('created_at')
+
+  if (error) {
+    return NextResponse.json({ error: error.message }, { status: 400 })
+  }
+
+  return NextResponse.json({ documents: data ?? [] })
+}
+
+// ╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В
+export async function POST(request: NextRequest) {
+  try {
+    const formData = await request.formData()
+    const file = formData.get('file') as File
+    const contract_id = formData.get('contract_id') as string
+    const user_name = formData.get('user_name') as string
+    const user_bitrix_id = formData.get('user_bitrix_id') as string
+    const confirm_all = formData.get('confirm_all') === 'true'
+
+    if (!contract_id || !user_bitrix_id) {
+      return NextResponse.json({ error: '╨Э╨╡ ╨▓╤Б╨╡ ╨┐╨░╤А╨░╨╝╨╡╤В╤А╤Л ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л' }, { status: 400 })
+    }
+
+    const userId = parseInt(user_bitrix_id)
+    const status_only = formData.get('status_only') === 'true'
+    const temp_upload = formData.get('temp_upload') === 'true'
+    const confirm_upload = formData.get('confirm_upload') === 'true'
+    const temp_file_url = formData.get('temp_file_url') as string
+    const temp_file_name = formData.get('temp_file_name') as string
+    const has_discrepancies = formData.get('has_discrepancies') === 'true'
+    const discrepancy_comment = formData.get('discrepancy_comment') as string
+    const discrepancy_summary = formData.get('discrepancy_summary') as string
+
+    // ╨Т╤А╨╡╨╝╨╡╨╜╨╜╨░╤П ╨╖╨░╨│╤А╤Г╨╖╨║╨░ тАФ ╨┐╤А╨╛╤Б╤В╨╛ ╤Б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ URL
+    if (temp_upload && file) {
+      const fileExt = file.name.split('.').pop()
+      const tempPath = `temp/${contract_id}/${Date.now()}.${fileExt}`
+      const fileBuffer = await file.arrayBuffer()
+      const { error: uploadError } = await supabase.storage
+        .from('contracts')
+        .upload(tempPath, fileBuffer, { contentType: file.type, upsert: true })
+      if (uploadError) return NextResponse.json({ error: uploadError.message }, { status: 400 })
+      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(tempPath)
+      return NextResponse.json({ success: true, temp_url: urlData.publicUrl })
+    }
+
+    // ╨Я╨╛╨┤╤В╨▓╨╡╤А╨╢╨┤╨╡╨╜╨╕╨╡ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ тАФ ╤Б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╤Г╨╢╨╡ ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╨╜╤Л╨╣ ╤Д╨░╨╣╨╗ ╨║╨░╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣
+    if (confirm_upload && temp_file_url) {
+      // ╨Я╨╡╤А╨╡╨╝╨╡╤Й╨░╨╡╨╝ ╨╕╨╖ temp ╨▓ ╨┐╨╛╤Б╤В╨╛╤П╨╜╨╜╨╛╨╡ ╤Е╤А╨░╨╜╨╕╨╗╨╕╤Й╨╡
+      const fileExt = temp_file_name.split('.').pop()
+      const finalPath = `signed/${contract_id}/${Date.now()}.${fileExt}`
+      const tempPath = temp_file_url.split('/contracts/')[1]
+
+      // ╨Ъ╨╛╨┐╨╕╤А╤Г╨╡╨╝ ╤Д╨░╨╣╨╗
+      const { error: copyError } = await supabase.storage
+        .from('contracts')
+        .copy(tempPath, finalPath)
+
+      if (copyError) {
+        // ╨Х╤Б╨╗╨╕ copy ╨╜╨╡ ╤А╨░╨▒╨╛╤В╨░╨╡╤В тАФ ╤Б╨║╨░╤З╨╕╨▓╨░╨╡╨╝ ╨╕ ╨┐╨╡╤А╨╡╨╖╨░╨╗╨╕╨▓╨░╨╡╨╝
+        const fileRes = await fetch(temp_file_url)
+        const fileBuffer = await fileRes.arrayBuffer()
+        await supabase.storage.from('contracts').upload(finalPath, fileBuffer, { upsert: true })
+      }
+
+      // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨▓╤А╨╡╨╝╨╡╨╜╨╜╤Л╨╣ ╤Д╨░╨╣╨╗
+      await supabase.storage.from('contracts').remove([tempPath])
+
+      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(finalPath)
+      const finalUrl = urlData.publicUrl
+
+      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨▓ ╨С╨Ф
+      await supabase.from('signed_documents').insert({
+        contract_id,
+        file_url: finalUrl,
+        file_name: temp_file_name,
+        uploaded_by_name: user_name,
+        uploaded_by_bitrix_id: userId,
+        signed_date: formData.get('signed_date') || null,
+        has_discrepancies,
+        discrepancy_comment: has_discrepancies ? discrepancy_comment : null,
+      })
+
+      // ╨Ы╨╛╨│
+      await supabase.from('contract_logs').insert({
+        contract_id,
+        action: has_discrepancies ? '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Б ╤А╨░╤Б╤Е╨╛╨╢╨┤╨╡╨╜╨╕╤П╨╝╨╕' : '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В',
+        details: has_discrepancies
+          ? `╨а╨░╤Б╤Е╨╛╨╢╨┤╨╡╨╜╨╕╤П: ${discrepancy_summary}. ╨Я╤А╨╕╤З╨╕╨╜╨░ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕: ${discrepancy_comment}`
+          : `╨д╨░╨╣╨╗: ${temp_file_name}`,
+        user_name: user_name ?? '╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М',
+      })
+
+      // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░
+      const { data: currentDocs } = await supabase
+        .from('signed_documents')
+        .select('id')
+        .eq('contract_id', contract_id)
+
+      const docsCount = (currentDocs?.length ?? 0)
+      const newStatus = docsCount >= 2 ? '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' : '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛'
+      await supabase.from('contracts').update({ status: newStatus }).eq('id', contract_id)
+
+      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨▓╤Б╨╡╤Е ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╛ ╨╖╨░╨│╤А╤Г╨╖╨║╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╨╛╨│╨╛ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░
+      const { data: lastSession2 } = await supabase
+        .from('approval_sessions')
+        .select('id, initiated_by_bitrix_id')
+        .eq('contract_id', contract_id)
+        .order('created_at', { ascending: false })
+        .limit(1)
+        .single()
+
+      if (lastSession2) {
+        const { data: participantsData2 } = await supabase
+          .from('approval_participants')
+          .select('bitrix_user_id')
+          .eq('session_id', lastSession2.id)
+          .not('bitrix_user_id', 'is', null)
+        const participantIds2 = participantsData2?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
+        const { data: contractData2 } = await supabase
+          .from('contracts')
+          .select('title, number, author_bitrix_id')
+          .eq('id', contract_id)
+          .single()
+        if (contractData2) {
+          const recipients2 = [...new Set([
+            ...(contractData2.author_bitrix_id ? [contractData2.author_bitrix_id] : []),
+            ...(lastSession2.initiated_by_bitrix_id ? [lastSession2.initiated_by_bitrix_id] : []),
+            ...participantIds2,
+          ])]
+          await sendBitrixNotify({
+            recipients: recipients2,
+            type: 'documents_uploaded',
+            document_id: contract_id,
+            document_title: contractData2.title ?? '',
+            document_number: contractData2.number ?? '',
+          })
+        }
+      }
+
+      return NextResponse.json({ success: true })
+    }
+
+    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ тАФ ╤Д╨░╨╣╨╗ ╨╜╨╡ ╨╜╤Г╨╢╨╡╨╜
+    if (!status_only && !file) {
+      return NextResponse.json({ error: '╨д╨░╨╣╨╗ ╨╜╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜' }, { status: 400 })
+    }
+
+    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨║╨╛╨╜╤В╤А╨░╨║╤В
+    const { data: contract, error: contractError } = await supabase
+      .from('contracts')
+      .select('id, status, author_bitrix_id, number')
+      .eq('id', contract_id)
+      .single()
+
+    if (contractError || !contract) {
+      return NextResponse.json({ error: `╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╜╨╡ ╨╜╨░╨╣╨┤╨╡╨╜: ${contractError?.message ?? '╨╜╨╡╤В ╨┤╨░╨╜╨╜╤Л╤Е'}` }, { status: 404 })
+    }
+
+    // ╨Ш╨╖╨▓╨╗╨╡╨║╨░╨╡╨╝ ╨┐╤А╨╡╤Д╨╕╨║╤Б ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕ ╨╕╨╖ ╨╜╨╛╨╝╨╡╤А╨░ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ (╨в╨е-╨Ф╨Ю╨У-2026/... тЖТ ╨в╨е)
+    const companyPrefix = contract.number?.split('-')[0] ?? ''
 
-    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
-    return NextResponse.json({ success: true })
+    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А╨░ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
+    const { data: session } = await supabase
+      .from('approval_sessions')
+      .select('initiated_by_bitrix_id')
+      .eq('contract_id', contract_id)
+      .order('created_at', { ascending: false })
+      .limit(1)
+      .single()
 
-  } catch (err) {
-    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
-  }
-}
+    const initiatorId = session?.initiated_by_bitrix_id ?? null
 
-================
-File: app/api/cron/approval-reminder/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextResponse } from 'next/server'
-import { sendBitrixNotify, sendBitrixChatMessage } from '@/app/lib/notify'
+    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╤А╨░╨▓╨░
+    if (!canUploadSigned(userId, contract.author_bitrix_id, initiatorId, companyPrefix)) {
+      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╨╛╨│╨╛ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░' }, { status: 403 })
+    }
 
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
+    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ ╨▒╨╡╨╖ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╤Д╨░╨╣╨╗╨░ тАФ ╨┤╨╡╨╗╨░╨╡╨╝ ╤Н╤В╨╛ ╤Б╤А╨░╨╖╤Г
+    if (status_only && confirm_all) {
+      await supabase
+        .from('contracts')
+        .update({
+          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
+          signed_at: new Date().toISOString(),
+          signed_by_name: user_name,
+          signed_by_bitrix_id: userId,
+        })
+        .eq('id', contract_id)
 
-export async function GET() {
-  try {
-    const now = new Date()
-    const todayStr = now.toISOString().slice(0, 10)
+      await supabase.from('contract_logs').insert({
+        contract_id,
+        action: '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л',
+        details: '╨б╤В╨░╤В╤Г╤Б ╨╕╨╖╨╝╨╡╨╜╤С╨╜ ╨╜╨░ "╨Я╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л"',
+        user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+      })
 
-    // ╨Ч╨░╨▓╤В╤А╨░
-    const tomorrow = new Date(now)
-    tomorrow.setDate(tomorrow.getDate() + 1)
-    const tomorrowStr = tomorrow.toISOString().slice(0, 10)
+      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ ╨╕ gc_manager
+      const { data: contractData } = await supabase
+        .from('contracts')
+        .select('title, number, author_bitrix_id')
+        .eq('id', contract_id)
+        .single()
 
-    // 3 ╨┤╨╜╤П ╨╜╨░╨╖╨░╨┤
-    const threeDaysAgo = new Date(now)
-    threeDaysAgo.setDate(threeDaysAgo.getDate() - 3)
-    const threeDaysAgoStr = threeDaysAgo.toISOString().slice(0, 10)
+      if (contractData) {
+        // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╨┐╨╛╤Б╨╗╨╡╨┤╨╜╨╡╨╣ ╤Б╨╡╤Б╤Б╨╕╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
+        const { data: lastSession } = await supabase
+          .from('approval_sessions')
+          .select('id')
+          .eq('contract_id', contract_id)
+          .order('created_at', { ascending: false })
+          .limit(1)
+          .single()
 
-    // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╨░╨║╤В╨╕╨▓╨╜╤Л╨╡ ╤Б╨╡╤Б╤Б╨╕╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
-    const { data: sessions, error } = await supabase
-      .from('approval_sessions')
-      .select(`
-        id,
-        contract_id,
-        deadline,
-        initiated_by_bitrix_id,
-        bitrix_chat_id,
-        contracts (
-          id, number, title, author_bitrix_id
-        ),
-        approval_participants (
-          bitrix_user_id,
-          status
-        )
-      `)
-      .eq('status', 'active')
-      .not('deadline', 'is', null)
+        let participantIds: number[] = []
+        if (lastSession) {
+          const { data: participantsData } = await supabase
+            .from('approval_participants')
+            .select('bitrix_user_id')
+            .eq('session_id', lastSession.id)
+            .not('bitrix_user_id', 'is', null)
+          participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
+        }
 
-    if (error) return NextResponse.json({ error: error.message }, { status: 500 })
-    if (!sessions || sessions.length === 0) {
-      return NextResponse.json({ success: true, notified: 0, message: '╨Э╨╡╤В ╨░╨║╤В╨╕╨▓╨╜╤Л╤Е ╤Б╨╡╤Б╤Б╨╕╨╣' })
+        const gcManagerIds = [1, 246, 504]
+        const recipients = [...new Set([
+          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
+          ...gcManagerIds,
+          ...participantIds,
+        ])]
+        await sendBitrixNotify({
+          recipients,
+          type: 'documents_uploaded',
+          document_id: contract_id,
+          document_title: contractData.title ?? '',
+          document_number: contractData.number ?? '',
+        })
+        
+      }
+
+      return NextResponse.json({ success: true, status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' })
     }
 
-    const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
-    let notified = 0
+    // ╨в╤А╨░╨╜╤Б╨╗╨╕╤В╨╡╤А╨░╤Ж╨╕╤П ╨╕╨╝╨╡╨╜╨╕ ╤Д╨░╨╣╨╗╨░
+    const safeFileName = file.name
+      .replace(/тДЦ/g, 'N')
+      .replace(/[╨░-╤П╤С╨Р-╨п╨Б\s]/g, (char) => {
+      const map: Record<string, string> = {
+        '╨░':'a','╨▒':'b','╨▓':'v','╨│':'g','╨┤':'d','╨╡':'e','╤С':'yo','╨╢':'zh','╨╖':'z','╨╕':'i',
+        '╨╣':'j','╨║':'k','╨╗':'l','╨╝':'m','╨╜':'n','╨╛':'o','╨┐':'p','╤А':'r','╤Б':'s','╤В':'t',
+        '╤Г':'u','╤Д':'f','╤Е':'h','╤Ж':'ts','╤З':'ch','╤И':'sh','╤Й':'sch','╤К':'','╤Л':'y','╤М':'',
+        '╤Н':'e','╤О':'yu','╤П':'ya',' ':'_',
+        '╨Р':'A','╨С':'B','╨Т':'V','╨У':'G','╨Ф':'D','╨Х':'E','╨Б':'Yo','╨Ц':'Zh','╨Ч':'Z','╨Ш':'I',
+        '╨Щ':'J','╨Ъ':'K','╨Ы':'L','╨Ь':'M','╨Э':'N','╨Ю':'O','╨Я':'P','╨а':'R','╨б':'S','╨в':'T',
+        '╨г':'U','╨д':'F','╨е':'H','╨ж':'Ts','╨з':'Ch','╨и':'Sh','╨й':'Sch','╨к':'','╨л':'Y','╨м':'',
+        '╨н':'E','╨о':'Yu','╨п':'Ya'
+      }
+      return map[char] ?? '_'
+    })
 
-    for (const session of sessions) {
-      const contract = Array.isArray(session.contracts)
-        ? session.contracts[0]
-        : session.contracts
-      if (!contract) continue
+    const filePath = `signed/${contract_id}/${Date.now()}_${safeFileName}`
+    const arrayBuffer = await file.arrayBuffer()
 
-      const deadlineStr = session.deadline.slice(0, 10)
-      const link = `https://${bitrixPortal}/contracts/${contract.id}`
-      const docRef = `[URL=${link}]${contract.number} тАФ ${contract.title}[/URL]`
+    // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╤Д╨░╨╣╨╗ ╨▓ Storage
+    const { error: uploadError } = await supabase.storage
+      .from('contracts')
+      .upload(filePath, new Uint8Array(arrayBuffer), {
+        contentType: file.type,
+        upsert: false,
+      })
 
-      // ╨г╤З╨░╤Б╤В╨╜╨╕╨║╨╕, ╨║╨╛╤В╨╛╤А╤Л╨╡ ╨╡╤Й╤С ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕
-      const pendingParticipants = (session.approval_participants ?? [])
-        .filter((p: { status: string; bitrix_user_id: number | null }) =>
-          p.status === 'pending' && p.bitrix_user_id
-        )
-        .map((p: { bitrix_user_id: number }) => p.bitrix_user_id)
+    if (uploadError) {
+      return NextResponse.json({ error: uploadError.message }, { status: 400 })
+    }
 
-      // ╨Ш╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А + ╨░╨▓╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░
-      const initiators = [...new Set([
-        session.initiated_by_bitrix_id,
-        contract.author_bitrix_id,
-      ].filter(Boolean) as number[])]
+    const { data: urlData } = supabase.storage
+      .from('contracts')
+      .getPublicUrl(filePath)
 
-      // ╨Я╨╛╨╗╤Г╤З╨░╤В╨╡╨╗╨╕ ╨║╨╛╨╗╨╛╨║╨╛╨╗╤М╤З╨╕╨║╨░ = ╤В╨╡ ╨║╤В╨╛ ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗ + ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А
-      const notifyRecipients = [...new Set([...pendingParticipants, ...initiators])]
+    // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨▓ ╨С╨Ф
+    const { error: dbError } = await supabase
+      .from('signed_documents')
+      .insert({
+        contract_id,
+        file_url: urlData.publicUrl,
+        file_name: file.name,
+        uploaded_by_name: user_name,
+        uploaded_by_bitrix_id: userId,
+      })
 
-      // ╨б╨┐╨╕╤Б╨╛╨║ ╨╛╨╢╨╕╨┤╨░╤О╤Й╨╕╤Е ╨┤╨╗╤П ╤В╨╡╨║╤Б╤В╨░ ╤Б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╤П
-      const pendingCount = pendingParticipants.length
-      const pendingNote = pendingCount > 0
-        ? `╨Ю╨╢╨╕╨┤╨░╤О╤В ╤А╨╡╤И╨╡╨╜╨╕╤П: ${pendingCount} ╤Г╤З╨░╤Б╤В╨╜╨╕╨║(╨╛╨▓).`
-        : ''
+    if (dbError) {
+      return NextResponse.json({ error: dbError.message }, { status: 400 })
+    }
 
-      let notifyType: 'approval_deadline_soon' | 'approval_deadline_reached' | 'approval_deadline_overdue' | null = null
-      let chatMessage = ''
+    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ ╨▒╨╡╨╖ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╤Д╨░╨╣╨╗╨░
+    if (status_only && confirm_all) {
+      await supabase
+        .from('contracts')
+        .update({
+          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
+          signed_at: new Date().toISOString(),
+          signed_by_name: user_name,
+          signed_by_bitrix_id: userId,
+        })
+        .eq('id', contract_id)
 
-      if (deadlineStr === tomorrowStr) {
-        // ╨Ч╨░ 1 ╨┤╨╡╨╜╤М ╨┤╨╛ ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
-        notifyType = 'approval_deadline_soon'
-        chatMessage = `тЪая╕П ╨б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╕╤Б╤В╨╡╨║╨░╨╡╤В ╨╖╨░╨▓╤В╤А╨░!\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
-      } else if (deadlineStr === todayStr) {
-        // ╨Т ╨┤╨╡╨╜╤М ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
-        notifyType = 'approval_deadline_reached'
-        chatMessage = `ЁЯФ┤ ╨б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╕╤Б╤В╤С╨║ ╤Б╨╡╨│╨╛╨┤╨╜╤П!\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
-      } else if (deadlineStr === threeDaysAgoStr) {
-        // ╨з╨╡╤А╨╡╨╖ 3 ╨┤╨╜╤П ╨┐╨╛╤Б╨╗╨╡ ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
-        notifyType = 'approval_deadline_overdue'
-        chatMessage = `ЁЯЪи ╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╛╤Б╤А╨╛╤З╨╡╨╜╨╛ ╨╜╨░ 3 ╨┤╨╜╤П. ╨а╨╡╨║╨╛╨╝╨╡╨╜╨┤╤Г╨╡╨╝ ╨┐╤А╨╛╨┤╨╗╨╕╤В╤М ╤Б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╤Б ╤Г╨║╨░╨╖╨░╨╜╨╕╨╡╨╝ ╨┐╤А╨╕╤З╨╕╨╜╤Л.\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
-      }
+      await supabase.from('contract_logs').insert({
+        contract_id,
+        action: '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л',
+        details: '╨б╤В╨░╤В╤Г╤Б ╨╕╨╖╨╝╨╡╨╜╤С╨╜ ╨╜╨░ "╨Я╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л"',
+        user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+      })
 
-      if (!notifyType) continue
+      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ ╨╕ gc_manager
+      const { data: contractData } = await supabase
+        .from('contracts')
+        .select('title, number, author_bitrix_id')
+        .eq('id', contract_id)
+        .single()
 
-      // 1. ╨Ъ╨╛╨╗╨╛╨║╨╛╨╗╤М╤З╨╕╨║ (pending + ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А)
-      if (notifyRecipients.length > 0) {
+      if (contractData) {
+        const gcManagerIds = [1, 246, 504]
+        const recipients = [...new Set([
+          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
+          ...gcManagerIds,
+        ])]
         await sendBitrixNotify({
-          recipients: notifyRecipients,
-          type: notifyType,
-          document_id: contract.id,
-          document_title: contract.title ?? '',
-          document_number: contract.number ?? '',
-          extra: pendingNote,
+          recipients,
+          type: 'documents_uploaded',
+          document_id: contract_id,
+          document_title: contractData.title ?? '',
+          document_number: contractData.number ?? '',
         })
+        
       }
 
-      // 2. ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В ╨С24
-      if (session.bitrix_chat_id) {
-        await sendBitrixChatMessage(session.bitrix_chat_id, chatMessage)
-      }
+      return NextResponse.json({ success: true, status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' })
+    }
 
-      // 3. ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨╗╨╡╨╜╤В╤Г ╨╜╨╛╨▓╨╛╤Б╤В╨╡╨╣ ╨С24 (im.message.add ╨▓╤Б╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░╨╝)
-      const webhookUrl = process.env.BITRIX_WEBHOOK_URL
-      if (webhookUrl && notifyRecipients.length > 0) {
-        await Promise.all(notifyRecipients.map(async (userId) => {
-          try {
-            await fetch(`${webhookUrl}im.message.add.json`, {
-              method: 'POST',
-              headers: { 'Content-Type': 'application/json' },
-              body: JSON.stringify({
-                DIALOG_ID: userId,
-                MESSAGE: chatMessage,
-              }),
-            })
-          } catch (err) {
-            console.error(`approval-reminder message error for user ${userId}:`, err)
-          }
-        }))
-      }
+    // ╨Ю╨┐╤А╨╡╨┤╨╡╨╗╤П╨╡╨╝ ╨╜╨╛╨▓╤Л╨╣ ╤Б╤В╨░╤В╤Г╤Б
+    let newStatus = '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛'
+    let logAction = '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╤Н╨║╨╖╨╡╨╝╨┐╨╗╤П╤А'
 
-      notified++
+    if (confirm_all) {
+      newStatus = '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜'
+      logAction = '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л'
+
+      await supabase
+        .from('contracts')
+        .update({
+          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
+          signed_at: new Date().toISOString(),
+          signed_by_name: user_name,
+          signed_by_bitrix_id: userId,
+        })
+        .eq('id', contract_id)
+    } else {
+      await supabase
+        .from('contracts')
+        .update({ status: '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛' })
+        .eq('id', contract_id)
     }
 
-    return NextResponse.json({ success: true, notified, sessions_found: sessions.length })
+    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
+    await supabase.from('contract_logs').insert({
+      contract_id,
+      action: logAction,
+      details: `╨д╨░╨╣╨╗: ${file.name}`,
+      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+    })
+
+    return NextResponse.json({ success: true, status: newStatus })
 
   } catch (err) {
     const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
@@ -7085,739 +7144,799 @@ export async function GET() {
   }
 }
 
-================
-File: app/api/signed-documents/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
-import { sendBitrixNotify, sendBitrixMessage } from '@/app/lib/notify'
-
-export const maxDuration = 60
-
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
-
-const ADMIN_IDS = [30, 1148]
-const GC_MANAGER_IDS = [1, 246, 504]
-// ╨Т╨╗╨░╨┤╨╕╨╝╨╕╤А╨╛╨▓ ╨Т╤П╤З╨╡╤Б╨╗╨░╨▓ (╨н-╨Ъ), ╨Т╨╕╨╜╨╛╨│╤А╨░╨┤╨╛╨▓╨░ ╨Р╨╜╨╜╨░ (╨б╨Я╨в, ╨Ю╨б, ╨Э╨Я╨Я)
-const SPECIAL_SIGNERS: Record<number, string[]> = {
-  782: ['╨н-╨Ъ'],
-  152: ['╨б╨Я╨в', '╨Ю╨б', '╨Э╨Я╨Я'],
-}
-
-function canUploadSigned(
-  userId: number,
-  authorId: number | null,
-  initiatorId: number | null,
-  companyPrefix: string
-): boolean {
-  if (ADMIN_IDS.includes(userId)) return true
-  if (GC_MANAGER_IDS.includes(userId)) return true
-  if (authorId === userId) return true
-  if (initiatorId === userId) return true
-  for (const [id, prefixes] of Object.entries(SPECIAL_SIGNERS)) {
-    if (parseInt(id) === userId && prefixes.includes(companyPrefix)) return true
-  }
-  return false
-}
-
-// ╨Я╨╛╨╗╤Г╤З╨╕╤В╤М ╤Б╨┐╨╕╤Б╨╛╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╤Е ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨╛╨▓
-export async function GET(request: NextRequest) {
-  const contract_id = request.nextUrl.searchParams.get('contract_id')
-  if (!contract_id) {
-    return NextResponse.json({ error: 'contract_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
-  }
-
-  const { data, error } = await supabase
-    .from('signed_documents')
-    .select('*')
-    .eq('contract_id', contract_id)
-    .order('created_at')
-
-  if (error) {
-    return NextResponse.json({ error: error.message }, { status: 400 })
-  }
-
-  return NextResponse.json({ documents: data ?? [] })
-}
-
-// ╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В
-export async function POST(request: NextRequest) {
+// ╨г╨┤╨░╨╗╨╕╤В╤М ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В
+export async function DELETE(request: NextRequest) {
   try {
-    const formData = await request.formData()
-    const file = formData.get('file') as File
-    const contract_id = formData.get('contract_id') as string
-    const user_name = formData.get('user_name') as string
-    const user_bitrix_id = formData.get('user_bitrix_id') as string
-    const confirm_all = formData.get('confirm_all') === 'true'
+    const body = await request.json()
+    const { id, file_url, contract_id, user_name, user_bitrix_id } = body
 
-    if (!contract_id || !user_bitrix_id) {
+    if (!id || !user_bitrix_id) {
       return NextResponse.json({ error: '╨Э╨╡ ╨▓╤Б╨╡ ╨┐╨░╤А╨░╨╝╨╡╤В╤А╤Л ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л' }, { status: 400 })
     }
 
     const userId = parseInt(user_bitrix_id)
-    const status_only = formData.get('status_only') === 'true'
-    const temp_upload = formData.get('temp_upload') === 'true'
-    const confirm_upload = formData.get('confirm_upload') === 'true'
-    const temp_file_url = formData.get('temp_file_url') as string
-    const temp_file_name = formData.get('temp_file_name') as string
-    const has_discrepancies = formData.get('has_discrepancies') === 'true'
-    const discrepancy_comment = formData.get('discrepancy_comment') as string
-    const discrepancy_summary = formData.get('discrepancy_summary') as string
 
-    // ╨Т╤А╨╡╨╝╨╡╨╜╨╜╨░╤П ╨╖╨░╨│╤А╤Г╨╖╨║╨░ тАФ ╨┐╤А╨╛╤Б╤В╨╛ ╤Б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ URL
-    if (temp_upload && file) {
-      const fileExt = file.name.split('.').pop()
-      const tempPath = `temp/${contract_id}/${Date.now()}.${fileExt}`
-      const fileBuffer = await file.arrayBuffer()
-      const { error: uploadError } = await supabase.storage
-        .from('contracts')
-        .upload(tempPath, fileBuffer, { contentType: file.type, upsert: true })
-      if (uploadError) return NextResponse.json({ error: uploadError.message }, { status: 400 })
-      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(tempPath)
-      return NextResponse.json({ success: true, temp_url: urlData.publicUrl })
+    // ╨в╨╛╨╗╤М╨║╨╛ admin ╨╕ gc_manager ╨╝╨╛╨│╤Г╤В ╤Г╨┤╨░╨╗╤П╤В╤М
+    if (!ADMIN_IDS.includes(userId) && !GC_MANAGER_IDS.includes(userId)) {
+      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╤Г╨┤╨░╨╗╨╡╨╜╨╕╤П' }, { status: 403 })
     }
 
-    // ╨Я╨╛╨┤╤В╨▓╨╡╤А╨╢╨┤╨╡╨╜╨╕╨╡ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ тАФ ╤Б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╤Г╨╢╨╡ ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╨╜╤Л╨╣ ╤Д╨░╨╣╨╗ ╨║╨░╨║ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣
-    if (confirm_upload && temp_file_url) {
-      // ╨Я╨╡╤А╨╡╨╝╨╡╤Й╨░╨╡╨╝ ╨╕╨╖ temp ╨▓ ╨┐╨╛╤Б╤В╨╛╤П╨╜╨╜╨╛╨╡ ╤Е╤А╨░╨╜╨╕╨╗╨╕╤Й╨╡
-      const fileExt = temp_file_name.split('.').pop()
-      const finalPath = `signed/${contract_id}/${Date.now()}.${fileExt}`
-      const tempPath = temp_file_url.split('/contracts/')[1]
+    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
+    if (file_url) {
+      const filePath = file_url.split('/contracts/')[1]
+      if (filePath) {
+        await supabase.storage.from('contracts').remove([filePath])
+      }
+    }
 
-      // ╨Ъ╨╛╨┐╨╕╤А╤Г╨╡╨╝ ╤Д╨░╨╣╨╗
-      const { error: copyError } = await supabase.storage
-        .from('contracts')
-        .copy(tempPath, finalPath)
+    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╕╨╖ ╨С╨Ф
+    await supabase.from('signed_documents').delete().eq('id', id)
 
-      if (copyError) {
-        // ╨Х╤Б╨╗╨╕ copy ╨╜╨╡ ╤А╨░╨▒╨╛╤В╨░╨╡╤В тАФ ╤Б╨║╨░╤З╨╕╨▓╨░╨╡╨╝ ╨╕ ╨┐╨╡╤А╨╡╨╖╨░╨╗╨╕╨▓╨░╨╡╨╝
-        const fileRes = await fetch(temp_file_url)
-        const fileBuffer = await fileRes.arrayBuffer()
-        await supabase.storage.from('contracts').upload(finalPath, fileBuffer, { upsert: true })
-      }
+    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨╛╤Б╤В╨░╨╗╨╕╤Б╤М ╨╗╨╕ ╨╡╤Й╤С ╤Д╨░╨╣╨╗╤Л
+    const { count } = await supabase
+      .from('signed_documents')
+      .select('*', { count: 'exact', head: true })
+      .eq('contract_id', contract_id)
 
-      // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨▓╤А╨╡╨╝╨╡╨╜╨╜╤Л╨╣ ╤Д╨░╨╣╨╗
-      await supabase.storage.from('contracts').remove([tempPath])
+    // ╨Х╤Б╨╗╨╕ ╤Д╨░╨╣╨╗╨╛╨▓ ╨╜╨╡ ╨╛╤Б╤В╨░╨╗╨╛╤Б╤М тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜
+    if (count === 0) {
+      await supabase
+        .from('contracts')
+        .update({ status: '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜' })
+        .eq('id', contract_id)
+    }
 
-      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(finalPath)
-      const finalUrl = urlData.publicUrl
+    await supabase.from('contract_logs').insert({
+      contract_id,
+      action: '╨г╨┤╨░╨╗╤С╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╤Н╨║╨╖╨╡╨╝╨┐╨╗╤П╤А',
+      details: '',
+      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+    })
 
-      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨▓ ╨С╨Ф
-      await supabase.from('signed_documents').insert({
-        contract_id,
-        file_url: finalUrl,
-        file_name: temp_file_name,
-        uploaded_by_name: user_name,
-        uploaded_by_bitrix_id: userId,
-        signed_date: formData.get('signed_date') || null,
-        has_discrepancies,
-        discrepancy_comment: has_discrepancies ? discrepancy_comment : null,
-      })
+    return NextResponse.json({ success: true })
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500 })
+  }
+}
 
-      // ╨Ы╨╛╨│
-      await supabase.from('contract_logs').insert({
-        contract_id,
-        action: has_discrepancies ? '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В ╤Б ╤А╨░╤Б╤Е╨╛╨╢╨┤╨╡╨╜╨╕╤П╨╝╨╕' : '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В',
-        details: has_discrepancies
-          ? `╨а╨░╤Б╤Е╨╛╨╢╨┤╨╡╨╜╨╕╤П: ${discrepancy_summary}. ╨Я╤А╨╕╤З╨╕╨╜╨░ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕: ${discrepancy_comment}`
-          : `╨д╨░╨╣╨╗: ${temp_file_name}`,
-        user_name: user_name ?? '╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М',
-      })
+================
+File: app/api/upload-proxy/route.ts
+================
+import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
 
-      // ╨Ю╨▒╨╜╨╛╨▓╨╗╤П╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░
-      const { data: currentDocs } = await supabase
-        .from('signed_documents')
-        .select('id')
-        .eq('contract_id', contract_id)
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
 
-      const docsCount = (currentDocs?.length ?? 0)
-      const newStatus = docsCount >= 2 ? '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' : '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛'
-      await supabase.from('contracts').update({ status: newStatus }).eq('id', contract_id)
+export const maxDuration = 60
+export const dynamic = 'force-dynamic'
 
-      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨▓╤Б╨╡╤Е ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╛ ╨╖╨░╨│╤А╤Г╨╖╨║╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╨╛╨│╨╛ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░
-      const { data: lastSession2 } = await supabase
-        .from('approval_sessions')
-        .select('id, initiated_by_bitrix_id')
-        .eq('contract_id', contract_id)
-        .order('created_at', { ascending: false })
-        .limit(1)
-        .single()
+// ╨Ю╤В╨║╨╗╤О╤З╨░╨╡╨╝ ╨▓╤Б╤В╤А╨╛╨╡╨╜╨╜╤Л╨╣ body parser Next.js ╨┤╨╗╤П ╨▒╨╛╨╗╤М╤И╨╕╤Е ╤Д╨░╨╣╨╗╨╛╨▓
+export const fetchCache = 'force-no-store'
+
+const CORS = { 'Access-Control-Allow-Origin': '*' }
 
-      if (lastSession2) {
-        const { data: participantsData2 } = await supabase
-          .from('approval_participants')
-          .select('bitrix_user_id')
-          .eq('session_id', lastSession2.id)
-          .not('bitrix_user_id', 'is', null)
-        const participantIds2 = participantsData2?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
-        const { data: contractData2 } = await supabase
-          .from('contracts')
-          .select('title, number, author_bitrix_id')
-          .eq('id', contract_id)
-          .single()
-        if (contractData2) {
-          const recipients2 = [...new Set([
-            ...(contractData2.author_bitrix_id ? [contractData2.author_bitrix_id] : []),
-            ...(lastSession2.initiated_by_bitrix_id ? [lastSession2.initiated_by_bitrix_id] : []),
-            ...participantIds2,
-          ])]
-          await sendBitrixNotify({
-            recipients: recipients2,
-            type: 'documents_uploaded',
-            document_id: contract_id,
-            document_title: contractData2.title ?? '',
-            document_number: contractData2.number ?? '',
-          })
-        }
-      }
+export async function OPTIONS() {
+  return new NextResponse(null, {
+    headers: { ...CORS, 'Access-Control-Allow-Methods': 'POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' }
+  })
+}
 
-      return NextResponse.json({ success: true })
+export async function POST(request: NextRequest) {
+  try {
+    const formData = await request.formData()
+    const file = formData.get('file') as File | null
+    const bucket = formData.get('bucket') as string | null
+    const path = formData.get('path') as string | null
+
+    if (!file || !bucket || !path) {
+      return NextResponse.json({ error: 'file, bucket ╨╕ path ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400, headers: CORS })
     }
 
-    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ тАФ ╤Д╨░╨╣╨╗ ╨╜╨╡ ╨╜╤Г╨╢╨╡╨╜
-    if (!status_only && !file) {
-      return NextResponse.json({ error: '╨д╨░╨╣╨╗ ╨╜╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜' }, { status: 400 })
+    const allowedBuckets = ['counterparty-docs', 'contracts']
+    if (!allowedBuckets.includes(bucket)) {
+      return NextResponse.json({ error: '╨Э╨╡╨┤╨╛╨┐╤Г╤Б╤В╨╕╨╝╤Л╨╣ bucket' }, { status: 403, headers: CORS })
     }
 
-    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨║╨╛╨╜╤В╤А╨░╨║╤В
-    const { data: contract, error: contractError } = await supabase
-      .from('contracts')
-      .select('id, status, author_bitrix_id, number')
-      .eq('id', contract_id)
-      .single()
+    const arrayBuffer = await file.arrayBuffer()
+    const buffer = new Uint8Array(arrayBuffer)
 
-    if (contractError || !contract) {
-      return NextResponse.json({ error: `╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В ╨╜╨╡ ╨╜╨░╨╣╨┤╨╡╨╜: ${contractError?.message ?? '╨╜╨╡╤В ╨┤╨░╨╜╨╜╤Л╤Е'}` }, { status: 404 })
+    const { error: uploadError } = await supabase.storage
+      .from(bucket)
+      .upload(path, buffer, {
+        contentType: file.type || 'application/octet-stream',
+        upsert: true,
+      })
+
+    if (uploadError) {
+      return NextResponse.json({ error: uploadError.message }, { status: 500, headers: CORS })
     }
 
-    // ╨Ш╨╖╨▓╨╗╨╡╨║╨░╨╡╨╝ ╨┐╤А╨╡╤Д╨╕╨║╤Б ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕ ╨╕╨╖ ╨╜╨╛╨╝╨╡╤А╨░ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ (╨в╨е-╨Ф╨Ю╨У-2026/... тЖТ ╨в╨е)
-    const companyPrefix = contract.number?.split('-')[0] ?? ''
+    const { data: publicData } = supabase.storage
+      .from(bucket)
+      .getPublicUrl(path)
 
-    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А╨░ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
-    const { data: session } = await supabase
-      .from('approval_sessions')
-      .select('initiated_by_bitrix_id')
-      .eq('contract_id', contract_id)
-      .order('created_at', { ascending: false })
-      .limit(1)
-      .single()
+    return NextResponse.json({
+      success: true,
+      public_url: publicData.publicUrl,
+    }, { headers: CORS })
 
-    const initiatorId = session?.initiated_by_bitrix_id ?? null
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500, headers: CORS })
+  }
+}
 
-    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╤А╨░╨▓╨░
-    if (!canUploadSigned(userId, contract.author_bitrix_id, initiatorId, companyPrefix)) {
-      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╨╛╨│╨╛ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░' }, { status: 403 })
-    }
+================
+File: app/api/versions/route.ts
+================
+import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
 
-    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ ╨▒╨╡╨╖ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╤Д╨░╨╣╨╗╨░ тАФ ╨┤╨╡╨╗╨░╨╡╨╝ ╤Н╤В╨╛ ╤Б╤А╨░╨╖╤Г
-    if (status_only && confirm_all) {
-      await supabase
-        .from('contracts')
-        .update({
-          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
-          signed_at: new Date().toISOString(),
-          signed_by_name: user_name,
-          signed_by_bitrix_id: userId,
-        })
-        .eq('id', contract_id)
+export const maxDuration = 60
 
-      await supabase.from('contract_logs').insert({
-        contract_id,
-        action: '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л',
-        details: '╨б╤В╨░╤В╤Г╤Б ╨╕╨╖╨╝╨╡╨╜╤С╨╜ ╨╜╨░ "╨Я╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л"',
-        user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
-      })
+export async function GET(request: NextRequest) {
+  const supabase = createClient(
+    process.env.NEXT_PUBLIC_SUPABASE_URL!,
+    process.env.SUPABASE_SECRET_KEY!
+  )
+  const contractId = request.nextUrl.searchParams.get('contract_id')
+  if (!contractId) {
+    return NextResponse.json({ error: 'contract_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+  }
+  const { data: versions, error } = await supabase
+    .from('versions')
+    .select('*')
+    .eq('contract_id', contractId)
+    .order('version_number', { ascending: false })
 
-      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ ╨╕ gc_manager
-      const { data: contractData } = await supabase
-        .from('contracts')
-        .select('title, number, author_bitrix_id')
-        .eq('id', contract_id)
-        .single()
+  if (error) {
+    return NextResponse.json({ error: error.message }, { status: 400 })
+  }
+  return NextResponse.json({ versions: versions ?? [] })
+}
 
-      if (contractData) {
-        // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨╛╨▓ ╨┐╨╛╤Б╨╗╨╡╨┤╨╜╨╡╨╣ ╤Б╨╡╤Б╤Б╨╕╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
-        const { data: lastSession } = await supabase
-          .from('approval_sessions')
-          .select('id')
-          .eq('contract_id', contract_id)
-          .order('created_at', { ascending: false })
-          .limit(1)
-          .single()
+export async function POST(request: NextRequest) {
+  console.log('=== versions API called ===')
 
-        let participantIds: number[] = []
-        if (lastSession) {
-          const { data: participantsData } = await supabase
-            .from('approval_participants')
-            .select('bitrix_user_id')
-            .eq('session_id', lastSession.id)
-            .not('bitrix_user_id', 'is', null)
-          participantIds = participantsData?.map(p => p.bitrix_user_id).filter(Boolean) ?? []
-        }
+  const supabase = createClient(
+    process.env.NEXT_PUBLIC_SUPABASE_URL!,
+    process.env.SUPABASE_SECRET_KEY!
+  )
 
-        const gcManagerIds = [1, 246, 504]
-        const recipients = [...new Set([
-          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
-          ...gcManagerIds,
-          ...participantIds,
-        ])]
-        await sendBitrixNotify({
-          recipients,
-          type: 'documents_uploaded',
-          document_id: contract_id,
-          document_title: contractData.title ?? '',
-          document_number: contractData.number ?? '',
-        })
-        
-      }
+  try {
+    const contentType = request.headers.get('content-type') ?? ''
+    let contractId: string
+    let comment: string
+    let userName: string
+    let fileUrl: string
+    let fileName: string
 
-      return NextResponse.json({ success: true, status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' })
-    }
+    if (contentType.includes('application/json')) {
+      // ╨д╨░╨╣╨╗ ╤Г╨╢╨╡ ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜ ╤З╨╡╤А╨╡╨╖ uploadFileDirect тАФ ╨┐╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤В╨╛╨╗╤М╨║╨╛ ╨╝╨╡╤В╨░╨┤╨░╨╜╨╜╤Л╨╡
+      const json = await request.json()
+      contractId = json.contract_id
+      comment = json.comment ?? ''
+      userName = json.user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░'
+      fileUrl = json.file_url
+      fileName = json.file_name
 
-    // ╨в╤А╨░╨╜╤Б╨╗╨╕╤В╨╡╤А╨░╤Ж╨╕╤П ╨╕╨╝╨╡╨╜╨╕ ╤Д╨░╨╣╨╗╨░
-    const safeFileName = file.name
-      .replace(/тДЦ/g, 'N')
-      .replace(/[╨░-╤П╤С╨Р-╨п╨Б\s]/g, (char) => {
-      const map: Record<string, string> = {
-        '╨░':'a','╨▒':'b','╨▓':'v','╨│':'g','╨┤':'d','╨╡':'e','╤С':'yo','╨╢':'zh','╨╖':'z','╨╕':'i',
-        '╨╣':'j','╨║':'k','╨╗':'l','╨╝':'m','╨╜':'n','╨╛':'o','╨┐':'p','╤А':'r','╤Б':'s','╤В':'t',
-        '╤Г':'u','╤Д':'f','╤Е':'h','╤Ж':'ts','╤З':'ch','╤И':'sh','╤Й':'sch','╤К':'','╤Л':'y','╤М':'',
-        '╤Н':'e','╤О':'yu','╤П':'ya',' ':'_',
-        '╨Р':'A','╨С':'B','╨Т':'V','╨У':'G','╨Ф':'D','╨Х':'E','╨Б':'Yo','╨Ц':'Zh','╨Ч':'Z','╨Ш':'I',
-        '╨Щ':'J','╨Ъ':'K','╨Ы':'L','╨Ь':'M','╨Э':'N','╨Ю':'O','╨Я':'P','╨а':'R','╨б':'S','╨в':'T',
-        '╨г':'U','╨д':'F','╨е':'H','╨ж':'Ts','╨з':'Ch','╨и':'Sh','╨й':'Sch','╨к':'','╨л':'Y','╨м':'',
-        '╨н':'E','╨о':'Yu','╨п':'Ya'
+      if (!contractId || !fileUrl || !fileName) {
+        return NextResponse.json({ error: 'contract_id, file_url ╨╕ file_name ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400 })
       }
-      return map[char] ?? '_'
-    })
+    } else {
+      // ╨б╤В╨░╤А╤Л╨╣ ╨┐╤Г╤В╤М ╤З╨╡╤А╨╡╨╖ FormData (╤Д╨░╨╣╨╗╤Л ╨┤╨╛ 4.5 ╨Ь╨С)
+      const formData = await request.formData()
+      const file = formData.get('file') as File
+      contractId = formData.get('contract_id') as string
+      comment = formData.get('comment') as string ?? ''
+      userName = formData.get('user_name') as string ?? '╨б╨╕╤Б╤В╨╡╨╝╨░'
 
-    const filePath = `signed/${contract_id}/${Date.now()}_${safeFileName}`
-    const arrayBuffer = await file.arrayBuffer()
+      if (!file || !contractId) {
+        return NextResponse.json({ error: '╨д╨░╨╣╨╗ ╨╕ ID ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400 })
+      }
 
-    // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╤Д╨░╨╣╨╗ ╨▓ Storage
-    const { error: uploadError } = await supabase.storage
-      .from('contracts')
-      .upload(filePath, new Uint8Array(arrayBuffer), {
-        contentType: file.type,
-        upsert: false,
-      })
+      const { count } = await supabase
+        .from('versions')
+        .select('*', { count: 'exact', head: true })
+        .eq('contract_id', contractId)
 
-    if (uploadError) {
-      return NextResponse.json({ error: uploadError.message }, { status: 400 })
+      const versionNumber = (count ?? 0) + 1
+      const fileExt = file.name.split('.').pop()
+      const filePath = `${contractId}/v${versionNumber}_${Date.now()}.${fileExt}`
+      const arrayBuffer = await file.arrayBuffer()
+      const fileBuffer = new Uint8Array(arrayBuffer)
+
+      const { error: uploadError } = await supabase.storage
+        .from('contracts')
+        .upload(filePath, fileBuffer, { contentType: file.type, upsert: false })
+
+      if (uploadError) {
+        return NextResponse.json({ error: uploadError.message }, { status: 400 })
+      }
+
+      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(filePath)
+      fileUrl = urlData.publicUrl
+      fileName = file.name
     }
 
-    const { data: urlData } = supabase.storage
-      .from('contracts')
-      .getPublicUrl(filePath)
+    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨╜╨╛╨╝╨╡╤А ╨▓╨╡╤А╤Б╨╕╨╕
+    const { count: vCount } = await supabase
+      .from('versions')
+      .select('*', { count: 'exact', head: true })
+      .eq('contract_id', contractId)
+    const versionNumber = (vCount ?? 0) + 1
 
-    // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨▓ ╨С╨Ф
-    const { error: dbError } = await supabase
-      .from('signed_documents')
+    const { error: versionError } = await supabase
+      .from('versions')
       .insert({
-        contract_id,
-        file_url: urlData.publicUrl,
-        file_name: file.name,
-        uploaded_by_name: user_name,
-        uploaded_by_bitrix_id: userId,
+        contract_id: contractId,
+        version_number: versionNumber,
+        file_url: fileUrl,
+        file_name: fileName,
+        comment: comment || null,
       })
 
-    if (dbError) {
-      return NextResponse.json({ error: dbError.message }, { status: 400 })
-    }
+    console.log('version insert:', versionError ? versionError.message : 'success')
 
-    // ╨Х╤Б╨╗╨╕ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╨▒╨╜╨╛╨▓╨╗╨╡╨╜╨╕╨╡ ╤Б╤В╨░╤В╤Г╤Б╨░ ╨▒╨╡╨╖ ╨╖╨░╨│╤А╤Г╨╖╨║╨╕ ╤Д╨░╨╣╨╗╨░
-    if (status_only && confirm_all) {
-      await supabase
-        .from('contracts')
-        .update({
-          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
-          signed_at: new Date().toISOString(),
-          signed_by_name: user_name,
-          signed_by_bitrix_id: userId,
-        })
-        .eq('id', contract_id)
+    if (versionError) {
+      return NextResponse.json({ error: versionError.message }, { status: 400 })
+    }
 
-      await supabase.from('contract_logs').insert({
-        contract_id,
-        action: '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л',
-        details: '╨б╤В╨░╤В╤Г╤Б ╨╕╨╖╨╝╨╡╨╜╤С╨╜ ╨╜╨░ "╨Я╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л"',
-        user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+    await supabase
+      .from('contract_logs')
+      .insert({
+        contract_id: contractId,
+        action: `╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜╨░ ╨▓╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ v${versionNumber}`,
+        details: `╨д╨░╨╣╨╗: ${fileName}${comment ? '. ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ' + comment : ''}`,
+        user_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
       })
 
-      // ╨г╨▓╨╡╨┤╨╛╨╝╨╗╤П╨╡╨╝ ╨░╨▓╤В╨╛╤А╨░ ╨╕ gc_manager
-      const { data: contractData } = await supabase
-        .from('contracts')
-        .select('title, number, author_bitrix_id')
-        .eq('id', contract_id)
-        .single()
+    console.log('=== versions API success ===')
+    return NextResponse.json({ success: true, version: versionNumber })
 
-      if (contractData) {
-        const gcManagerIds = [1, 246, 504]
-        const recipients = [...new Set([
-          ...(contractData.author_bitrix_id ? [contractData.author_bitrix_id] : []),
-          ...gcManagerIds,
-        ])]
-        await sendBitrixNotify({
-          recipients,
-          type: 'documents_uploaded',
-          document_id: contract_id,
-          document_title: contractData.title ?? '',
-          document_number: contractData.number ?? '',
-        })
-        
-      }
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    console.error('versions API error:', message)
+    return NextResponse.json({ error: message }, { status: 500 })
+  }
+}
 
-      return NextResponse.json({ success: true, status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜' })
-    }
+export async function DELETE(request: NextRequest) {
+  const supabase = createClient(
+    process.env.NEXT_PUBLIC_SUPABASE_URL!,
+    process.env.SUPABASE_SECRET_KEY!
+  )
 
-    // ╨Ю╨┐╤А╨╡╨┤╨╡╨╗╤П╨╡╨╝ ╨╜╨╛╨▓╤Л╨╣ ╤Б╤В╨░╤В╤Г╤Б
-    let newStatus = '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛'
-    let logAction = '╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╤Н╨║╨╖╨╡╨╝╨┐╨╗╤П╤А'
+  try {
+    const body = await request.json()
+    const { version_id, file_path, contract_id, user_name } = body
 
-    if (confirm_all) {
-      newStatus = '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜'
-      logAction = '╨Т╤Б╨╡ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜╤Л'
+    if (!version_id) {
+      return NextResponse.json({ error: 'version_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+    }
 
-      await supabase
-        .from('contracts')
-        .update({
-          status: '╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜',
-          signed_at: new Date().toISOString(),
-          signed_by_name: user_name,
-          signed_by_bitrix_id: userId,
-        })
-        .eq('id', contract_id)
-    } else {
-      await supabase
+    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
+    if (file_path) {
+      await supabase.storage
         .from('contracts')
-        .update({ status: '╨╖╨░╨│╤А╤Г╨╢╨╡╨╜_╤З╨░╤Б╤В╨╕╤З╨╜╨╛' })
-        .eq('id', contract_id)
+        .remove([file_path])
     }
 
-    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
-    await supabase.from('contract_logs').insert({
-      contract_id,
-      action: logAction,
-      details: `╨д╨░╨╣╨╗: ${file.name}`,
-      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
-    })
+    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╕╨╖ ╨▒╨░╨╖╤Л
+    const { error } = await supabase
+      .from('versions')
+      .delete()
+      .eq('id', version_id)
 
-    return NextResponse.json({ success: true, status: newStatus })
+    if (error) {
+      return NextResponse.json({ error: error.message }, { status: 400 })
+    }
+
+    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
+    await supabase
+      .from('contract_logs')
+      .insert({
+        contract_id,
+        action: '╨Т╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ ╤Г╨┤╨░╨╗╨╡╨╜╨░',
+        details: `╨г╨┤╨░╨╗╨╡╨╜╨░ ╨▓╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░`,
+        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
+      })
 
+    return NextResponse.json({ success: true })
   } catch (err) {
     const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
     return NextResponse.json({ error: message }, { status: 500 })
   }
 }
 
-// ╨г╨┤╨░╨╗╨╕╤В╤М ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В
-export async function DELETE(request: NextRequest) {
+================
+File: app/api/bitrix-tasks/route.ts
+================
+import { NextRequest, NextResponse } from 'next/server'
+import { createClient } from '@supabase/supabase-js'
+
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
+
+const WEBHOOK = process.env.BITRIX_WEBHOOK_URL!
+
+// ╨Я╨╛╤Б╤В╨░╨╜╨╛╨▓╤Й╨╕╨║ = ╨У╨Ф ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕ ╨┐╨╛ ╨┐╤А╨╡╤Д╨╕╨║╤Б╤Г
+const COMPANY_DIRECTORS: Record<string, number> = {
+  '╨в╨е':  1,    // ╨з╨░╤Й╨╕╨╜╨░ ╨Х╨╗╨╡╨╜╨░ тАФ ╨У╨╡╨╜. ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨Ю╨Ю╨Ю ╨в╨╡╤Е╨╜╨╛
+  '╨Э╨Я╨Я': 592,  // ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨Э╨Я╨Я
+  '╨б╨Я╨в': 6,    // ╨Т╨░╨╗╤О╨║ ╨Р╨╜╨┤╤А╨╡╨╣ тАФ ╨У╨Ф ╨б╨Я╨в/╨Ю╨б
+  '╨Ю╨б':  6,
+  '╨н-╨Ъ': 954,  // ╨┤╨╕╤А╨╡╨║╤В╨╛╤А ╨н-╨Ъ
+}
+
+export async function POST(request: NextRequest) {
   try {
     const body = await request.json()
-    const { id, file_url, contract_id, user_name, user_bitrix_id } = body
+    const {
+      checklist_item_id,   // id ╨┐╤Г╨╜╨║╤В╨░ ╤З╨╡╨║-╨╗╨╕╤Б╤В╨░ (╨╕╨╗╨╕ null ╨┐╤А╨╕ ╤Б╨╛╨╖╨┤╨░╨╜╨╕╨╕ ╨▓╤Б╨╡╤Е)
+      contract_id,
+      contract_number,
+      contract_title,
+      company_prefix,
+      item_title,
+      item_description,
+      due_date,
+      responsible_bitrix_id,  // ╨╕╤Б╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М (╨┐╨╛ ╤Г╨╝╨╛╨╗╤З╨░╨╜╨╕╤О = ╨░╨▓╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░)
+      author_bitrix_id,        // ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ тЖТ ╨╜╨░╨▒╨╗╤О╨┤╨░╤В╨╡╨╗╤М ╨▓ ╨╖╨░╨┤╨░╤З╨╡
+      // ╨Ф╨╗╤П ╤Б╨╛╨╖╨┤╨░╨╜╨╕╤П ╨▓╤Б╨╡╤Е ╨╖╨░╨┤╨░╤З ╤Б╤А╨░╨╖╤Г
+      items,  // ╨╝╨░╤Б╤Б╨╕╨▓ {id, title, description, due_date} ╨╡╤Б╨╗╨╕ bulk
+      mode,   // 'single_with_checklist' ╨┤╨╗╤П ╨╛╨┤╨╜╨╛╨╣ ╨╖╨░╨┤╨░╤З╨╕ ╤Б ╤З╨╡╨║-╨╗╨╕╤Б╤В╨╛╨╝
+      task_title, // ╨╜╨░╨╖╨▓╨░╨╜╨╕╨╡ ╨╖╨░╨┤╨░╤З╨╕ ╨┤╨╗╤П single_with_checklist
+    } = body
 
-    if (!id || !user_bitrix_id) {
-      return NextResponse.json({ error: '╨Э╨╡ ╨▓╤Б╨╡ ╨┐╨░╤А╨░╨╝╨╡╤В╤А╤Л ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л' }, { status: 400 })
+    if (!contract_id || !company_prefix) {
+      return NextResponse.json({ error: '╨Э╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╨┐╨╛╨╗╤П' }, { status: 400 })
     }
 
-    const userId = parseInt(user_bitrix_id)
+    const directorId = COMPANY_DIRECTORS[company_prefix] ?? 1
 
-    // ╨в╨╛╨╗╤М╨║╨╛ admin ╨╕ gc_manager ╨╝╨╛╨│╤Г╤В ╤Г╨┤╨░╨╗╤П╤В╤М
-    if (!ADMIN_IDS.includes(userId) && !GC_MANAGER_IDS.includes(userId)) {
-      return NextResponse.json({ error: '╨Э╨╡╤В ╨┐╤А╨░╨▓ ╨┤╨╗╤П ╤Г╨┤╨░╨╗╨╡╨╜╨╕╤П' }, { status: 403 })
-    }
+    // ╨д╤Г╨╜╨║╤Ж╨╕╤П ╤Б╨╛╨╖╨┤╨░╨╜╨╕╤П ╨╛╨┤╨╜╨╛╨╣ ╨╖╨░╨┤╨░╤З╨╕ ╨▓ ╨С╨╕╤В╤А╨╕╨║╤Б24
+    const createBitrixTask = async (
+      title: string,
+      description: string | null,
+      deadline: string | null,
+      responsibleId: number,
+      itemId: string
+    ): Promise<{ bitrix_task_id: string | null; error?: string }> => {
+      
+      // ╨д╨╛╤А╨╝╨╕╤А╤Г╨╡╨╝ ╨╛╨┐╨╕╤Б╨░╨╜╨╕╨╡ ╨╖╨░╨┤╨░╤З╨╕
+      const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
+      const contractLink = `https://${bitrixPortal}/contracts/${contract_id}`
+      const contractRef = `ЁЯУД ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: [URL=${contractLink}]${contract_number} тАФ ${contract_title}[/URL]`
+      const noDeadlineNote = !deadline 
+        ? '\n\nтЪая╕П ╨б╤А╨╛╨║ ╨╜╨╡ ╤Г╤Б╤В╨░╨╜╨╛╨▓╨╗╨╡╨╜. ╨Э╨╡╨╛╨▒╤Е╨╛╨┤╨╕╨╝╨╛ ╨╖╨░╨┤╨░╤В╤М ╤Б╤А╨╛╨║ ╨╕╤Б╨┐╨╛╨╗╨╜╨╡╨╜╨╕╤П!' 
+        : ''
+      const fullDescription = `${description || title}\n\n${contractRef}${noDeadlineNote}`
 
-    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
-    if (file_url) {
-      const filePath = file_url.split('/contracts/')[1]
-      if (filePath) {
-        await supabase.storage.from('contracts').remove([filePath])
+      const fields: Record<string, unknown> = {
+        TITLE: title,
+        DESCRIPTION: fullDescription,
+        DESCRIPTION_IN_BBCODE: 'Y',
+        RESPONSIBLE_ID: String(responsibleId),
+        CREATED_BY: String(directorId),  // ╨┐╨╛╤Б╤В╨░╨╜╨╛╨▓╤Й╨╕╨║ = ╨У╨Ф ╨║╨╛╨╝╨┐╨░╨╜╨╕╨╕
+        PRIORITY: '1',
+        ...(author_bitrix_id && author_bitrix_id !== responsibleId
+          ? { SE_PARAMETER: [{ CODE: 'WATCHERS', VALUE: String(author_bitrix_id) }] }
+          : {}),
       }
-    }
 
-    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╕╨╖ ╨С╨Ф
-    await supabase.from('signed_documents').delete().eq('id', id)
+      if (deadline) {
+        // ╨Ъ╨╛╨╜╨▓╨╡╤А╤В╨╕╤А╤Г╨╡╨╝ ╨┤╨░╤В╤Г ╨▓ ╤Д╨╛╤А╨╝╨░╤В ╨С╨╕╤В╤А╨╕╨║╤Б (ISO 8601)
+        fields.DEADLINE = new Date(deadline).toISOString()
+      }
 
-    // ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨╛╤Б╤В╨░╨╗╨╕╤Б╤М ╨╗╨╕ ╨╡╤Й╤С ╤Д╨░╨╣╨╗╤Л
-    const { count } = await supabase
-      .from('signed_documents')
-      .select('*', { count: 'exact', head: true })
-      .eq('contract_id', contract_id)
+      const url = `${WEBHOOK}tasks.task.add.json`
+      const resp = await fetch(url, {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        body: JSON.stringify({ fields }),
+      })
+
+      const data = await resp.json()
+
+      if (data.error) {
+        return { bitrix_task_id: null, error: data.error_description ?? data.error }
+      }
+
+      const taskId = String(data.result?.task?.id)
 
-    // ╨Х╤Б╨╗╨╕ ╤Д╨░╨╣╨╗╨╛╨▓ ╨╜╨╡ ╨╛╤Б╤В╨░╨╗╨╛╤Б╤М тАФ ╨▓╨╛╨╖╨▓╤А╨░╤Й╨░╨╡╨╝ ╤Б╤В╨░╤В╤Г╤Б ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜
-    if (count === 0) {
+      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ bitrix_task_id ╨▓ Supabase
       await supabase
-        .from('contracts')
-        .update({ status: '╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜' })
-        .eq('id', contract_id)
-    }
+        .from('contract_checklist')
+        .update({ bitrix_task_id: taskId })
+        .eq('id', itemId)
 
-    await supabase.from('contract_logs').insert({
-      contract_id,
-      action: '╨г╨┤╨░╨╗╤С╨╜ ╨┐╨╛╨┤╨┐╨╕╤Б╨░╨╜╨╜╤Л╨╣ ╤Н╨║╨╖╨╡╨╝╨┐╨╗╤П╤А',
-      details: '',
-      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
-    })
+      return { bitrix_task_id: taskId }
+    }
 
-    return NextResponse.json({ success: true })
-  } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500 })
-  }
-}
+    // === SINGLE WITH CHECKLIST: ╨╛╨┤╨╜╨░ ╨╖╨░╨┤╨░╤З╨░ ╤Б ╤З╨╡╨║-╨╗╨╕╤Б╤В╨╛╨╝ ===
+    if (mode === 'single_with_checklist' && items && Array.isArray(items)) {
+      const taskTitle = task_title || `╨Ъ╨╛╨╜╤В╤А╨╛╨╗╤М ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ тДЦ ${contract_number}`
 
-================
-File: app/api/upload-proxy/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
+      // ╨Ф╨╡╨┤╨╗╨░╨╣╨╜ = ╨╝╨░╨║╤Б╨╕╨╝╨░╨╗╤М╨╜╨░╤П ╨┤╨░╤В╨░ ╨╕╨╖ ╨┐╤Г╨╜╨║╤В╨╛╨▓
+      const dates = items
+        .map((i: { due_date?: string }) => i.due_date)
+        .filter(Boolean)
+        .map((d: string | undefined) => new Date(d!).getTime())
+      const maxDeadline = dates.length > 0 ? new Date(Math.max(...dates)).toISOString() : null
 
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
+      const contractRef = `ЁЯУД ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${contract_number} тАФ ${contract_title}`
+      const noDeadlineNote = !maxDeadline
+        ? '\n\nтЪая╕П ╨б╤А╨╛╨║ ╨╜╨╡ ╤Г╤Б╤В╨░╨╜╨╛╨▓╨╗╨╡╨╜. ╨Э╨╡╨╛╨▒╤Е╨╛╨┤╨╕╨╝╨╛ ╨╖╨░╨┤╨░╤В╤М ╤Б╤А╨╛╨║ ╨╕╤Б╨┐╨╛╨╗╨╜╨╡╨╜╨╕╤П!'
+        : ''
 
-export const maxDuration = 60
-export const dynamic = 'force-dynamic'
+      const fields: Record<string, unknown> = {
+        TITLE: taskTitle,
+        DESCRIPTION: `${contractRef}${noDeadlineNote}`,
+        DESCRIPTION_IN_BBCODE: 'Y',
+        RESPONSIBLE_ID: String(responsible_bitrix_id),
+        CREATED_BY: String(directorId),
+        PRIORITY: '1',
+      }
+      if (maxDeadline) fields.DEADLINE = maxDeadline
 
-// ╨Ю╤В╨║╨╗╤О╤З╨░╨╡╨╝ ╨▓╤Б╤В╤А╨╛╨╡╨╜╨╜╤Л╨╣ body parser Next.js ╨┤╨╗╤П ╨▒╨╛╨╗╤М╤И╨╕╤Е ╤Д╨░╨╣╨╗╨╛╨▓
-export const fetchCache = 'force-no-store'
+      // ╨б╨╛╨╖╨┤╨░╤С╨╝ ╨╖╨░╨┤╨░╤З╤Г
+      const taskResp = await fetch(`${WEBHOOK}tasks.task.add.json`, {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        body: JSON.stringify({ fields }),
+      })
+      const taskData = await taskResp.json()
 
-const CORS = { 'Access-Control-Allow-Origin': '*' }
+      if (taskData.error) {
+        return NextResponse.json(
+          { error: taskData.error_description ?? taskData.error },
+          { status: 400 }
+        )
+      }
 
-export async function OPTIONS() {
-  return new NextResponse(null, {
-    headers: { ...CORS, 'Access-Control-Allow-Methods': 'POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' }
-  })
-}
+      const taskId = String(taskData.result?.task?.id)
 
-export async function POST(request: NextRequest) {
-  try {
-    const formData = await request.formData()
-    const file = formData.get('file') as File | null
-    const bucket = formData.get('bucket') as string | null
-    const path = formData.get('path') as string | null
+      // ╨Ф╨╛╨▒╨░╨▓╨╗╤П╨╡╨╝ ╨┐╤Г╨╜╨║╤В╤Л ╨║╨░╨║ ╤З╨╡╨║-╨╗╨╕╤Б╤В ╨╖╨░╨┤╨░╤З╨╕
+      for (const item of items) {
+        const checkResp = await fetch(`${WEBHOOK}task.checklistitem.add.json`, {
+          method: 'POST',
+          headers: { 'Content-Type': 'application/json' },
+          body: JSON.stringify({
+            TASKID: taskId,
+            fields: { TITLE: item.title },
+          }),
+        })
+        const checkData = await checkResp.json()
+        console.log(`checklistitem.add taskId=${taskId} title="${item.title}":`, JSON.stringify(checkData))
+        await new Promise(r => setTimeout(r, 200))
+      }
 
-    if (!file || !bucket || !path) {
-      return NextResponse.json({ error: 'file, bucket ╨╕ path ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400, headers: CORS })
+      return NextResponse.json({ success: true, bitrix_task_id: taskId })
     }
 
-    const allowedBuckets = ['counterparty-docs', 'contracts']
-    if (!allowedBuckets.includes(bucket)) {
-      return NextResponse.json({ error: '╨Э╨╡╨┤╨╛╨┐╤Г╤Б╤В╨╕╨╝╤Л╨╣ bucket' }, { status: 403, headers: CORS })
+    // === BULK: ╤Б╨╛╨╖╨┤╨░╤В╤М ╨▓╤Б╨╡ ╨╖╨░╨┤╨░╤З╨╕ ===
+    if (items && Array.isArray(items)) {
+      const results = []
+      for (const item of items) {
+        // ╨Я╤А╨╛╨┐╤Г╤Б╨║╨░╨╡╨╝ ╤Г╨╢╨╡ ╤Б╨╛╨╖╨┤╨░╨╜╨╜╤Л╨╡
+        if (item.bitrix_task_id) {
+          results.push({ id: item.id, skipped: true, bitrix_task_id: item.bitrix_task_id })
+          continue
+        }
+        const result = await createBitrixTask(
+          item.title,
+          item.description,
+          item.due_date,
+          responsible_bitrix_id,
+          item.id
+        )
+        results.push({ id: item.id, ...result })
+        // ╨Я╨░╤Г╨╖╨░ ╨╝╨╡╨╢╨┤╤Г ╨╖╨░╨┐╤А╨╛╤Б╨░╨╝╨╕ ╤З╤В╨╛╨▒╤Л ╨╜╨╡ ╨┐╤А╨╡╨▓╤Л╤Б╨╕╤В╤М ╨╗╨╕╨╝╨╕╤В ╨С╨╕╤В╤А╨╕╨║╤Б
+        await new Promise(r => setTimeout(r, 300))
+      }
+      return NextResponse.json({ success: true, results })
     }
 
-    const arrayBuffer = await file.arrayBuffer()
-    const buffer = new Uint8Array(arrayBuffer)
+    // === SINGLE: ╤Б╨╛╨╖╨┤╨░╤В╤М ╨╛╨┤╨╜╤Г ╨╖╨░╨┤╨░╤З╤Г ===
+    if (!checklist_item_id || !item_title) {
+      return NextResponse.json({ error: '╨Э╨╡ ╨┐╨╡╤А╨╡╨┤╨░╨╜╤Л item ╨┤╨░╨╜╨╜╤Л╨╡' }, { status: 400 })
+    }
 
-    const { error: uploadError } = await supabase.storage
-      .from(bucket)
-      .upload(path, buffer, {
-        contentType: file.type || 'application/octet-stream',
-        upsert: true,
-      })
+    const result = await createBitrixTask(
+      item_title,
+      item_description,
+      due_date,
+      responsible_bitrix_id,
+      checklist_item_id
+    )
 
-    if (uploadError) {
-      return NextResponse.json({ error: uploadError.message }, { status: 500, headers: CORS })
+    if (result.error) {
+      return NextResponse.json({ error: result.error }, { status: 400 })
     }
 
-    const { data: publicData } = supabase.storage
-      .from(bucket)
-      .getPublicUrl(path)
-
-    return NextResponse.json({
-      success: true,
-      public_url: publicData.publicUrl,
-    }, { headers: CORS })
+    return NextResponse.json({ success: true, bitrix_task_id: result.bitrix_task_id })
 
   } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500, headers: CORS })
+    console.error('bitrix-tasks error:', err)
+    return NextResponse.json({ error: '╨Т╨╜╤Г╤В╤А╨╡╨╜╨╜╤П╤П ╨╛╤И╨╕╨▒╨║╨░ ╤Б╨╡╤А╨▓╨╡╤А╨░' }, { status: 500 })
   }
 }
 
 ================
-File: app/api/versions/route.ts
+File: app/api/cron/approval-reminder/route.ts
 ================
 import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
+import { NextResponse } from 'next/server'
+import { sendBitrixNotify, sendBitrixChatMessage } from '@/app/lib/notify'
 
-export const maxDuration = 60
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
 
-export async function GET(request: NextRequest) {
-  const supabase = createClient(
-    process.env.NEXT_PUBLIC_SUPABASE_URL!,
-    process.env.SUPABASE_SECRET_KEY!
-  )
-  const contractId = request.nextUrl.searchParams.get('contract_id')
-  if (!contractId) {
-    return NextResponse.json({ error: 'contract_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
-  }
-  const { data: versions, error } = await supabase
-    .from('versions')
-    .select('*')
-    .eq('contract_id', contractId)
-    .order('version_number', { ascending: false })
+export async function GET() {
+  try {
+    const now = new Date()
+    const todayStr = now.toISOString().slice(0, 10)
 
-  if (error) {
-    return NextResponse.json({ error: error.message }, { status: 400 })
-  }
-  return NextResponse.json({ versions: versions ?? [] })
-}
+    // ╨Ч╨░╨▓╤В╤А╨░
+    const tomorrow = new Date(now)
+    tomorrow.setDate(tomorrow.getDate() + 1)
+    const tomorrowStr = tomorrow.toISOString().slice(0, 10)
 
-export async function POST(request: NextRequest) {
-  console.log('=== versions API called ===')
+    // 3 ╨┤╨╜╤П ╨╜╨░╨╖╨░╨┤
+    const threeDaysAgo = new Date(now)
+    threeDaysAgo.setDate(threeDaysAgo.getDate() - 3)
+    const threeDaysAgoStr = threeDaysAgo.toISOString().slice(0, 10)
 
-  const supabase = createClient(
-    process.env.NEXT_PUBLIC_SUPABASE_URL!,
-    process.env.SUPABASE_SECRET_KEY!
-  )
+    // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╨░╨║╤В╨╕╨▓╨╜╤Л╨╡ ╤Б╨╡╤Б╤Б╨╕╨╕ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
+    const { data: sessions, error } = await supabase
+      .from('approval_sessions')
+      .select(`
+        id,
+        contract_id,
+        deadline,
+        initiated_by_bitrix_id,
+        bitrix_chat_id,
+        contracts (
+          id, number, title, author_bitrix_id
+        ),
+        approval_participants (
+          bitrix_user_id,
+          status
+        )
+      `)
+      .eq('status', 'active')
+      .not('deadline', 'is', null)
 
-  try {
-    const contentType = request.headers.get('content-type') ?? ''
-    let contractId: string
-    let comment: string
-    let userName: string
-    let fileUrl: string
-    let fileName: string
+    if (error) return NextResponse.json({ error: error.message }, { status: 500 })
+    if (!sessions || sessions.length === 0) {
+      return NextResponse.json({ success: true, notified: 0, message: '╨Э╨╡╤В ╨░╨║╤В╨╕╨▓╨╜╤Л╤Е ╤Б╨╡╤Б╤Б╨╕╨╣' })
+    }
 
-    if (contentType.includes('application/json')) {
-      // ╨д╨░╨╣╨╗ ╤Г╨╢╨╡ ╨╖╨░╨│╤А╤Г╨╢╨╡╨╜ ╤З╨╡╤А╨╡╨╖ uploadFileDirect тАФ ╨┐╨╛╨╗╤Г╤З╨░╨╡╨╝ ╤В╨╛╨╗╤М╨║╨╛ ╨╝╨╡╤В╨░╨┤╨░╨╜╨╜╤Л╨╡
-      const json = await request.json()
-      contractId = json.contract_id
-      comment = json.comment ?? ''
-      userName = json.user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░'
-      fileUrl = json.file_url
-      fileName = json.file_name
+    const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
+    let notified = 0
+
+    for (const session of sessions) {
+      const contract = Array.isArray(session.contracts)
+        ? session.contracts[0]
+        : session.contracts
+      if (!contract) continue
 
-      if (!contractId || !fileUrl || !fileName) {
-        return NextResponse.json({ error: 'contract_id, file_url ╨╕ file_name ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400 })
-      }
-    } else {
-      // ╨б╤В╨░╤А╤Л╨╣ ╨┐╤Г╤В╤М ╤З╨╡╤А╨╡╨╖ FormData (╤Д╨░╨╣╨╗╤Л ╨┤╨╛ 4.5 ╨Ь╨С)
-      const formData = await request.formData()
-      const file = formData.get('file') as File
-      contractId = formData.get('contract_id') as string
-      comment = formData.get('comment') as string ?? ''
-      userName = formData.get('user_name') as string ?? '╨б╨╕╤Б╤В╨╡╨╝╨░'
+      const deadlineStr = session.deadline.slice(0, 10)
+      const link = `https://${bitrixPortal}/contracts/${contract.id}`
+      const docRef = `[URL=${link}]${contract.number} тАФ ${contract.title}[/URL]`
 
-      if (!file || !contractId) {
-        return NextResponse.json({ error: '╨д╨░╨╣╨╗ ╨╕ ID ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╤М╨╜╤Л' }, { status: 400 })
-      }
+      // ╨г╤З╨░╤Б╤В╨╜╨╕╨║╨╕, ╨║╨╛╤В╨╛╤А╤Л╨╡ ╨╡╤Й╤С ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗╨╕
+      const pendingParticipants = (session.approval_participants ?? [])
+        .filter((p: { status: string; bitrix_user_id: number | null }) =>
+          p.status === 'pending' && p.bitrix_user_id
+        )
+        .map((p: { bitrix_user_id: number }) => p.bitrix_user_id)
 
-      const { count } = await supabase
-        .from('versions')
-        .select('*', { count: 'exact', head: true })
-        .eq('contract_id', contractId)
+      // ╨Ш╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А + ╨░╨▓╤В╨╛╤А ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░
+      const initiators = [...new Set([
+        session.initiated_by_bitrix_id,
+        contract.author_bitrix_id,
+      ].filter(Boolean) as number[])]
 
-      const versionNumber = (count ?? 0) + 1
-      const fileExt = file.name.split('.').pop()
-      const filePath = `${contractId}/v${versionNumber}_${Date.now()}.${fileExt}`
-      const arrayBuffer = await file.arrayBuffer()
-      const fileBuffer = new Uint8Array(arrayBuffer)
+      // ╨Я╨╛╨╗╤Г╤З╨░╤В╨╡╨╗╨╕ ╨║╨╛╨╗╨╛╨║╨╛╨╗╤М╤З╨╕╨║╨░ = ╤В╨╡ ╨║╤В╨╛ ╨╜╨╡ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╗ + ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А
+      const notifyRecipients = [...new Set([...pendingParticipants, ...initiators])]
 
-      const { error: uploadError } = await supabase.storage
-        .from('contracts')
-        .upload(filePath, fileBuffer, { contentType: file.type, upsert: false })
+      // ╨б╨┐╨╕╤Б╨╛╨║ ╨╛╨╢╨╕╨┤╨░╤О╤Й╨╕╤Е ╨┤╨╗╤П ╤В╨╡╨║╤Б╤В╨░ ╤Б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╤П
+      const pendingCount = pendingParticipants.length
+      const pendingNote = pendingCount > 0
+        ? `╨Ю╨╢╨╕╨┤╨░╤О╤В ╤А╨╡╤И╨╡╨╜╨╕╤П: ${pendingCount} ╤Г╤З╨░╤Б╤В╨╜╨╕╨║(╨╛╨▓).`
+        : ''
 
-      if (uploadError) {
-        return NextResponse.json({ error: uploadError.message }, { status: 400 })
+      let notifyType: 'approval_deadline_soon' | 'approval_deadline_reached' | 'approval_deadline_overdue' | null = null
+      let chatMessage = ''
+
+      if (deadlineStr === tomorrowStr) {
+        // ╨Ч╨░ 1 ╨┤╨╡╨╜╤М ╨┤╨╛ ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
+        notifyType = 'approval_deadline_soon'
+        chatMessage = `тЪая╕П ╨б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╕╤Б╤В╨╡╨║╨░╨╡╤В ╨╖╨░╨▓╤В╤А╨░!\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
+      } else if (deadlineStr === todayStr) {
+        // ╨Т ╨┤╨╡╨╜╤М ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
+        notifyType = 'approval_deadline_reached'
+        chatMessage = `ЁЯФ┤ ╨б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╨╕╤Б╤В╤С╨║ ╤Б╨╡╨│╨╛╨┤╨╜╤П!\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
+      } else if (deadlineStr === threeDaysAgoStr) {
+        // ╨з╨╡╤А╨╡╨╖ 3 ╨┤╨╜╤П ╨┐╨╛╤Б╨╗╨╡ ╨┤╨╡╨┤╨╗╨░╨╣╨╜╨░
+        notifyType = 'approval_deadline_overdue'
+        chatMessage = `ЁЯЪи ╨б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╨╡ ╨┐╤А╨╛╤Б╤А╨╛╤З╨╡╨╜╨╛ ╨╜╨░ 3 ╨┤╨╜╤П. ╨а╨╡╨║╨╛╨╝╨╡╨╜╨┤╤Г╨╡╨╝ ╨┐╤А╨╛╨┤╨╗╨╕╤В╤М ╤Б╤А╨╛╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П ╤Б ╤Г╨║╨░╨╖╨░╨╜╨╕╨╡╨╝ ╨┐╤А╨╕╤З╨╕╨╜╤Л.\n╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В: ${docRef}\n${pendingNote}`
       }
 
-      const { data: urlData } = supabase.storage.from('contracts').getPublicUrl(filePath)
-      fileUrl = urlData.publicUrl
-      fileName = file.name
-    }
+      if (!notifyType) continue
 
-    // ╨Я╨╛╨╗╤Г╤З╨░╨╡╨╝ ╨╜╨╛╨╝╨╡╤А ╨▓╨╡╤А╤Б╨╕╨╕
-    const { count: vCount } = await supabase
-      .from('versions')
-      .select('*', { count: 'exact', head: true })
-      .eq('contract_id', contractId)
-    const versionNumber = (vCount ?? 0) + 1
+      // 1. ╨Ъ╨╛╨╗╨╛╨║╨╛╨╗╤М╤З╨╕╨║ (pending + ╨╕╨╜╨╕╤Ж╨╕╨░╤В╨╛╤А)
+      if (notifyRecipients.length > 0) {
+        await sendBitrixNotify({
+          recipients: notifyRecipients,
+          type: notifyType,
+          document_id: contract.id,
+          document_title: contract.title ?? '',
+          document_number: contract.number ?? '',
+          extra: pendingNote,
+        })
+      }
 
-    const { error: versionError } = await supabase
-      .from('versions')
-      .insert({
-        contract_id: contractId,
-        version_number: versionNumber,
-        file_url: fileUrl,
-        file_name: fileName,
-        comment: comment || null,
-      })
+      // 2. ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╤З╨░╤В ╨С24
+      if (session.bitrix_chat_id) {
+        await sendBitrixChatMessage(session.bitrix_chat_id, chatMessage)
+      }
 
-    console.log('version insert:', versionError ? versionError.message : 'success')
+      // 3. ╨б╨╛╨╛╨▒╤Й╨╡╨╜╨╕╨╡ ╨▓ ╨╗╨╡╨╜╤В╤Г ╨╜╨╛╨▓╨╛╤Б╤В╨╡╨╣ ╨С24 (im.message.add ╨▓╤Б╨╡╨╝ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║╨░╨╝)
+      const webhookUrl = process.env.BITRIX_WEBHOOK_URL
+      if (webhookUrl && notifyRecipients.length > 0) {
+        await Promise.all(notifyRecipients.map(async (userId) => {
+          try {
+            await fetch(`${webhookUrl}im.message.add.json`, {
+              method: 'POST',
+              headers: { 'Content-Type': 'application/json' },
+              body: JSON.stringify({
+                DIALOG_ID: userId,
+                MESSAGE: chatMessage,
+              }),
+            })
+          } catch (err) {
+            console.error(`approval-reminder message error for user ${userId}:`, err)
+          }
+        }))
+      }
 
-    if (versionError) {
-      return NextResponse.json({ error: versionError.message }, { status: 400 })
+      notified++
     }
 
-    await supabase
-      .from('contract_logs')
-      .insert({
-        contract_id: contractId,
-        action: `╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜╨░ ╨▓╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ v${versionNumber}`,
-        details: `╨д╨░╨╣╨╗: ${fileName}${comment ? '. ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣: ' + comment : ''}`,
-        user_name: '╨б╨╕╤Б╤В╨╡╨╝╨░',
-      })
-
-    console.log('=== versions API success ===')
-    return NextResponse.json({ success: true, version: versionNumber })
+    return NextResponse.json({ success: true, notified, sessions_found: sessions.length })
 
   } catch (err) {
     const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    console.error('versions API error:', message)
     return NextResponse.json({ error: message }, { status: 500 })
   }
 }
 
-export async function DELETE(request: NextRequest) {
-  const supabase = createClient(
-    process.env.NEXT_PUBLIC_SUPABASE_URL!,
-    process.env.SUPABASE_SECRET_KEY!
-  )
+================
+File: app/api/my-documents/route.ts
+================
+import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
 
-  try {
-    const body = await request.json()
-    const { version_id, file_path, contract_id, user_name } = body
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
 
-    if (!version_id) {
-      return NextResponse.json({ error: 'version_id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
-    }
+export async function GET(request: NextRequest) {
+  const bitrixUserId = request.nextUrl.searchParams.get('bitrix_user_id')
 
-    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
-    if (file_path) {
-      await supabase.storage
-        .from('contracts')
-        .remove([file_path])
-    }
+  if (!bitrixUserId) {
+    return NextResponse.json({ error: 'bitrix_user_id ╨а╤Х╨а┬▒╨б╨П╨а┬╖╨а┬░╨бтАЪ╨а┬╡╨а┬╗╨а┬╡╨а╨Е' }, { status: 400 })
+  }
 
-    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╕╨╖ ╨▒╨░╨╖╤Л
-    const { error } = await supabase
-      .from('versions')
-      .delete()
-      .eq('id', version_id)
+  const userId = parseInt(bitrixUserId)
 
-    if (error) {
-      return NextResponse.json({ error: error.message }, { status: 400 })
-    }
+  try {
+    // 1. ╨атАЭ╨а╤Х╨а╤Ф╨б╤У╨а╤Ш╨а┬╡╨а╨Е╨бтАЪ╨бтА╣ ╨а╤Ц╨а╥С╨а┬╡ ╨б╨П ╨б╨Г╨а╤Х╨а╤Ц╨а┬╗╨а┬░╨б╨Г╨б╤У╨б╨Л╨бтА░╨а╤С╨атДЦ (╨а╤Х╨а┬▒╨б╨П╨а┬╖╨а┬░╨бтАЪ╨а┬╡╨а┬╗╨б╨К╨а╨Е╨бтА╣╨атДЦ) ╨б╨Г╨а╤Х ╨б╨Г╨бтАЪ╨а┬░╨бтАЪ╨б╤У╨б╨Г╨а╤Х╨а╤Ш pending
+    const { data: myApprovals } = await supabase
+      .from('approval_participants')
+      .select(`
+        id,
+        role,
+        status,
+        stage,
+        session_id,
+        approval_sessions!inner (
+          id,
+          contract_id,
+          deadline,
+          initiated_by_name,
+          contracts!inner (
+            id,
+            number,
+            title,
+            counterparty,
+            status,
+            amount,
+            deleted_at,
+            document_category
+          )
+        )
+      `)
+      .eq('bitrix_user_id', userId)
+      .eq('status', 'pending')
 
-    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
-    await supabase
-      .from('contract_logs')
-      .insert({
-        contract_id,
-        action: '╨Т╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░ ╤Г╨┤╨░╨╗╨╡╨╜╨░',
-        details: `╨г╨┤╨░╨╗╨╡╨╜╨░ ╨▓╨╡╤А╤Б╨╕╤П ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░`,
-        user_name: user_name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
-      })
+    // 2. ╨а╤Ъ╨а╤Х╨а╤С ╨бтАб╨а┬╡╨б╨В╨а╨Е╨а╤Х╨а╨Ж╨а╤С╨а╤Ф╨а╤С
+    const { data: myDrafts } = await supabase
+      .from('contracts')
+      .select('*')
+      .eq('author_bitrix_id', userId)
+      .eq('status', '╤З╨╡╤А╨╜╨╛╨▓╨╕╨║')
+      .in('document_category', ['contract', 'document'])
+      .is('deleted_at', null)
+      .order('created_at', { ascending: false })
+
+    // 3. ╨атАЭ╨а╤Х╨а╤Ф╨б╤У╨а╤Ш╨а┬╡╨а╨Е╨бтАЪ╨бтА╣ ╨а╤Ц╨а╥С╨а┬╡ ╨б╨П ╨а╤С╨а╨Е╨а╤С╨бтАа╨а╤С╨а┬░╨бтАЪ╨а╤Х╨б╨В ╨б╨Г╨а╤Х╨а╤Ц╨а┬╗╨а┬░╨б╨Г╨а╤Х╨а╨Ж╨а┬░╨а╨Е╨а╤С╨б╨П
+    const { data: myInitiated } = await supabase
+      .from('approval_sessions')
+      .select(`
+        id,
+        deadline,
+        status,
+        created_at,
+        contracts!inner (
+          id,
+          number,
+          title,
+          counterparty,
+          status,
+          amount,
+          deleted_at
+        )
+      `)
+      .eq('initiated_by_bitrix_id', userId)
+      .eq('status', 'active')
+      .filter('contracts.deleted_at', 'is', null)
+      .order('created_at', { ascending: false })
+// ╨д╨╕╨╗╤М╤В╤А╤Г╨╡╨╝ тАФ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╤Б╨╜╨╛╨▓╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л (╨╜╨╡ ╨▓╨╗╨╛╨╢╨╡╨╜╨╕╤П) ╨╕ ╨╜╨╡ ╤Г╨┤╨░╨╗╤С╨╜╨╜╤Л╨╡
+    const filteredApprovals = (myApprovals ?? []).filter(p => {
+      const sessions = p.approval_sessions as unknown as { contracts: { document_category?: string, deleted_at?: string | null } }
+      const category = sessions?.contracts?.document_category
+      const deletedAt = sessions?.contracts?.deleted_at
+      return category !== 'attachment' && !deletedAt
+    })
+    const requiredApprovals = filteredApprovals.filter(p => p.role === 'required')
+    const optionalApprovals = filteredApprovals.filter(p => p.role === 'optional')
+    // ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╜╨░ ╨┐╨╛╨┤╨┐╨╕╤Б╨╕ ╨▓ ╨н╨Ф╨Ю ╨│╨┤╨╡ ╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М ╨░╨▓╤В╨╛╤А ╨╕╨╗╨╕ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
+    const { data: myEdo } = await supabase
+      .from('contracts')
+      .select('id, number, title, counterparty, status, amount, company_prefix')
+      .eq('status', '╨╜╨░_╨┐╨╛╨┤╨┐╨╕╤Б╨╕_╨▓_╤Н╨┤╨╛')
+      .is('deleted_at', null)
+      .or(`author_bitrix_id.eq.${userId}`)
+      .order('updated_at', { ascending: false })
 
-    return NextResponse.json({ success: true })
+    // ╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╨░╤П ╤Д╨╕╨╗╤М╤В╤А╨░╤Ж╨╕╤П ╤Г╨┤╨░╨╗╤С╨╜╨╜╤Л╤Е ╨╜╨░ ╤Г╤А╨╛╨▓╨╜╨╡ JS
+    const filteredInitiated = (myInitiated ?? []).filter(s => {
+      const contract = s.contracts as unknown as { deleted_at: string | null }
+      return !contract?.deleted_at
+    })
+
+    return NextResponse.json({
+      required_approvals: requiredApprovals,
+      optional_approvals: optionalApprovals,
+      my_drafts: myDrafts ?? [],
+      my_initiated: filteredInitiated,
+      my_edo: myEdo ?? [],
+    })
   } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    const message = err instanceof Error ? err.message : '╨а╤Ь╨а┬╡╨а╤С╨а┬╖╨а╨Ж╨а┬╡╨б╨Г╨бтАЪ╨а╨Е╨а┬░╨б╨П ╨а╤Х╨бтВм╨а╤С╨а┬▒╨а╤Ф╨а┬░'
     return NextResponse.json({ error: message }, { status: 500 })
   }
 }
@@ -8076,125 +8195,6 @@ export async function POST(
   }
 }
 
-================
-File: app/api/my-documents/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
-
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
-
-export async function GET(request: NextRequest) {
-  const bitrixUserId = request.nextUrl.searchParams.get('bitrix_user_id')
-
-  if (!bitrixUserId) {
-    return NextResponse.json({ error: 'bitrix_user_id ╨а╤Х╨а┬▒╨б╨П╨а┬╖╨а┬░╨бтАЪ╨а┬╡╨а┬╗╨а┬╡╨а╨Е' }, { status: 400 })
-  }
-
-  const userId = parseInt(bitrixUserId)
-
-  try {
-    // 1. ╨атАЭ╨а╤Х╨а╤Ф╨б╤У╨а╤Ш╨а┬╡╨а╨Е╨бтАЪ╨бтА╣ ╨а╤Ц╨а╥С╨а┬╡ ╨б╨П ╨б╨Г╨а╤Х╨а╤Ц╨а┬╗╨а┬░╨б╨Г╨б╤У╨б╨Л╨бтА░╨а╤С╨атДЦ (╨а╤Х╨а┬▒╨б╨П╨а┬╖╨а┬░╨бтАЪ╨а┬╡╨а┬╗╨б╨К╨а╨Е╨бтА╣╨атДЦ) ╨б╨Г╨а╤Х ╨б╨Г╨бтАЪ╨а┬░╨бтАЪ╨б╤У╨б╨Г╨а╤Х╨а╤Ш pending
-    const { data: myApprovals } = await supabase
-      .from('approval_participants')
-      .select(`
-        id,
-        role,
-        status,
-        stage,
-        session_id,
-        approval_sessions!inner (
-          id,
-          contract_id,
-          deadline,
-          initiated_by_name,
-          contracts!inner (
-            id,
-            number,
-            title,
-            counterparty,
-            status,
-            amount,
-            deleted_at,
-            document_category
-          )
-        )
-      `)
-      .eq('bitrix_user_id', userId)
-      .eq('status', 'pending')
-
-    // 2. ╨а╤Ъ╨а╤Х╨а╤С ╨бтАб╨а┬╡╨б╨В╨а╨Е╨а╤Х╨а╨Ж╨а╤С╨а╤Ф╨а╤С
-    const { data: myDrafts } = await supabase
-      .from('contracts')
-      .select('*')
-      .eq('author_bitrix_id', userId)
-      .eq('status', '╤З╨╡╤А╨╜╨╛╨▓╨╕╨║')
-      .in('document_category', ['contract', 'document'])
-      .is('deleted_at', null)
-      .order('created_at', { ascending: false })
-
-    // 3. ╨атАЭ╨а╤Х╨а╤Ф╨б╤У╨а╤Ш╨а┬╡╨а╨Е╨бтАЪ╨бтА╣ ╨а╤Ц╨а╥С╨а┬╡ ╨б╨П ╨а╤С╨а╨Е╨а╤С╨бтАа╨а╤С╨а┬░╨бтАЪ╨а╤Х╨б╨В ╨б╨Г╨а╤Х╨а╤Ц╨а┬╗╨а┬░╨б╨Г╨а╤Х╨а╨Ж╨а┬░╨а╨Е╨а╤С╨б╨П
-    const { data: myInitiated } = await supabase
-      .from('approval_sessions')
-      .select(`
-        id,
-        deadline,
-        status,
-        created_at,
-        contracts!inner (
-          id,
-          number,
-          title,
-          counterparty,
-          status,
-          amount,
-          deleted_at
-        )
-      `)
-      .eq('initiated_by_bitrix_id', userId)
-      .eq('status', 'active')
-      .filter('contracts.deleted_at', 'is', null)
-      .order('created_at', { ascending: false })
-// ╨д╨╕╨╗╤М╤В╤А╤Г╨╡╨╝ тАФ ╤В╨╛╨╗╤М╨║╨╛ ╨╛╤Б╨╜╨╛╨▓╨╜╤Л╨╡ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л (╨╜╨╡ ╨▓╨╗╨╛╨╢╨╡╨╜╨╕╤П) ╨╕ ╨╜╨╡ ╤Г╨┤╨░╨╗╤С╨╜╨╜╤Л╨╡
-    const filteredApprovals = (myApprovals ?? []).filter(p => {
-      const sessions = p.approval_sessions as unknown as { contracts: { document_category?: string, deleted_at?: string | null } }
-      const category = sessions?.contracts?.document_category
-      const deletedAt = sessions?.contracts?.deleted_at
-      return category !== 'attachment' && !deletedAt
-    })
-    const requiredApprovals = filteredApprovals.filter(p => p.role === 'required')
-    const optionalApprovals = filteredApprovals.filter(p => p.role === 'optional')
-    // ╨Ф╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨╜╨░ ╨┐╨╛╨┤╨┐╨╕╤Б╨╕ ╨▓ ╨н╨Ф╨Ю ╨│╨┤╨╡ ╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М ╨░╨▓╤В╨╛╤А ╨╕╨╗╨╕ ╤Г╤З╨░╤Б╤В╨╜╨╕╨║ ╤Б╨╛╨│╨╗╨░╤Б╨╛╨▓╨░╨╜╨╕╤П
-    const { data: myEdo } = await supabase
-      .from('contracts')
-      .select('id, number, title, counterparty, status, amount, company_prefix')
-      .eq('status', '╨╜╨░_╨┐╨╛╨┤╨┐╨╕╤Б╨╕_╨▓_╤Н╨┤╨╛')
-      .is('deleted_at', null)
-      .or(`author_bitrix_id.eq.${userId}`)
-      .order('updated_at', { ascending: false })
-
-    // ╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╨░╤П ╤Д╨╕╨╗╤М╤В╤А╨░╤Ж╨╕╤П ╤Г╨┤╨░╨╗╤С╨╜╨╜╤Л╤Е ╨╜╨░ ╤Г╤А╨╛╨▓╨╜╨╡ JS
-    const filteredInitiated = (myInitiated ?? []).filter(s => {
-      const contract = s.contracts as unknown as { deleted_at: string | null }
-      return !contract?.deleted_at
-    })
-
-    return NextResponse.json({
-      required_approvals: requiredApprovals,
-      optional_approvals: optionalApprovals,
-      my_drafts: myDrafts ?? [],
-      my_initiated: filteredInitiated,
-      my_edo: myEdo ?? [],
-    })
-  } catch (err) {
-    const message = err instanceof Error ? err.message : '╨а╤Ь╨а┬╡╨а╤С╨а┬╖╨а╨Ж╨а┬╡╨б╨Г╨бтАЪ╨а╨Е╨а┬░╨б╨П ╨а╤Х╨бтВм╨а╤С╨а┬▒╨а╤Ф╨а┬░'
-    return NextResponse.json({ error: message }, { status: 500 })
-  }
-}
-
 ================
 File: app/api/attachments/route.ts
 ================
@@ -8323,62 +8323,214 @@ export async function POST(request: NextRequest) {
         uploaded_by_bitrix_id: user_bitrix_id ? parseInt(user_bitrix_id) : null,
       })
 
-    if (dbError) {
-      return NextResponse.json({ error: dbError.message }, { status: 400 })
+    if (dbError) {
+      return NextResponse.json({ error: dbError.message }, { status: 400 })
+    }
+
+    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
+    await supabase.from('contract_logs').insert({
+      contract_id,
+      action: `╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜╨╛: ${attachment_type} тДЦ${nextNumber}`,
+      details: `╨д╨░╨╣╨╗: ${file.name}${comment ? '. ' + comment : ''}`,
+      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+    })
+
+    return NextResponse.json({ success: true })
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500 })
+  }
+}
+
+export async function DELETE(request: NextRequest) {
+  try {
+    const body = await request.json()
+    const { id, file_url, contract_id, user_name } = body
+
+    if (!id) {
+      return NextResponse.json({ error: 'id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+    }
+
+    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
+    if (file_url) {
+      const filePath = file_url.split('/contracts/')[1]
+      if (filePath) {
+        await supabase.storage.from('contracts').remove([filePath])
+      }
+    }
+
+    const { error } = await supabase
+      .from('document_attachments')
+      .delete()
+      .eq('id', id)
+
+    if (error) {
+      return NextResponse.json({ error: error.message }, { status: 400 })
+    }
+
+    await supabase.from('contract_logs').insert({
+      contract_id,
+      action: '╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╤Л╨╣ ╨╝╨░╤В╨╡╤А╨╕╨░╨╗ ╤Г╨┤╨░╨╗╤С╨╜',
+      details: `╨д╨░╨╣╨╗ ╤Г╨┤╨░╨╗╤С╨╜`,
+      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
+    })
+
+    return NextResponse.json({ success: true })
+  } catch (err) {
+    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+    return NextResponse.json({ error: message }, { status: 500 })
+  }
+}
+
+================
+File: app/api/counterparties/route.ts
+================
+import { createClient } from '@supabase/supabase-js'
+import { NextRequest, NextResponse } from 'next/server'
+
+const supabase = createClient(
+  process.env.NEXT_PUBLIC_SUPABASE_URL!,
+  process.env.SUPABASE_SECRET_KEY!
+)
+
+// GET тАФ ╤Б╨┐╨╕╤Б╨╛╨║ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓ ╨╕╨╗╨╕ ╨╛╨┤╨╕╨╜ ╨┐╨╛ id
+export async function GET(request: NextRequest) {
+  try {
+    const id = request.nextUrl.searchParams.get('id')
+    const inn = request.nextUrl.searchParams.get('inn')
+    const search = request.nextUrl.searchParams.get('search')
+
+    if (id) {
+      const { data, error } = await supabase
+        .from('counterparties')
+        .select('*, contracts(id, number, title, status, created_at)')
+        .eq('id', id)
+        .single()
+
+      // ╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╨╛ ╨╖╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨┐╨╛ ╤В╨╡╨║╤Б╤В╨╛╨▓╨╛╨╝╤Г ╤Б╨╛╨▓╨┐╨░╨┤╨╡╨╜╨╕╤О ╨╡╤Б╨╗╨╕ counterparty_id ╨╜╨╡ ╨╖╨░╨┐╨╛╨╗╨╜╨╡╨╜
+      if (data && data.contracts?.length === 0) {
+        const searchName = data.short_name ?? data.full_name
+        const { data: extraContracts } = await supabase
+          .from('contracts')
+          .select('id, number, title, status, created_at')
+          .ilike('counterparty', `%${searchName}%`)
+          .is('deleted_at', null)
+          .order('created_at', { ascending: false })
+        if (extraContracts?.length) {
+          data.contracts = extraContracts
+        }
+      }
+      if (error) return NextResponse.json({ error: error.message }, { status: 400 })
+      return NextResponse.json({ counterparty: data })
+    }
+
+    if (inn) {
+      const kpp = request.nextUrl.searchParams.get('kpp')
+      let query = supabase
+        .from('counterparties')
+        .select('*')
+        .eq('inn', inn)
+      if (kpp) query = query.eq('kpp', kpp)
+      const { data, error } = await query.single()
+      if (error && error.code !== 'PGRST116') return NextResponse.json({ error: error.message }, { status: 400 })
+      return NextResponse.json({ counterparty: data ?? null })
+    }
+
+    const foreignOnly = request.nextUrl.searchParams.get('foreign_only')
+    const russianOnly = request.nextUrl.searchParams.get('russian_only')
+
+    let query = supabase
+      .from('counterparties')
+      .select('id, inn, kpp, ogrn, short_name, full_name, status, risk_level, director_name, director_title, phone, email, legal_address, signatory_name, poa_number, poa_date, created_at, is_foreign, country, registration_number, is_individual')
+      .order('full_name', { ascending: true })
+
+    const individualOnly = request.nextUrl.searchParams.get('individual_only')
+    if (individualOnly === 'true') {
+      query = query.eq('is_individual', true)
+    } else if (foreignOnly === 'true') {
+      query = query.eq('is_foreign', true)
+    } else if (russianOnly === 'true') {
+      query = query.eq('is_foreign', false).eq('is_individual', false)
     }
 
-    // ╨Ч╨░╨┐╨╕╤Б╤Л╨▓╨░╨╡╨╝ ╨▓ ╨╗╨╛╨│
-    await supabase.from('contract_logs').insert({
-      contract_id,
-      action: `╨Ч╨░╨│╤А╤Г╨╢╨╡╨╜╨╛: ${attachment_type} тДЦ${nextNumber}`,
-      details: `╨д╨░╨╣╨╗: ${file.name}${comment ? '. ' + comment : ''}`,
-      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
-    })
+    if (search) {
+      query = query.or(`full_name.ilike.%${search}%,short_name.ilike.%${search}%,inn.ilike.%${search}%`)
+    }
+
+    const { data, error } = await query
+    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
+    return NextResponse.json({ counterparties: data ?? [] })
 
-    return NextResponse.json({ success: true })
   } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500 })
+    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
   }
 }
 
-export async function DELETE(request: NextRequest) {
+// POST тАФ ╤Б╨╛╨╖╨┤╨░╤В╤М ╨╕╨╗╨╕ ╨╛╨▒╨╜╨╛╨▓╨╕╤В╤М ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨░
+export async function POST(request: NextRequest) {
   try {
     const body = await request.json()
-    const { id, file_url, contract_id, user_name } = body
+    const { id, contracts, check_risk, ...fields } = body
 
-    if (!id) {
-      return NextResponse.json({ error: 'id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+    fields.updated_at = new Date().toISOString()
+
+    if (id) {
+      const { data, error } = await supabase
+        .from('counterparties')
+        .update(fields)
+        .eq('id', id)
+        .select()
+        .single()
+      if (error) return NextResponse.json({ error: error.message }, { status: 400 })
+      return NextResponse.json({ success: true, counterparty: data })
     }
 
-    // ╨г╨┤╨░╨╗╤П╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╕╨╖ Storage
-    if (file_url) {
-      const filePath = file_url.split('/contracts/')[1]
-      if (filePath) {
-        await supabase.storage.from('contracts').remove([filePath])
+    // ╨Ч╨░╤Й╨╕╤В╨░ ╨╛╤В ╨┤╤Г╨▒╨╗╨╡╨╣ ╨╕╨╜╨╛╤Б╤В╤А╨░╨╜╨╜╤Л╤Е ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓
+    if (fields.is_foreign && fields.full_name) {
+      const { data: existing } = await supabase
+        .from('counterparties')
+        .select('id, full_name')
+        .eq('is_foreign', true)
+        .ilike('full_name', fields.full_name.trim())
+        .maybeSingle()
+      if (existing) {
+        return NextResponse.json({ error: '╨Ш╨╜╨╛╤Б╤В╤А╨░╨╜╨╜╤Л╨╣ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В ╤Б ╤В╨░╨║╨╕╨╝ ╨╜╨░╨╖╨▓╨░╨╜╨╕╨╡╨╝ ╤Г╨╢╨╡ ╤Б╤Г╤Й╨╡╤Б╤В╨▓╤Г╨╡╤В ╨▓ ╤А╨╡╨╡╤Б╤В╤А╨╡.' }, { status: 400 })
       }
     }
 
-    const { error } = await supabase
-      .from('document_attachments')
-      .delete()
-      .eq('id', id)
-
+    const { data, error } = await supabase
+      .from('counterparties')
+      .insert(fields)
+      .select()
+      .single()
     if (error) {
+      if (error.code === '23505' || error.message?.includes('duplicate key') || error.message?.includes('counterparties_inn_kpp_unique')) {
+        return NextResponse.json({ error: '╨Ъ╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В ╤Б ╤В╨░╨║╨╕╨╝ ╨Ш╨Э╨Э ╤Г╨╢╨╡ ╨┤╨╛╨▒╨░╨▓╨╗╨╡╨╜ ╨▓ ╨▒╨░╨╖╤Г. ╨Э╨░╨╣╨┤╨╕╤В╨╡ ╨╡╨│╨╛ ╨▓ ╤А╨╡╨╡╤Б╤В╤А╨╡ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓.' }, { status: 409 })
+      }
       return NextResponse.json({ error: error.message }, { status: 400 })
     }
+    return NextResponse.json({ success: true, counterparty: data })
 
-    await supabase.from('contract_logs').insert({
-      contract_id,
-      action: '╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╤Л╨╣ ╨╝╨░╤В╨╡╤А╨╕╨░╨╗ ╤Г╨┤╨░╨╗╤С╨╜',
-      details: `╨д╨░╨╣╨╗ ╤Г╨┤╨░╨╗╤С╨╜`,
-      user_name: user_name || '╨б╨╕╤Б╤В╨╡╨╝╨░',
-    })
+  } catch (err) {
+    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
+  }
+}
 
+// DELETE тАФ ╤Г╨┤╨░╨╗╨╕╤В╤М ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨░
+export async function DELETE(request: NextRequest) {
+  try {
+    const id = request.nextUrl.searchParams.get('id')
+    if (!id) return NextResponse.json({ error: 'id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
+
+    const { error } = await supabase
+      .from('counterparties')
+      .delete()
+      .eq('id', id)
+    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
     return NextResponse.json({ success: true })
+
   } catch (err) {
-    const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-    return NextResponse.json({ error: message }, { status: 500 })
+    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
   }
 }
 
@@ -8486,9 +8638,8 @@ export async function createBitrixChat(opts: {
     const data = await res.json()
     if (data.error) { console.error('Bitrix chat create error:', data.error); return null }
     const chatId = data.result
-    const bitrixPortal = process.env.BITRIX_PORTAL ?? 'gkepotos.bitrix24.ru'
     const link = opts.contract_id
-      ? `https://${bitrixPortal}/contracts/${opts.contract_id}`
+      ? `https://epotos-ur-intel.vercel.app/contracts/${opts.contract_id}`
       : null
     const docRef = link
       ? `[URL=${link}]${opts.document_number} тАФ ${opts.document_title}[/URL]`
@@ -8533,158 +8684,6 @@ export async function sendBitrixChatMessage(chatId: number, message: string): Pr
   } catch (err) { console.error('sendBitrixChatMessage error:', err) }
 }
 
-================
-File: app/api/counterparties/route.ts
-================
-import { createClient } from '@supabase/supabase-js'
-import { NextRequest, NextResponse } from 'next/server'
-
-const supabase = createClient(
-  process.env.NEXT_PUBLIC_SUPABASE_URL!,
-  process.env.SUPABASE_SECRET_KEY!
-)
-
-// GET тАФ ╤Б╨┐╨╕╤Б╨╛╨║ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓ ╨╕╨╗╨╕ ╨╛╨┤╨╕╨╜ ╨┐╨╛ id
-export async function GET(request: NextRequest) {
-  try {
-    const id = request.nextUrl.searchParams.get('id')
-    const inn = request.nextUrl.searchParams.get('inn')
-    const search = request.nextUrl.searchParams.get('search')
-
-    if (id) {
-      const { data, error } = await supabase
-        .from('counterparties')
-        .select('*, contracts(id, number, title, status, created_at)')
-        .eq('id', id)
-        .single()
-
-      // ╨Ф╨╛╨┐╨╛╨╗╨╜╨╕╤В╨╡╨╗╤М╨╜╨╛ ╨╖╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╤Л ╨┐╨╛ ╤В╨╡╨║╤Б╤В╨╛╨▓╨╛╨╝╤Г ╤Б╨╛╨▓╨┐╨░╨┤╨╡╨╜╨╕╤О ╨╡╤Б╨╗╨╕ counterparty_id ╨╜╨╡ ╨╖╨░╨┐╨╛╨╗╨╜╨╡╨╜
-      if (data && data.contracts?.length === 0) {
-        const searchName = data.short_name ?? data.full_name
-        const { data: extraContracts } = await supabase
-          .from('contracts')
-          .select('id, number, title, status, created_at')
-          .ilike('counterparty', `%${searchName}%`)
-          .is('deleted_at', null)
-          .order('created_at', { ascending: false })
-        if (extraContracts?.length) {
-          data.contracts = extraContracts
-        }
-      }
-      if (error) return NextResponse.json({ error: error.message }, { status: 400 })
-      return NextResponse.json({ counterparty: data })
-    }
-
-    if (inn) {
-      const kpp = request.nextUrl.searchParams.get('kpp')
-      let query = supabase
-        .from('counterparties')
-        .select('*')
-        .eq('inn', inn)
-      if (kpp) query = query.eq('kpp', kpp)
-      const { data, error } = await query.single()
-      if (error && error.code !== 'PGRST116') return NextResponse.json({ error: error.message }, { status: 400 })
-      return NextResponse.json({ counterparty: data ?? null })
-    }
-
-    const foreignOnly = request.nextUrl.searchParams.get('foreign_only')
-    const russianOnly = request.nextUrl.searchParams.get('russian_only')
-
-    let query = supabase
-      .from('counterparties')
-      .select('id, inn, kpp, ogrn, short_name, full_name, status, risk_level, director_name, director_title, phone, email, legal_address, signatory_name, poa_number, poa_date, created_at, is_foreign, country, registration_number, is_individual')
-      .order('full_name', { ascending: true })
-
-    const individualOnly = request.nextUrl.searchParams.get('individual_only')
-    if (individualOnly === 'true') {
-      query = query.eq('is_individual', true)
-    } else if (foreignOnly === 'true') {
-      query = query.eq('is_foreign', true)
-    } else if (russianOnly === 'true') {
-      query = query.eq('is_foreign', false).eq('is_individual', false)
-    }
-
-    if (search) {
-      query = query.or(`full_name.ilike.%${search}%,short_name.ilike.%${search}%,inn.ilike.%${search}%`)
-    }
-
-    const { data, error } = await query
-    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
-    return NextResponse.json({ counterparties: data ?? [] })
-
-  } catch (err) {
-    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
-  }
-}
-
-// POST тАФ ╤Б╨╛╨╖╨┤╨░╤В╤М ╨╕╨╗╨╕ ╨╛╨▒╨╜╨╛╨▓╨╕╤В╤М ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨░
-export async function POST(request: NextRequest) {
-  try {
-    const body = await request.json()
-    const { id, contracts, check_risk, ...fields } = body
-
-    fields.updated_at = new Date().toISOString()
-
-    if (id) {
-      const { data, error } = await supabase
-        .from('counterparties')
-        .update(fields)
-        .eq('id', id)
-        .select()
-        .single()
-      if (error) return NextResponse.json({ error: error.message }, { status: 400 })
-      return NextResponse.json({ success: true, counterparty: data })
-    }
-
-    // ╨Ч╨░╤Й╨╕╤В╨░ ╨╛╤В ╨┤╤Г╨▒╨╗╨╡╨╣ ╨╕╨╜╨╛╤Б╤В╤А╨░╨╜╨╜╤Л╤Е ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓
-    if (fields.is_foreign && fields.full_name) {
-      const { data: existing } = await supabase
-        .from('counterparties')
-        .select('id, full_name')
-        .eq('is_foreign', true)
-        .ilike('full_name', fields.full_name.trim())
-        .maybeSingle()
-      if (existing) {
-        return NextResponse.json({ error: '╨Ш╨╜╨╛╤Б╤В╤А╨░╨╜╨╜╤Л╨╣ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В ╤Б ╤В╨░╨║╨╕╨╝ ╨╜╨░╨╖╨▓╨░╨╜╨╕╨╡╨╝ ╤Г╨╢╨╡ ╤Б╤Г╤Й╨╡╤Б╤В╨▓╤Г╨╡╤В ╨▓ ╤А╨╡╨╡╤Б╤В╤А╨╡.' }, { status: 400 })
-      }
-    }
-
-    const { data, error } = await supabase
-      .from('counterparties')
-      .insert(fields)
-      .select()
-      .single()
-    if (error) {
-      if (error.code === '23505' || error.message?.includes('duplicate key') || error.message?.includes('counterparties_inn_kpp_unique')) {
-        return NextResponse.json({ error: '╨Ъ╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В ╤Б ╤В╨░╨║╨╕╨╝ ╨Ш╨Э╨Э ╤Г╨╢╨╡ ╨┤╨╛╨▒╨░╨▓╨╗╨╡╨╜ ╨▓ ╨▒╨░╨╖╤Г. ╨Э╨░╨╣╨┤╨╕╤В╨╡ ╨╡╨│╨╛ ╨▓ ╤А╨╡╨╡╤Б╤В╤А╨╡ ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨╛╨▓.' }, { status: 409 })
-      }
-      return NextResponse.json({ error: error.message }, { status: 400 })
-    }
-    return NextResponse.json({ success: true, counterparty: data })
-
-  } catch (err) {
-    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
-  }
-}
-
-// DELETE тАФ ╤Г╨┤╨░╨╗╨╕╤В╤М ╨║╨╛╨╜╤В╤А╨░╨│╨╡╨╜╤В╨░
-export async function DELETE(request: NextRequest) {
-  try {
-    const id = request.nextUrl.searchParams.get('id')
-    if (!id) return NextResponse.json({ error: 'id ╨╛╨▒╤П╨╖╨░╤В╨╡╨╗╨╡╨╜' }, { status: 400 })
-
-    const { error } = await supabase
-      .from('counterparties')
-      .delete()
-      .eq('id', id)
-    if (error) return NextResponse.json({ error: error.message }, { status: 400 })
-    return NextResponse.json({ success: true })
-
-  } catch (err) {
-    return NextResponse.json({ error: err instanceof Error ? err.message : '╨Ю╤И╨╕╨▒╨║╨░' }, { status: 500 })
-  }
-}
-
 
 
 
diff --git a/repomix-components-yurintel.txt b/repomix-components-yurintel.txt
index 4bd4d7d..25dde3a 100644
--- a/repomix-components-yurintel.txt
+++ b/repomix-components-yurintel.txt
@@ -4176,147 +4176,6 @@ export default function MyDocuments({ data, loading, onReload }: MyDocumentsProp
   )
 }
 
-================
-File: app/hooks/useBitrixAuth.ts
-================
-'use client'
-
-import { useState, useEffect } from 'react'
-
-export interface BitrixUser {
-  id: string
-  name: string
-  email: string
-  avatar: string | null
-  auth_id: string
-  refresh_id: string
-  domain: string
-  member_id: string
-}
-
-// ╨Ю╨▒╤К╤П╨▓╨╗╤П╨╡╨╝ ╤В╨╕╨┐ ╨┤╨╗╤П BX24 SDK
-declare global {
-  interface Window {
-    BX24?: {
-      setTitle: (count: number) => void
-      init: (callback: () => void) => void
-    }
-  }
-}
-
-export function useBitrixAuth() {
-  const [user, setUser] = useState<BitrixUser | null>(null)
-  const [loading, setLoading] = useState(true)
-
-  useEffect(() => {
-    const authenticate = async () => {
-      try {
-        // 1. ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╨░╤А╨░╨╝╨╡╤В╤А╤Л ╨▓ URL (╨╛╤В ╨С╨╕╤В╤А╨╕╨║╤Б24)
-        const params = new URLSearchParams(window.location.search)
-        const authId = params.get('auth_id')
-        const refreshId = params.get('refresh_id')
-        const domain = params.get('domain')
-        const memberId = params.get('member_id')
-
-        // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ contract_id ╨╕╨╖ URL ╨Ф╨Ю ╨░╨▓╤В╨╛╤А╨╕╨╖╨░╤Ж╨╕╨╕
-        const contractIdFromUrl = params.get('contract_id')
-        if (contractIdFromUrl) {
-          sessionStorage.setItem('pending_contract_id', contractIdFromUrl)
-        }
-
-        if (authId && domain && memberId) {
-          const formData = new FormData()
-          formData.append('AUTH_ID', authId)
-          formData.append('REFRESH_ID', refreshId ?? '')
-          formData.append('DOMAIN', domain)
-          formData.append('member_id', memberId)
-
-          const response = await fetch('/api/bitrix/callback', { method: 'POST', body: formData })
-          const data = await response.json()
-
-          if (data.success) {
-            setUser(data.user)
-            sessionStorage.setItem('bitrix_user', JSON.stringify({
-              ...data.user,
-              auth_id: authId,
-              member_id: memberId,
-            }))
-            const contractId = params.get('contract_id')
-              ?? sessionStorage.getItem('pending_contract_id')
-            sessionStorage.removeItem('pending_contract_id')
-            if (contractId) {
-              window.location.replace(`/contracts/${contractId}`)
-            } else {
-              window.history.replaceState({}, '', '/')
-            }
-          }
-          setLoading(false)
-          return
-        }
-
-        // 2. ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ sessionStorage
-        const stored = sessionStorage.getItem('bitrix_user')
-        if (stored) {
-          const storedUser = JSON.parse(stored)
-          if (storedUser.auth_id && storedUser.member_id) {
-            const verifyRes = await fetch('/api/bitrix/verify', {
-              method: 'POST',
-              headers: { 'Content-Type': 'application/json' },
-              body: JSON.stringify({
-                auth_id: storedUser.auth_id,
-                member_id: storedUser.member_id,
-                user_id: storedUser.id,
-              }),
-            })
-            const verifyData = await verifyRes.json()
-            if (verifyData.valid) {
-              setUser(storedUser)
-              // ╨Х╤Б╨╗╨╕ ╨▓ URL ╨╡╤Б╤В╤М contract_id тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨╜╨░ ╨║╨░╤А╤В╨╛╤З╨║╤Г
-              const contractId = params.get('contract_id')
-              if (contractId && !window.location.pathname.startsWith('/contracts/')) {
-                window.location.replace(`/contracts/${contractId}`)
-                return
-              }
-            } else {
-              sessionStorage.removeItem('bitrix_user')
-              window.location.reload()
-            }
-          } else {
-            // ╨Э╨╡╤В auth_id тАФ ╨╝╨╛╨▒╨╕╨╗╤М╨╜╤Л╨╣ ╨С24, ╤А╨░╨╖╤А╨╡╤И╨░╨╡╨╝
-            setUser(storedUser)
-            // ╨Х╤Б╨╗╨╕ ╨▓ URL ╨╡╤Б╤В╤М contract_id тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨╜╨░ ╨║╨░╤А╤В╨╛╤З╨║╤Г
-            const contractId = params.get('contract_id')
-            if (contractId && !window.location.pathname.startsWith('/contracts/')) {
-              window.location.replace(`/contracts/${contractId}`)
-              return
-            }
-          }
-        } else {
-          // ╨Э╨╡╤В ╨╜╨╕ URL ╨┐╨░╤А╨░╨╝╨╡╤В╤А╨╛╨▓ ╨╜╨╕ sessionStorage тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨▓ ╨С24
-          if (window.self === window.top) {
-            // ╨Ю╤В╨║╤А╤Л╤В ╨╜╨╡ ╨▓ iframe тАФ ╨┐╤А╤П╨╝╨░╤П ╤Б╤Б╤Л╨╗╨║╨░ ╨▒╨╡╨╖ ╨░╨▓╤В╨╛╤А╨╕╨╖╨░╤Ж╨╕╨╕
-            window.location.replace('https://gkepotos.bitrix24.ru/marketplace/app/252/')
-          }
-        }
-      } catch (err) {
-        console.error('Bitrix auth error:', err)
-      } finally {
-        setLoading(false)
-      }
-    }
-
-    authenticate()
-  }, [])
-
-  const logout = () => {
-    sessionStorage.removeItem('bitrix_user')
-    setUser(null)
-    if (window.BX24?.setTitle) window.BX24.setTitle(0)
-  }
-
-  return { user, loading, logout }
-}
-
 ================
 File: app/components/GenerateFromTemplate.tsx
 ================
@@ -5150,6 +5009,150 @@ export default function GenerateFromTemplate({ contract, onUploaded }: GenerateF
   return null
 }
 
+================
+File: app/hooks/useBitrixAuth.ts
+================
+'use client'
+
+import { useState, useEffect } from 'react'
+
+export interface BitrixUser {
+  id: string
+  name: string
+  email: string
+  avatar: string | null
+  auth_id: string
+  refresh_id: string
+  domain: string
+  member_id: string
+}
+
+// ╨Ю╨▒╤К╤П╨▓╨╗╤П╨╡╨╝ ╤В╨╕╨┐ ╨┤╨╗╤П BX24 SDK
+declare global {
+  interface Window {
+    BX24?: {
+      setTitle: (count: number) => void
+      init: (callback: () => void) => void
+    }
+  }
+}
+
+export function useBitrixAuth() {
+  const [user, setUser] = useState<BitrixUser | null>(null)
+  const [loading, setLoading] = useState(true)
+
+  useEffect(() => {
+    const authenticate = async () => {
+      try {
+        // 1. ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ ╨┐╨░╤А╨░╨╝╨╡╤В╤А╤Л ╨▓ URL (╨╛╤В ╨С╨╕╤В╤А╨╕╨║╤Б24)
+        const params = new URLSearchParams(window.location.search)
+        const authId = params.get('auth_id')
+        const refreshId = params.get('refresh_id')
+        const domain = params.get('domain')
+        const memberId = params.get('member_id')
+
+        // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ contract_id ╨╕╨╖ URL ╨Ф╨Ю ╨░╨▓╤В╨╛╤А╨╕╨╖╨░╤Ж╨╕╨╕
+        const contractIdFromUrl = params.get('contract_id')
+        console.log('[BX24 Auth] URL:', window.location.href)
+        console.log('[BX24 Auth] contract_id from URL:', contractIdFromUrl)
+        console.log('[BX24 Auth] all params:', window.location.search)
+        if (contractIdFromUrl) {
+          sessionStorage.setItem('pending_contract_id', contractIdFromUrl)
+        }
+
+        if (authId && domain && memberId) {
+          const formData = new FormData()
+          formData.append('AUTH_ID', authId)
+          formData.append('REFRESH_ID', refreshId ?? '')
+          formData.append('DOMAIN', domain)
+          formData.append('member_id', memberId)
+
+          const response = await fetch('/api/bitrix/callback', { method: 'POST', body: formData })
+          const data = await response.json()
+
+          if (data.success) {
+            setUser(data.user)
+            sessionStorage.setItem('bitrix_user', JSON.stringify({
+              ...data.user,
+              auth_id: authId,
+              member_id: memberId,
+            }))
+            const contractId = params.get('contract_id')
+              ?? sessionStorage.getItem('pending_contract_id')
+            sessionStorage.removeItem('pending_contract_id')
+            if (contractId) {
+              window.location.replace(`/contracts/${contractId}`)
+            } else if (!window.location.pathname.startsWith('/contracts/')) {
+              window.history.replaceState({}, '', '/')
+            }
+          }
+          setLoading(false)
+          return
+        }
+
+        // 2. ╨Я╤А╨╛╨▓╨╡╤А╤П╨╡╨╝ sessionStorage
+        const stored = sessionStorage.getItem('bitrix_user')
+        if (stored) {
+          const storedUser = JSON.parse(stored)
+          if (storedUser.auth_id && storedUser.member_id) {
+            const verifyRes = await fetch('/api/bitrix/verify', {
+              method: 'POST',
+              headers: { 'Content-Type': 'application/json' },
+              body: JSON.stringify({
+                auth_id: storedUser.auth_id,
+                member_id: storedUser.member_id,
+                user_id: storedUser.id,
+              }),
+            })
+            const verifyData = await verifyRes.json()
+            if (verifyData.valid) {
+              setUser(storedUser)
+              // ╨Х╤Б╨╗╨╕ ╨▓ URL ╨╡╤Б╤В╤М contract_id тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨╜╨░ ╨║╨░╤А╤В╨╛╤З╨║╤Г
+              const contractId = params.get('contract_id')
+              if (contractId && !window.location.pathname.startsWith('/contracts/')) {
+                window.location.replace(`/contracts/${contractId}`)
+                return
+              }
+            } else {
+              sessionStorage.removeItem('bitrix_user')
+              window.location.reload()
+            }
+          } else {
+            // ╨Э╨╡╤В auth_id тАФ ╨╝╨╛╨▒╨╕╨╗╤М╨╜╤Л╨╣ ╨С24, ╤А╨░╨╖╤А╨╡╤И╨░╨╡╨╝
+            setUser(storedUser)
+            // ╨Х╤Б╨╗╨╕ ╨▓ URL ╨╡╤Б╤В╤М contract_id тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨╜╨░ ╨║╨░╤А╤В╨╛╤З╨║╤Г
+            const contractId = params.get('contract_id')
+            if (contractId && !window.location.pathname.startsWith('/contracts/')) {
+              window.location.replace(`/contracts/${contractId}`)
+              return
+            }
+          }
+        } else {
+          // ╨Э╨╡╤В ╨╜╨╕ URL ╨┐╨░╤А╨░╨╝╨╡╤В╤А╨╛╨▓ ╨╜╨╕ sessionStorage тАФ ╤А╨╡╨┤╨╕╤А╨╡╨║╤В╨╕╨╝ ╨▓ ╨С24
+          if (window.self === window.top) {
+            // ╨Ю╤В╨║╤А╤Л╤В ╨╜╨╡ ╨▓ iframe тАФ ╨┐╤А╤П╨╝╨░╤П ╤Б╤Б╤Л╨╗╨║╨░ ╨▒╨╡╨╖ ╨░╨▓╤В╨╛╤А╨╕╨╖╨░╤Ж╨╕╨╕
+            window.location.replace('https://gkepotos.bitrix24.ru/marketplace/app/252/')
+          }
+        }
+      } catch (err) {
+        console.error('Bitrix auth error:', err)
+      } finally {
+        setLoading(false)
+      }
+    }
+
+    authenticate()
+  }, [])
+
+  const logout = () => {
+    sessionStorage.removeItem('bitrix_user')
+    setUser(null)
+    if (window.BX24?.setTitle) window.BX24.setTitle(0)
+  }
+
+  return { user, loading, logout }
+}
+
 ================
 File: app/components/ContractTabs.tsx
 ================
diff --git a/repomix-pages-yurintel.txt b/repomix-pages-yurintel.txt
index af1ad01..794caae 100644
--- a/repomix-pages-yurintel.txt
+++ b/repomix-pages-yurintel.txt
@@ -1705,6 +1705,180 @@ export default function ApprovePage() {
   )
 }
 
+================
+File: app/contracts/[id]/upload/page.tsx
+================
+'use client'
+
+import { useState } from 'react'
+import Link from 'next/link'
+import { useParams } from 'next/navigation'
+import { useBitrixAuth } from '@/app/hooks/useBitrixAuth'
+import { uploadFileDirect } from '@/app/utils/uploadFile'
+
+export default function UploadVersionPage() {
+  const params = useParams()
+  const contractId = params.id as string
+  const { user } = useBitrixAuth()
+
+  const [loading, setLoading] = useState(false)
+  const [error, setError] = useState('')
+  const [file, setFile] = useState<File | null>(null)
+  const [comment, setComment] = useState('')
+
+  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
+    const selected = e.target.files?.[0]
+    if (!selected) return
+
+    const allowed = [
+      'application/pdf',
+      'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
+      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
+    ]
+
+    if (!allowed.includes(selected.type)) {
+      setError('╨Ф╨╛╨┐╤Г╤Б╤В╨╕╨╝╤Л╨╡ ╤Д╨╛╤А╨╝╨░╤В╤Л: PDF, DOCX, XLSX')
+      return
+    }
+
+    if (selected.size > 20 * 1024 * 1024) {
+      setError('╨д╨░╨╣╨╗ ╨╜╨╡ ╨┤╨╛╨╗╨╢╨╡╨╜ ╨┐╤А╨╡╨▓╤Л╤И╨░╤В╤М 20 ╨Ь╨С')
+      return
+    }
+
+    setFile(selected)
+    setError('')
+  }
+
+  const handleSubmit = async (e: React.FormEvent) => {
+    e.preventDefault()
+    if (!file) { setError('╨Т╤Л╨▒╨╡╤А╨╕╤В╨╡ ╤Д╨░╨╣╨╗'); return }
+
+    setLoading(true)
+    setError('')
+
+    try {
+      // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╜╨░╨┐╤А╤П╨╝╤Г╤О ╨▓ Supabase (╨╛╨▒╤Е╨╛╨┤╨╕╤В ╨╗╨╕╨╝╨╕╤В Vercel 4.5 ╨Ь╨С)
+      const { public_url, file_name } = await uploadFileDirect(
+        file,
+        'contracts',
+        `versions/${contractId}`
+      )
+
+      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╛ ╨▓╨╡╤А╤Б╨╕╨╕ ╨▓ ╨С╨Ф
+      const response = await fetch('https://epotos-ur-intel.vercel.app/api/versions', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        body: JSON.stringify({
+          contract_id: contractId,
+          comment,
+          user_name: user?.name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
+          file_url: public_url,
+          file_name,
+        }),
+      })
+
+      const data = await response.json()
+
+      if (!response.ok) {
+        setError('╨Ю╤И╨╕╨▒╨║╨░ ╤Б╨╛╤Е╤А╨░╨╜╨╡╨╜╨╕╤П ╨▓╨╡╤А╤Б╨╕╨╕: ' + (data.error ?? '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'))
+        setLoading(false)
+        return
+      }
+
+      window.location.href = `https://epotos-ur-intel.vercel.app/contracts/${contractId}`
+    } catch (err) {
+      const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
+      setError('╨Ю╤И╨╕╨▒╨║╨░: ' + message)
+      setLoading(false)
+    }
+  }
+
+  return (
+    <div className="min-h-screen bg-gray-50">
+      <div className="max-w-2xl mx-auto px-4 py-8">
+
+        <div className="flex items-center gap-3 mb-8">
+          <Link href={`/contracts/${contractId}`}
+            className="text-sm text-gray-500 hover:text-gray-700 transition-colors">
+            тЖР ╨Э╨░╨╖╨░╨┤
+          </Link>
+          <span className="text-gray-300">/</span>
+          <h1 className="text-xl font-semibold text-gray-900">╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨▓╨╡╤А╤Б╨╕╤О ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░</h1>
+        </div>
+
+        <div className="bg-white rounded-xl border border-gray-200 p-6">
+          <form onSubmit={handleSubmit} className="space-y-5">
+
+            {error && (
+              <div className="bg-red-50 border border-red-200 rounded-lg px-4 py-3 text-sm text-red-700">
+                {error}
+              </div>
+            )}
+
+            <div>
+              <label className="block text-sm font-medium text-gray-700 mb-2">
+                ╨д╨░╨╣╨╗ ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ <span className="text-red-500">*</span>
+              </label>
+              <div className={`relative border-2 border-dashed rounded-xl p-8 text-center transition-colors ${file ? 'border-gray-900 bg-gray-50' : 'border-gray-200 hover:border-gray-400'}`}>
+                {file ? (
+                  <div>
+                    <p className="text-sm font-medium text-gray-900">{file.name}</p>
+                    <p className="text-xs text-gray-500 mt-1">
+                      {(file.size / 1024 / 1024).toFixed(2)} ╨Ь╨С
+                    </p>
+                    <button type="button" onClick={() => setFile(null)}
+                      className="mt-2 text-xs text-red-500 hover:text-red-700 underline">
+                      ╨г╨┤╨░╨╗╨╕╤В╤М
+                    </button>
+                  </div>
+                ) : (
+                  <div>
+                    <p className="text-sm text-gray-500 mb-2">╨Э╨░╨╢╨╝╨╕╤В╨╡ ╨┤╨╗╤П ╨▓╤Л╨▒╨╛╤А╨░ ╤Д╨░╨╣╨╗╨░</p>
+                    <p className="text-xs text-gray-400">PDF, DOCX, XLSX тАФ ╨┤╨╛ 20 ╨Ь╨С</p>
+                  </div>
+                )}
+                {!file && (
+                  <input
+                    type="file"
+                    accept=".pdf,.docx,.xlsx"
+                    onChange={handleFileChange}
+                    className="absolute inset-0 w-full h-full opacity-0 cursor-pointer"
+                  />
+                )}
+              </div>
+            </div>
+
+            <div>
+              <label className="block text-sm font-medium text-gray-700 mb-1">
+                ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣ ╨║ ╨▓╨╡╤А╤Б╨╕╨╕
+              </label>
+              <input
+                value={comment}
+                onChange={(e) => setComment(e.target.value)}
+                placeholder="╨Э╨░╨┐╤А╨╕╨╝╨╡╤А: ╨┐╨╡╤А╨▓╨╕╤З╨╜╨░╤П ╨▓╨╡╤А╤Б╨╕╤П, ╨┐╤А╨░╨▓╨║╨╕ ╤О╤А╨╕╤Б╤В╨░..."
+                className="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-gray-900"
+              />
+            </div>
+
+            <div className="flex gap-3 pt-2">
+              <button type="submit" disabled={loading || !file}
+                className="flex-1 bg-gray-900 text-white py-2 rounded-lg text-sm font-medium hover:bg-gray-700 transition-colors disabled:opacity-50">
+                {loading ? '╨Ч╨░╨│╤А╤Г╨╖╨║╨░...' : '╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В'}
+              </button>
+              <Link href={`/contracts/${contractId}`}
+                className="px-4 py-2 border border-gray-200 rounded-lg text-sm text-gray-600 hover:bg-gray-50 transition-colors">
+                ╨Ю╤В╨╝╨╡╨╜╨░
+              </Link>
+            </div>
+
+          </form>
+        </div>
+      </div>
+    </div>
+  )
+}
+
 ================
 File: app/dashboard/page.tsx
 ================
@@ -3644,180 +3818,6 @@ useEffect(() => {
   )
 }
 
-================
-File: app/contracts/[id]/upload/page.tsx
-================
-'use client'
-
-import { useState } from 'react'
-import Link from 'next/link'
-import { useParams } from 'next/navigation'
-import { useBitrixAuth } from '@/app/hooks/useBitrixAuth'
-import { uploadFileDirect } from '@/app/utils/uploadFile'
-
-export default function UploadVersionPage() {
-  const params = useParams()
-  const contractId = params.id as string
-  const { user } = useBitrixAuth()
-
-  const [loading, setLoading] = useState(false)
-  const [error, setError] = useState('')
-  const [file, setFile] = useState<File | null>(null)
-  const [comment, setComment] = useState('')
-
-  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
-    const selected = e.target.files?.[0]
-    if (!selected) return
-
-    const allowed = [
-      'application/pdf',
-      'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
-      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
-    ]
-
-    if (!allowed.includes(selected.type)) {
-      setError('╨Ф╨╛╨┐╤Г╤Б╤В╨╕╨╝╤Л╨╡ ╤Д╨╛╤А╨╝╨░╤В╤Л: PDF, DOCX, XLSX')
-      return
-    }
-
-    if (selected.size > 20 * 1024 * 1024) {
-      setError('╨д╨░╨╣╨╗ ╨╜╨╡ ╨┤╨╛╨╗╨╢╨╡╨╜ ╨┐╤А╨╡╨▓╤Л╤И╨░╤В╤М 20 ╨Ь╨С')
-      return
-    }
-
-    setFile(selected)
-    setError('')
-  }
-
-  const handleSubmit = async (e: React.FormEvent) => {
-    e.preventDefault()
-    if (!file) { setError('╨Т╤Л╨▒╨╡╤А╨╕╤В╨╡ ╤Д╨░╨╣╨╗'); return }
-
-    setLoading(true)
-    setError('')
-
-    try {
-      // ╨Ч╨░╨│╤А╤Г╨╢╨░╨╡╨╝ ╤Д╨░╨╣╨╗ ╨╜╨░╨┐╤А╤П╨╝╤Г╤О ╨▓ Supabase (╨╛╨▒╤Е╨╛╨┤╨╕╤В ╨╗╨╕╨╝╨╕╤В Vercel 4.5 ╨Ь╨С)
-      const { public_url, file_name } = await uploadFileDirect(
-        file,
-        'contracts',
-        `versions/${contractId}`
-      )
-
-      // ╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝ ╨╖╨░╨┐╨╕╤Б╤М ╨╛ ╨▓╨╡╤А╤Б╨╕╨╕ ╨▓ ╨С╨Ф
-      const response = await fetch('https://epotos-ur-intel.vercel.app/api/versions', {
-        method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({
-          contract_id: contractId,
-          comment,
-          user_name: user?.name ?? '╨б╨╕╤Б╤В╨╡╨╝╨░',
-          file_url: public_url,
-          file_name,
-        }),
-      })
-
-      const data = await response.json()
-
-      if (!response.ok) {
-        setError('╨Ю╤И╨╕╨▒╨║╨░ ╤Б╨╛╤Е╤А╨░╨╜╨╡╨╜╨╕╤П ╨▓╨╡╤А╤Б╨╕╨╕: ' + (data.error ?? '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'))
-        setLoading(false)
-        return
-      }
-
-      window.location.href = `https://epotos-ur-intel.vercel.app/contracts/${contractId}`
-    } catch (err) {
-      const message = err instanceof Error ? err.message : '╨Э╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░'
-      setError('╨Ю╤И╨╕╨▒╨║╨░: ' + message)
-      setLoading(false)
-    }
-  }
-
-  return (
-    <div className="min-h-screen bg-gray-50">
-      <div className="max-w-2xl mx-auto px-4 py-8">
-
-        <div className="flex items-center gap-3 mb-8">
-          <Link href={`/contracts/${contractId}`}
-            className="text-sm text-gray-500 hover:text-gray-700 transition-colors">
-            тЖР ╨Э╨░╨╖╨░╨┤
-          </Link>
-          <span className="text-gray-300">/</span>
-          <h1 className="text-xl font-semibold text-gray-900">╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨▓╨╡╤А╤Б╨╕╤О ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В╨░</h1>
-        </div>
-
-        <div className="bg-white rounded-xl border border-gray-200 p-6">
-          <form onSubmit={handleSubmit} className="space-y-5">
-
-            {error && (
-              <div className="bg-red-50 border border-red-200 rounded-lg px-4 py-3 text-sm text-red-700">
-                {error}
-              </div>
-            )}
-
-            <div>
-              <label className="block text-sm font-medium text-gray-700 mb-2">
-                ╨д╨░╨╣╨╗ ╨┤╨╛╨│╨╛╨▓╨╛╤А╨░ <span className="text-red-500">*</span>
-              </label>
-              <div className={`relative border-2 border-dashed rounded-xl p-8 text-center transition-colors ${file ? 'border-gray-900 bg-gray-50' : 'border-gray-200 hover:border-gray-400'}`}>
-                {file ? (
-                  <div>
-                    <p className="text-sm font-medium text-gray-900">{file.name}</p>
-                    <p className="text-xs text-gray-500 mt-1">
-                      {(file.size / 1024 / 1024).toFixed(2)} ╨Ь╨С
-                    </p>
-                    <button type="button" onClick={() => setFile(null)}
-                      className="mt-2 text-xs text-red-500 hover:text-red-700 underline">
-                      ╨г╨┤╨░╨╗╨╕╤В╤М
-                    </button>
-                  </div>
-                ) : (
-                  <div>
-                    <p className="text-sm text-gray-500 mb-2">╨Э╨░╨╢╨╝╨╕╤В╨╡ ╨┤╨╗╤П ╨▓╤Л╨▒╨╛╤А╨░ ╤Д╨░╨╣╨╗╨░</p>
-                    <p className="text-xs text-gray-400">PDF, DOCX, XLSX тАФ ╨┤╨╛ 20 ╨Ь╨С</p>
-                  </div>
-                )}
-                {!file && (
-                  <input
-                    type="file"
-                    accept=".pdf,.docx,.xlsx"
-                    onChange={handleFileChange}
-                    className="absolute inset-0 w-full h-full opacity-0 cursor-pointer"
-                  />
-                )}
-              </div>
-            </div>
-
-            <div>
-              <label className="block text-sm font-medium text-gray-700 mb-1">
-                ╨Ъ╨╛╨╝╨╝╨╡╨╜╤В╨░╤А╨╕╨╣ ╨║ ╨▓╨╡╤А╤Б╨╕╨╕
-              </label>
-              <input
-                value={comment}
-                onChange={(e) => setComment(e.target.value)}
-                placeholder="╨Э╨░╨┐╤А╨╕╨╝╨╡╤А: ╨┐╨╡╤А╨▓╨╕╤З╨╜╨░╤П ╨▓╨╡╤А╤Б╨╕╤П, ╨┐╤А╨░╨▓╨║╨╕ ╤О╤А╨╕╤Б╤В╨░..."
-                className="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-gray-900"
-              />
-            </div>
-
-            <div className="flex gap-3 pt-2">
-              <button type="submit" disabled={loading || !file}
-                className="flex-1 bg-gray-900 text-white py-2 rounded-lg text-sm font-medium hover:bg-gray-700 transition-colors disabled:opacity-50">
-                {loading ? '╨Ч╨░╨│╤А╤Г╨╖╨║╨░...' : '╨Ч╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨┤╨╛╨║╤Г╨╝╨╡╨╜╤В'}
-              </button>
-              <Link href={`/contracts/${contractId}`}
-                className="px-4 py-2 border border-gray-200 rounded-lg text-sm text-gray-600 hover:bg-gray-50 transition-colors">
-                ╨Ю╤В╨╝╨╡╨╜╨░
-              </Link>
-            </div>
-
-          </form>
-        </div>
-      </div>
-    </div>
-  )
-}
-
 ================
 File: app/help/page.tsx
 ================
