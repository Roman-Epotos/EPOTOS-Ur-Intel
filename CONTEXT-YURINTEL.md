# CONTEXT-YURINTEL.md — ЮрИнтел-Эпотос
Дата: 2026-07-09
Статус: Спринт 4 активен

=============================================================
1. О ПРОЕКТЕ
=============================================================
Название: ЮрИнтел-Эпотос (первый модуль платформы ЭПОТОС-Витрина Данных)
Компания: ГК ЭПОТОС (epotos.ru) — производство противопожарного оборудования
Дочерние: ООО Техно (ТХ), НПП ЭПОТОС (НПП), СПТ (СПТ), ОС (ОС), Эпотос-К (Э-К)

URL: https://epotos-ur-intel.vercel.app
GitHub: https://github.com/Roman-Epotos/EPOTOS-Ur-Intel
Битрикс24: https://gkepotos.bitrix24.ru/marketplace/app/252/
Название в Б24: ЮрИнтел-Эпотос
ВАЖНО: iframe-приложение в Битрикс24. Авторизация через Битрикс24 SSO.
ВАЖНО: код приложения в Б24 менялся 248 → 252 после переустановки.
  Если приложение переустанавливали снова — проверить актуальный код
  в URL Б24 (gkepotos.bitrix24.ru/marketplace/app/{код}/) и при
  необходимости заменить во всех ссылках (notify.ts, компоненты).

Путь к проекту: E:\ИСКУССТВЕННЫЙ ИНТЕЛЛЕКТ (AI)\ЭПОТОС-ВИТРИНА ДАННЫХ\ДОГОВОРЫ ЭПОТОС\EPOTOS-Ur-Intel
ВАЖНО: диск именно E:\ — не F:\ (регулярная путаница в PowerShell)

Общеплатформенная архитектура (карта модулей, коннекторы между модулями,
  стратегия переезда на Selectel) — теперь в CONTEXT-VITRINA.md, НЕ здесь.
  Этот файл — только про сам модуль ЮрИнтел и его связь с Core.


=============================================================
2. ТЕХНОЛОГИЧЕСКИЙ СТЕК
=============================================================
Next.js 14, TypeScript, Tailwind CSS, Supabase (cloud) + pgvector
AI основная: OpenRouter (google/gemini-2.5-flash)
AI PDF сравнение: OpenRouter (anthropic/claude-sonnet-4-5) — ПЛАТНАЯ! только ai-document-compare
Редактор: OnlyOffice (https://office.epotos-port.ru)
Storage: Supabase (buckets: contracts PUBLIC, templates PUBLIC, counterparty-docs PUBLIC)
Хостинг: Vercel Pro (оплачен и подключен с 03.07.2026)
Генерация .docx: JSZip (прямая замена {{field}} в XML)
PDF извлечение текста: unpdf — нестабилен на Vercel! Показываем ошибку
Связанный модуль: ЭПОТОС-Core — URL теперь через переменную окружения
  NEXT_PUBLIC_CORE_URL (09.07.2026, был захардкожен в 3 файлах — раздел 10)

⚠️ КРИТИЧЕСКИ ВАЖНО: OpenRouter с 27.06.2026 блокирует API-запросы от
  аккаунтов из российского региона. ЮрИнтел НЕ затронут — запросы идут
  через Vercel (нероссийский IP). Подробности, включая ПОДТВЕРЖДЁННЫЙ
  рабочий статус OpenRouter на Selectel-сервере в Казахстане — теперь
  в CONTEXT-VITRINA.md (раздел про переезд).

driver.js (библиотека туров) — УДАЛЕНА из зависимостей 08.07.2026, попытка
  реализации онбординг-тура не удалась, см. раздел 7.

app/lib/fetchWithTimeout.ts (09.07.2026) — переиспользуемая обёртка над
  fetch() с AbortController-таймаутом и логированием латентности с
  префиксом [B24_LATENCY] в Vercel Logs. Применена во всех трёх роутах
  авторизации Битрикс24 (см. раздел 10). Использовать для ЛЮБЫХ будущих
  внешних HTTP-вызовов в проекте, а не только для Битрикс24.


=============================================================
3. ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ
=============================================================
NEXT_PUBLIC_SUPABASE_URL=https://qmzyybisajjmneydekoo.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY — .env.local
OPENROUTER_API_KEY, BITRIX_CLIENT_ID, BITRIX_CLIENT_SECRET — .env.local
BITRIX_PORTAL=gkepotos.bitrix24.ru
BITRIX_WEBHOOK_URL=вебхук ЭПОТОС Ассистент (ID: 678)
ONLYOFFICE_URL=https://office.epotos-port.ru
YANDEX_DISK_TOKEN, DADATA_API_KEY — .env.local
NEXT_PUBLIC_CORE_URL=https://epotos-core.vercel.app (НОВОЕ 09.07.2026 —
  вынесен из хардкода в FloatingAssistant.tsx, admin/page.tsx, help/page.tsx)

ВАЖНО: Variables в Vercel — в левом меню (не в Settings!)
ВАЖНО: В Битрикс24 имя = "Фамилия Имя" → приветствие: split(' ')[1]
ВАЖНО: После переподключения Б24 приложения — обновить BITRIX_CLIENT_ID/
  SECRET в Vercel + Redeploy, проверить код приложения в URL (раздел 1)
ВАЖНО: BITRIX_CLIENT_ID/SECRET также в /api/bitrix/refresh (раздел 10)
ВАЖНО: BITRIX_WEBHOOK_URL также используется для пакетного user.get.json
  (резолв bitrix_user_id → имя, см. раздел 10, обогащение дашборда —
  теперь применено во ВСЕХ трёх разделах юр. дашборда, не в одном)
ВАЖНО: после добавления/изменения NEXT_PUBLIC_* переменных — обязательно
  Redeploy в Vercel, простой push не подхватывает новые переменные сам


=============================================================
4. СТРУКТУРА БАЗЫ ДАННЫХ
=============================================================
GRANT SELECT,INSERT,UPDATE,DELETE ON public.<table> TO anon,authenticated,service_role;
RLS включён на всех таблицах.

counterparties — УНИКАЛЬНЫЙ КЛЮЧ: (inn, kpp)!
  Основные поля: id, inn, kpp, ogrn, full_name, short_name, status, risk_level,
    director_name, director_title, phone, email, legal_address, website,
    signatory_name, poa_number, poa_date, poa_expires, notes,
    actual_address, created_at, updated_at
  Иностранные: is_foreign=true, country, registration_number
  Физлица: is_individual=true, person_birth_date, passport_series,
    passport_number, passport_issued_by, passport_issued_date,
    passport_dept_code, person_snils
  Иностранные: inn='FOREIGN-{timestamp}', kpp=NULL
  Физлица: inn=ИНН (12 цифр) или 'IND-{timestamp}' если не указан
  inn_verified_at (timestamptz) — дата подтверждения ИНН физлица по алгоритму ФНС

counterparty_documents:
  category (charter/poa/order/other), Storage: counterparty-docs
  ВАЖНО: загрузка через uploadFileDirect (presigned URL)

counterparty_checks — кэш проверок надёжности (10 дней):
  inn, risk_level IN ('low','medium','high'), dadata/fssp/bankrupt/rnp JSONB
  API: POST /api/counterparties/check, GET /api/counterparties/check-cache

company_requisites:
  Все поля включая: warehouse_address (адрес склада)
  director_short_name, basis, fax

approval_sessions — ЭДО поля:
  edo_requested, edo_director_decision IN ('approved','rejected')
  signing_method IN ('edo','simple')
  status IN ('active','completed','cancelled') — 'cancelled' используется
    при отклонении документа; GET /api/approvals ВОЗВРАЩАЕТ все три статуса
  initiated_by_bitrix_id — используется также при проверке прав на смену
    номера документа (раздел 10)
  ВАЖНО: выбор специалиста ЭДО убран из UI

approval_participants:
  role IN ('required','optional','observer') — CHECK-ограничение обновлено
  status IN ('pending','approved','rejected','acknowledged','disabled',
    'completed_by_initiator','observing') — CHECK-ограничение обновлено
  stage IN ('director','legal','legal_gc','finance','accounting','other',...) —
    используется в т.ч. для проверки «юрист-участник» при смене номера
  Наблюдатель (role='observer') сразу получает status='observing' при
    создании — не блокирует allApproved, не получает push-уведомление
  ВАЖНО: ГД (director) и Бухгалтерия (accounting) — обязательны при запуске
  ⚠️ ВАЖНО: добавление участника НИКОГДА не должно само завершать
    согласование — только реальное голосование (route approve). Удаление
    участника — МОЖЕТ завершить (если все оставшиеся обязательные уже
    согласовали). Это две РАЗНЫЕ, не взаимозаменяемые проверки (раздел 10).
  НОВОЕ: запрет дублирования и добавления инициатора при добавлении
    участника — валидация на бэкенде (раздел 6)

approval_settings:
  bitrix_user_id, stage IN ('director','legal','finance','accounting','custom'),
    company_prefix, is_active — назначение согласующих по компании/этапу
  ИСПОЛЬЗУЕТСЯ также для company-scoped проверки прав вне согласования
    (contract_payments, storage-confirm) — рабочий, проверенный паттерн

contract_payments — факты оплаты по договору:
  id, contract_id, amount, payment_date, payment_type
    IN ('аванс','частичная_оплата','окончательный_расчет'),
  comment, created_by_name, created_by_bitrix_id, created_at
  Дебиторка = contract.amount − SUM(contract_payments.amount) по договору

contracts:
  storage_confirmed_at (timestamptz NULL)
  storage_confirmed_by_name (text NULL)
  storage_confirmed_by_bitrix_id (integer NULL)
  number — РЕДАКТИРУЕМОЕ поле через /api/contracts/[id]/change-number,
    проверка уникальности при сохранении (без проверки формата)

document_attachments: без колонки file_type!

system_roles: role IN ('admin','gc_manager','finance_gc','finance_company','legal_gc')
ADMIN_IDS = [30, 1148]


=============================================================
5. НУМЕРАЦИЯ, ТИПЫ, СТАТУСЫ
=============================================================
Нумерация: ПРЕФИКС-ТИПКОД-ГОД/МЕСЯЦ/НН
ВАЖНО: используем numeric max (не ORDER BY)
ВАЖНО: фильтр deleted_at=is.null УБРАН из запроса генерации номера
⚠️ ОСТАТОЧНЫЙ РИСК: race condition между select максимума и insert —
  раздел 12, пункт 6 (не устранён)
ВАЖНО: Э-К — двойной префикс → company_prefix из БД или startsWith('Э-К')
Номер можно менять вручную после создания — см. раздел 6 «Изменение номера
  документа». Уникальность проверяется, формат — нет.

Статусы: черновик → на_согласовании → согласован → [на_подписи_в_эдо →]
  загружен_частично → подписан → на_исполнении (↘ отклонён)
LOCKED_STATUSES = ['согласован','на_подписи_в_эдо','загружен_частично','подписан','на_исполнении']
⚠️ В ContractTabs.tsx есть ВТОРОЙ, отдельно захардкоженный похожий список
  для кнопки «+ Добавить» вложение (без 'на_подписи_в_эдо') — известный
  технический долг, не унифицирован, см. раздел 12, пункт 14

Приём на хранение (storage_confirmed_*) НЕ является статусом — независимый
  бейдж поверх статуса, доступен когда status IN ('подписан','на_исполнении')


=============================================================
6. РЕАЛИЗОВАННЫЙ ФУНКЦИОНАЛ
=============================================================
✅ CORE, СОГЛАСОВАНИЕ, роль «Наблюдатель», отмена отклонения, лист
   согласования, крон напоминаний, уведомление ЭДО в чат, учёт оплат/
   дебиторка, плавающий Ассистент, шаблоны Э-К, тултипы (первая волна),
   приём на хранение — без изменений с прошлых версий CONTEXT

НОВОЕ 08.07.2026:

✅ Изменение номера документа: иконка ✏️ рядом с номером в шапке карточки.
   Права: ADMIN_IDS — всегда; инициатор документа ИЛИ юрист-участник ТЕКУЩЕЙ
   сессии согласования (stage IN 'legal'/'legal_gc') — только пока статус
   НЕ в LOCKED_STATUSES. Обязательный комментарий причины. Проверяется
   только уникальность нового номера. Логируется в contract_logs; если
   есть сессия согласования — сообщение также в её чат.
   API: POST /api/contracts/[id]/change-number

✅ Запрет дублирования при добавлении участника согласования: нельзя
   добавить уже существующего участника (по bitrix_user_id), нельзя
   добавить инициатора документа (session.initiated_by_bitrix_id И
   contracts.author_bitrix_id). Валидация на бэкенде.

✅ Исправлены два связанных бага в approval_participants POST/DELETE —
   добавление НЕ-обязательного участника к завершённой сессии могло
   мгновенно закрыть документ обратно; побочный эффект фикса случайно
   задел и легитимную логику в DELETE — восстановлена. См. раздел 11,
   правило 15 — важный урок про str_replace и задублированный текст.

❌ ПОПЫТКА И ОТКАТ: онбординг-тур (driver.js). Полностью реализован, но
   подсветка стабильно уезжала — 4 разных подхода не решили проблему.
   Полностью удалён (Роман Пирог, 08.07.2026). Детали — если возвращаться,
   начинать диагностику заново с другим подходом к позиционированию.

НОВОЕ 09.07.2026:

✅ Обогащение карточек юридического дашборда именами инициаторов —
   ЗАВЕРШЕНО ПОЛНОСТЬЮ, все три раздела: «Просроченные согласования»
   (через initiated_by_name сессии), «Согласованы — ожидают подписания»
   и «Просроченные пункты чек-листа» (оба через новое поле author_name,
   один пакетный запрос к Bitrix user.get.json на все разделы разом).

✅ Возможность свернуть блок «Мои документы» + карточки статистики на
   главной странице:
   Веб: карточки статистики (Всего документов/На согласовании/На подписи
     в ЭДО/Подписаны/Черновики) НЕ сворачиваются, но уменьшены по высоте
     (~25%, компактные отступы/шрифт только на десктопе)
   Мобильная: кнопка «▲ Свернуть»/«▼ Показать» сворачивает ОБА блока разом
     (Мои документы + карточки статистики)
   РЕГРЕССИЯ И ФИКС: когда у пользователя пусто во ВСЕХ категориях (нет
     черновиков/согласований/ЭДО/инициировано) — компонент MyDocuments
     (и его кнопка «Свернуть») по ДАВНО СУЩЕСТВУЮЩЕЙ, не связанной с этой
     фичей логике (`if (!hasAnything) return null`) полностью скрывается —
     значит на мобильной кнопка сворачивания карточек статистики тоже
     пропадала вместе с ним. Добавлена РЕЗЕРВНАЯ кнопка на уровне
     app/page.tsx (md:hidden, показывается ТОЛЬКО когда MyDocuments пуст),
     управляющая тем же isCollapsed state.

✅ Хардкод URL Core вынесен в NEXT_PUBLIC_CORE_URL (3 файла: 
   FloatingAssistant.tsx, admin/page.tsx, help/page.tsx), с фоллбэком на
   старое значение на случай отсутствия переменной.

✅ КРУПНЫЙ ФИКС УСТОЙЧИВОСТИ — жалобы пользователей на зависания/таймауты
   при переходе по ссылкам из Битрикс24 («Превышено время ожидания ответа
   от сайта»). Проведена консультация с 4 внешними AI (Qwen/Gemini/
   ChatGPT/DeepSeek) — единогласное подтверждение гипотезы + ценные
   дополнения (см. раздел 10 для полной архитектуры фикса):
   - fetchWithTimeout (8-10 сек) применён на всех трёх Bitrix auth-роутах
     (callback, verify, refresh) — раньше fetch() к Битрикс24 не имел
     ВООБЩЕ никакого таймаута, мог висеть неограниченно долго
   - Логирование латентности [B24_LATENCY] в Vercel Logs — встроено в
     саму утилиту fetchWithTimeout, работает автоматически
   - Защита от цикла перезагрузок (shouldAllowReload, cooldown 15 сек) —
     раньше при невалидном verify+ошибке refresh код мог зацикленно
     перезагружать страницу, если Битрикс24 недоступен продолжительное
     время (не только заблокирован, но и работал бы каждый раз медленно)
   - Promise.all вместо трёх последовательных await в серверном
     компоненте карточки документа (app/contracts/[id]/page.tsx)
   ⚠️ НЕ РЕАЛИЗОВАНО (осознанно отложено, не хотфикс) — архитектурные
     идеи из консультации: не блокировать рендер ВСЕГО приложения через
     authLoading целиком (ChatGPT called this an anti-pattern), кэшировать
     валидность токена локально на несколько минут вместо verify на
     каждой загрузке (снижает частоту обращений к Битрикс24 в принципе).
     См. раздел 12, пункты 15-16.


=============================================================
7. ИЗВЕСТНЫЕ ПРОБЛЕМЫ / НА ПАУЗЕ
=============================================================
⚠️ ГЛУБОКАЯ ССЫЛКА ИЗ ЧАТА Б24 — частично решено. Прямой клик ВНУТРИ
   iframe чата Б24 всё ещё не работает — раздел 12, пункт 9
⚠️ PDF анализ — просим конвертировать в DOCX
⚠️ Баг: кнопка загрузки в базе знаний зависает (файл загружается) — отложено
⚠️ Обратная связь п.5+п.7, объединённые в «онбординг-тур» — ПОПЫТКА НЕ
   УДАЛАСЬ, откачена полностью (раздел 6). Тултипы (п.5) частично сделаны
   отдельно и остаются в силе — сам интерактивный тур не реализован.
⚠️ Дублирующийся, не унифицированный список LOCKED_STATUSES для кнопки
   загрузки вложений в ContractTabs.tsx — раздел 5, раздел 12
⚠️ НОВОЕ (09.07.2026): в МОБИЛЬНОМ приложении Битрикс24 новый документ,
   появившийся в «Мои документы» (добавление, назначение согласующим,
   возврат из корзины), не отображается без выхода и повторного входа в
   мобильное приложение. Вероятно связано с тем, как мобильное приложение
   Б24 кэширует iframe/данные. Не диагностировано, добавлено в план
   (раздел 12, пункт 17).


=============================================================
8. ГДЕ ОСТАНОВИЛИСЬ (09.07.2026)
=============================================================
Сделано в текущую сессию:
  ✅ Обогащение юр. дашборда именами инициаторов — ЗАВЕРШЕНО для всех
     трёх разделов (было: бэкенд готов, фронтенд только для 1 из 3)
  ✅ Возможность свернуть «Мои документы» + карточки статистики — включая
     фикс регрессии на мобильной версии при пустом состоянии
  ✅ Обсуждена и зафиксирована стратегическая архитектура платформы —
     полностью переписан CONTEXT-VITRINA.md (карта 7 модулей, принципы
     коннекторов между модулями, стратегия поэтапного переезда на Selectel,
     подтверждённый рабочий статус OpenRouter на казахстанском сервере)
  ✅ Устранён технический долг — хардкод URL Core → переменная окружения
  ✅ КРУПНЫЙ фикс устойчивости системы (таймауты, защита от циклов,
     параллельные запросы) — по итогам консультации с 4 внешними AI,
     полная синтезированная методология в разделе 10

Следующие задачи (по приоритету):
  1. ⬜ Мониторинг: через день-два проверить логи Vercel по [B24_LATENCY],
     подтвердится ли снижение жалоб на зависания
  2. ⬜ Продолжить тултипы порциями (юридический/финансовый дашборды,
     реестр контрагентов)
  3. ⬜ Общая архитектурная доработка авторизации (не блокировать рендер
     через authLoading целиком + локальное кэширование валидности токена)
     — раздел 12, пункты 15-16, из консультации по устойчивости
  4. ⬜ Диагностировать баг с обновлением документов в мобильном Б24
  5. ⬜ Составить читабельный список всех функций ЮрИнтел для базы знаний
     Ассистента — раздел 12, пункт 18
  6. ⬜ ЭПОТОС-ПЕРСОНАЛ (следующий модуль платформы)
  7. ⬜ Пункты раздела 12 (внешние консультации), по возможности
  8. ⬜ (при желании вернуться) Новый заход на интерактивный онбординг-тур


=============================================================
9. ПЛАН РАЗРАБОТКИ
=============================================================
✅ Спринт 1, 2, 3 — завершены
🔄 Спринт 4 (активен):
  ✅ Всё до 08.07.2026 включительно (см. историю в предыдущих версиях)
  ✅ Изменение номера документа
  ✅ Обогащение юр. дашборда именами инициаторов (все 3 раздела)
  ✅ Фиксы авто-закрытия согласования (add/delete participant)
  ✅ Запрет дублирования участников согласования
  ✅ Сворачиваемый блок Мои документы + статистика (веб + мобильная)
  ✅ Крупный фикс устойчивости (таймауты Б24, защита от циклов, Promise.all)
  ✅ Хардкод Core URL → переменная окружения
  ❌ Онбординг-тур — не удался, откачен, статус: на паузе
  ⬜ ЭПОТОС-ПЕРСОНАЛ

🔴 Спринт 5:
  - Шаблоны НПП
  - Импорт договоров из Excel
  - Календарь судебных заседаний
  - ЭПОТОС-РИД (сдвинут после ПЕРСОНАЛ и ПЛАН — см. CONTEXT-VITRINA)
  - Пункты раздела 12 (внешние консультации)

Порядок будущих модулей платформы (уточнено 09.07.2026, см. также
  CONTEXT-VITRINA.md): после ЮрИнтел — ПЕРСОНАЛ-ЭПОТОС, затем ПЛАН-ЭПОТОС,
  РИД сдвинут на более поздний срок.


=============================================================
10. АРХИТЕКТУРНЫЕ РЕШЕНИЯ
=============================================================
⚠️ КАРТА КОМПОНЕНТОВ КАРТОЧКИ ДОГОВОРА (см. также правило 14):
  app/contracts/[id]/page.tsx — SERVER-компонент (без хуков!). Три запроса
    к Supabase теперь через Promise.all (09.07.2026), не последовательно.
  app/components/ContractTabs.tsx — родитель, TABS-навигация + инлайн для
    простых вкладок (включая изменение номера документа); делегирует:
    execution→<ExecutionControl>, storage→<StorageConfirm>
  app/components/ExecutionControl.tsx — чек-лист + блок «Оплаты» (ВСЁ
    здесь, не в ContractTabs!)
  app/components/StorageConfirm.tsx — вкладка «Хранение», самостоятельна

Изменение номера документа:
  canChangeNumber ВЫЧИСЛЯЕТСЯ НА КЛИЕНТЕ из уже загруженных contract/
    session/user — ADMIN_IDS → true; LOCKED_STATUSES.includes(contractStatus)
    → false (кроме админа); contract.author_bitrix_id === uid → true;
    session.initiated_by_bitrix_id === uid → true; иначе — участник этой
    сессии с bitrix_user_id === uid и stage IN ('legal','legal_gc')
  API /api/contracts/[id]/change-number POST — СЕРВЕРНО повторяет ту же
    проверку прав (defense in depth), плюс проверка уникальности номера

Обогащение дашборда именами инициаторов (dashboard-legal) — ЗАВЕРШЕНО:
  Один пакетный POST к {WEBHOOK}user.get.json с filter: {ID: [...уникальные
    author_bitrix_id со всех 3 разделов]}, select: ['ID','NAME','LAST_NAME']
  ⚠️ ВАЖНАЯ ОСОБЕННОСТЬ Supabase: связь contracts!inner(...) ТИПИЗИРУЕТСЯ
    TypeScript как МАССИВ, но РЕАЛЬНО на рантайме возвращается ОДИН ОБЪЕКТ.
    Решение — приведение типа через `(x as unknown as {...}).contracts`,
    НЕ индексация массивом (`.contracts?.[0]`).

Баг авто-закрытия согласования (POST/DELETE participants):
  POST (добавление участника) — ПОСЛЕ реактивации завершённой сессии НЕ
    должен пересчитывать «все ли обязательные согласовали» — убрано.
  DELETE (удаление участника) — ЗАКОННО пересчитывает после удаления,
    восстановлено на месте после случайной затирки.

Запрет дублирования участников:
  POST /api/approvals/[sessionId]/participants проверяет ДО insert:
    1) bitrix_user_id не совпадает с session.initiated_by_bitrix_id или
       contracts.author_bitrix_id (запрет добавить инициатора)
    2) bitrix_user_id ещё не существует среди участников ЭТОЙ session_id
       (запрет дублирования)

Сворачиваемый блок «Мои документы» + статистика:
  isCollapsed — ПОДНЯТО состояние с уровня MyDocuments.tsx на app/page.tsx,
    передаётся пропсами в ОБА компонента (MyDocuments получает isCollapsed
    + onToggleCollapse; PersonalStats получает только isCollapsed)
  PersonalStats.tsx: className={isCollapsed ? 'hidden md:grid' : 'grid'}
    — ЧИСТО CSS responsive-паттерн, не нужен отдельный JS для разного
    поведения моб/веб. На десктопе ВСЕГДА grid (игнорирует isCollapsed),
    на мобильном hidden когда isCollapsed=true. Плюс md:p-3/md:text-xl
    вместо p-4/text-2xl — уменьшение высоты на ~25% ТОЛЬКО на десктопе.
  MyDocuments.tsx: имеет ДАВНО СУЩЕСТВУЮЩИЙ (не наш) early return
    `if (!hasAnything) return null` — если у пользователя пусто во ВСЕХ
    четырёх категориях, компонент НЕ рендерится вообще, включая кнопку
    сворачивания. Из-за этого на мобильной версии кнопка управления
    PersonalStats пропадала вместе с пустым MyDocuments.
  РЕШЕНИЕ: резервная кнопка в app/page.tsx (md:hidden, условие
    !hasMyDocsContent — та же логика hasAnything, продублированная на
    уровне страницы из уже загруженных данных, без доп. запроса),
    управляет тем же setIsCollapsed. На вебе не нужна (PersonalStats
    там всегда видна независимо от isCollapsed).

Устойчивость к зависаниям Битрикс24 (09.07.2026, по итогам 4-AI консультации):
  app/lib/fetchWithTimeout.ts — обёртка над fetch(): AbortController с
    настраиваемым timeoutMs (по умолчанию 8000), опция label для
    логирования, автоматический console.log/error с префиксом
    [B24_LATENCY] (успех: "label -> status (Nms)"; таймаут: "label ->
    TIMEOUT after Nms"). Применена в:
    - /api/bitrix/verify (8с, label: 'bitrix-verify')
    - /api/bitrix/callback (8с по умолчанию, label: 'bitrix-callback')
    - /api/bitrix/refresh (10с, OAuth-обмен обычно чуть медленнее)
  shouldAllowReload() в useBitrixAuth.ts — модульная функция (не хук),
    хранит timestamp последней попытки в sessionStorage под ключом
    'bitrix_reload_attempted_at', разрешает reload не чаще раза в 15 сек
    (RELOAD_COOLDOWN_MS). Обёрнуты все 3 существовавших вызова
    window.location.reload() в ветке невалидного verify/неудачного refresh.
  Диагностика ПЕРЕД фиксом: единогласно все 4 внешних AI подтвердили
    гипотезу «fetch к Битрикс24 без таймаута в критическом пути
    авторизации» — но с важными уточнениями от каждого (Qwen: browser-level
    timeout обычно означает зависание НА СЕРВЕРЕ/SSR, не просто клиентский
    fetch — проверили middleware.ts, которого НЕТ в проекте, что делает
    эту теорию менее вероятной именно для нас; Gemini: вероятнее цикл
    reload при повторяющихся зависаниях — отсюда и появился фикс
    shouldAllowReload; ChatGPT: архитектурная критика authLoading как
    единой точки блокировки всего рендера — НЕ исправлено, раздел 12).

Тултипы, Tooltip.tsx, StorageConfirm, плавающий Ассистент, шаблоны — без
  изменений с прошлых версий CONTEXT


=============================================================
11. ПРАВИЛА РАБОТЫ
=============================================================
1. Новые файлы — New-Item в PowerShell (относительный путь если терминал
   уже в папке проекта)
2. Редактирование — ТОЛЬКО Ctrl+H в VS Code
3. Не более 3 блоков кода за раз, ждать подтверждения
4. Перед деплоем КОДА — npm run build
5. CONTEXT — без build, сразу git push (для CONTEXT-YURINTEL.md — это
   файл В РЕПОЗИТОРИИ, деплоится через git; CONTEXT-VITRINA.md — НЕ в
   репозитории, это файл базы знаний проекта Claude, деплою не подлежит,
   заменяется вручную загрузкой)
6. При зависании деплоя — git commit --allow-empty + git push
7. Repomix — три конфига (api/components/pages)
8. Деплой (три команды построчно в одном блоке): git add -A / commit / push
9. Если фрагмент не найден → обновить repomix, ИСКАТЬ ТОЧНЫЙ путь файла
   через grep/awk — никогда не угадывать
10. git log завис → нажать q
11. check_xml.py: INPUT = 'ИмяФайла.docx'
12. Диск проекта — E:\; repomix-файлы версionируются, конфликт при
    revert/checkout — git checkout --theirs
13. Server-компоненты (без хуков, напр. app/contracts/[id]/page.tsx) —
    хуки/context добавлять в дочерний client-компонент
14. При поиске кода НЕ полагаться на память «должно быть в файле X» —
    подтверждать awk/grep, В КАКОМ ИМЕННО файле встречается строка.
    Если 2 попытки подряд не находят код — не пробовать третий раз по
    памяти, а просить пользователя прислать точный фрагмент из редактора
15. Если два РАЗНЫХ обработчика (например POST и DELETE в одном route.ts)
    содержат ПОХОЖИЙ или СКОПИРОВАННЫЙ фрагмент кода — правка ОДНОГО через
    str_replace рискует случайно задеть и другой. После правки в файле с
    несколькими похожими обработчиками — ПЕРЕПРОВЕРЯТЬ через свежий
    repomix, что СОСЕДНИЕ функции не пострадали, не полагаться только на
    успешный build (TypeScript не поймает случайно скопированную бизнес-логику)
16. При системных сбоях (баг «ходим по кругу», 3+ попытки чинить одно и то
    же без результата) — не предлагать пятую догадку подряд: честно
    остановиться, предложить простые диагностические шаги, или откатить/
    удалить фичу целиком. Пример — раздел 6, онбординг-тур.
17. НОВОЕ (09.07.2026): при СЛОЖНЫХ, неочевидных production-проблемах
    (зависания, таймауты, нестабильность — не локализуемые быстро по
    коду) — подготовить структурированный документ-консультацию (симптом
    + архитектура + гипотеза + конкретный код + прямые вопросы) и
    отправить НЕСКОЛЬКИМ внешним AI параллельно, а не пытаться решить
    в одиночку методом проб и ошибок. Подтверждённая рабочая методика —
    см. раздел 6, фикс устойчивости. Особенно ценно: разные AI подмечают
    РАЗНЫЕ нюансы одной и той же проблемы (пример — Qwen про browser-level
    timeout и SSR, ChatGPT про архитектурный анти-паттерн authLoading),
    сумма находок больше, чем у любого одного источника. Также стоит
    ПРОВЕРЯТЬ специфичные технические утверждения от консультаций (не
    все догадки верны — пример: DeepSeek назвал конкретные имена
    компонентов, которых ему не давали, это была неподтверждённая догадка).


=============================================================
12. ПРЕДЛОЖЕНИЯ ИЗ ВНЕШНИХ AI-КОНСУЛЬТАЦИЙ И СОБСТВЕННЫЙ БЭКЛОГ
=============================================================
1. ⬜ getCompanyPrefix.ts — устранить дублирование парсинга company_prefix
2. ✅ ВЫПОЛНЕНО — ручной ввод факта оплаты (contract_payments)
3. ⬜ Хэш документа (SHA-256) в листе согласования — compliance/аудит
4. ⬜ Перенос SPECIAL_SIGNERS/ADMIN_IDS из хардкода в Supabase-таблицы
5. ⬜ Декомпозиция ContractTabs.tsx — файл огромный, продолжает расти
   (изменение номера документа тоже добавлено инлайн)
6. ⬜ Race condition при генерации номера договора — уникальный констрейнт
   + retry при конфликте
7. ⬜ lib/contractStatus.ts — единый модуль меток/цветов статуса договора
8. ⬜ Проверить и убрать дефолтный JWT-секрет OnlyOffice
9. ⬜ postMessage как альтернатива refresh_id для клика ВНУТРИ iframe чата Б24
10. ⬜ «Паспорт контрагента» / risk-паттерны по истории договоров
11. ✅ ВЫПОЛНЕНО — сворачивание блока Мои документы + статистика (09.07.2026)
12. ✅ ВЫПОЛНЕНО — крупный фикс устойчивости/скорости по конкретной жалобе
    (таймауты Б24, защита от циклов, Promise.all). Общий, не-реактивный
    аудит (профилирование остальных частей системы) — НЕ проводился,
    актуален как отдельная, менее срочная задача, если появятся новые сигналы.
13. ⬜ Перенос ЭПОТОС-Витрины на Selectel — детали и стратегия ПОЛНОСТЬЮ
    перенесены в CONTEXT-VITRINA.md, здесь больше не дублируется
14. ⬜ Унифицировать дублирующийся список статусов для кнопки «+ Добавить»
    вложение в ContractTabs.tsx с общей константой LOCKED_STATUSES
15. ⬜ (НОВОЕ, из консультации по устойчивости) Архитектурный анти-паттерн:
    authLoading блокирует рендер ВСЕГО приложения через множественные
    `if (authLoading) return null`. Один внешний сервис (Битрикс24)
    полностью контролирует, покажется ли приложение вообще. Предложение
    ChatGPT: рендерить каркас/skeleton страницы сразу, блокировать только
    действия, требующие подтверждённой авторизации. Не хотфикс — заметная
    архитектурная работа, требует отдельного планирования.
16. ⬜ (НОВОЕ, из консультации по устойчивости) Снизить частоту обращений
    к Битрикс24 API: сейчас verify дёргается на КАЖДОЙ загрузке страницы.
    Идеи: локальное кэширование факта валидности токена на несколько минут
    (DeepSeek — не дёргать verify, если auth_id выдан недавно, известный
    примерный срок жизни токена), либо собственный короткоживущий JWT
    поверх подтверждённой Bitrix-сессии (ChatGPT, TTL ~10 минут). Снижает
    зависимость от отзывчивости Битрикс24 в целом, не только защищает от
    зависаний. Требует отдельного проектирования.
17. ⬜ (НОВОЕ) Новый документ в «Мои документы» не появляется в МОБИЛЬНОМ
    приложении Битрикс24 без выхода/повторного входа — раздел 7. Не
    диагностировано, вероятно кэширование iframe/данных мобильным
    приложением Б24.
18. ⬜ (НОВОЕ) Составить читабельный, сгруппированный список ВСЕХ функций
    и возможностей ЮрИнтел простым человеческим языком (не текущая
    громоздкая инструкция, которую никто не читает). Цель — материал для
    объединения базы знаний Ассистента ПОСЛЕ завершения основной
    разработки ЮрИнтел, когда все накопленные материалы (Дополнения
    v1.1-v1.4 и далее, эта инструкция, FAQ) будут сведены в единую,
    обновлённую базу знаний.

Также отмечено (не приоритет на ближайшие спринты, горизонт 6-12 месяцев):
крупный рефакторинг Domain/Service Layer, интеграция с ЭДО-операторами,
вынос PDF-парсинга в микросервис, rate limiting, CSP/helmet, серверная
проверка identity вместо доверия bitrix_user_id из тела запроса.
